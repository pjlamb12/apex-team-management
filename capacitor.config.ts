import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apexteam.app',
  appName: 'Apex Team',
  webDir: 'dist/apps/frontend/browser'
};

export default config;
