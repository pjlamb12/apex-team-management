---
phase: 11-practice-management-schedule
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/api/src/events/events.module.ts
  - apps/api/src/events/events.service.ts
  - apps/api/src/events/events.controller.ts
  - apps/api/src/events/dto/create-event.dto.ts
  - apps/api/src/events/dto/update-event.dto.ts
  - apps/api/src/app/app.module.ts
autonomous: true
requirements:
  - PRAC-01
  - PRAC-02
  - SCHD-02

must_haves:
  truths:
    - "API endpoints are accessible via /api/teams/:id/events"
    - "New practices inherit location from season.defaultPracticeLocation"
    - "Events list can be filtered by 'upcoming' or 'past' scope"
  artifacts:
    - path: "apps/api/src/events/events.service.ts"
      provides: "Event business logic"
      contains: "class EventsService"
    - path: "apps/api/src/events/events.controller.ts"
      provides: "Event API endpoints"
      contains: "@Controller('teams/:teamId/events')"
  key_links:
    - from: "EventsController"
      to: "EventsService"
      via: "Dependency Injection"
---

<objective>
Refactor the backend API from "Games" to "Events" and implement practice-specific logic, including season default inheritance and chronological filtering.

Purpose: Provide a unified API for all team events and automate practice setup.
Output: EventsService, EventsController, and updated DTOs supporting both games and practices.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-practice-management-schedule/11-01-SUMMARY.md
@.planning/phases/11-practice-management-schedule/11-CONTEXT.md
@.planning/phases/11-practice-management-schedule/11-RESEARCH.md

<interfaces>
From apps/api/src/entities/event.entity.ts (as of 11-01):
```typescript
@Entity('events')
export class EventEntity {
  @PrimaryGeneratedColumn('uuid')
  id: string;
  @Column({ type: 'enum', enum: ['game', 'practice'], default: 'game' })
  type: 'game' | 'practice';
  @Column({ name: 'season_id' })
  seasonId: string;
  @Column({ nullable: true })
  opponent: string;
  @Column({ name: 'scheduled_at', type: 'timestamp', nullable: true })
  scheduledAt: Date;
  @Column({ nullable: true })
  location: string | null;
  @Column({ name: 'duration_minutes', nullable: true, type: 'integer' })
  durationMinutes: number | null;
  @Column({ type: 'text', nullable: true })
  notes: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Folder & Service Rename</name>
  <files>
    apps/api/src/events/events.module.ts,
    apps/api/src/events/events.service.ts,
    apps/api/src/events/events.controller.ts,
    apps/api/src/events/dto/create-event.dto.ts,
    apps/api/src/events/dto/update-event.dto.ts
  </files>
  <action>
    1. Rename apps/api/src/games/ to apps/api/src/events/.
    2. Rename files within to use 'events' prefix (e.g., games.service.ts -> events.service.ts).
    3. Rename classes: GamesService -> EventsService, GamesController -> EventsController.
    4. Update @Controller path to 'teams/:teamId/events'.
    5. Update DTOs to include 'type', 'durationMinutes', and 'notes'. Rename CreateGameDto -> CreateEventDto (if applicable, ensuring no conflict with existing in-game event DTOs).
    6. Update imports in app.module.ts or other consumers.
  </action>
  <verify>
    <automated>npx nx test api --testNamePattern="Events"</automated>
  </verify>
  <done>Backend structure renamed to 'events', existing game functionality preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Practice CRUD & Default Inheritance</name>
  <files>apps/api/src/events/events.service.ts, apps/api/src/events/events.service.spec.ts</files>
  <behavior>
    - When creating an event with type='practice', if location is missing, it must inherit from season.defaultPracticeLocation.
    - Creating an event with type='game' should still function normally (opponent required, etc.).
    - Practices should allow 'durationMinutes' and 'notes' to be saved.
  </behavior>
  <action>
    Implement logic in EventsService.create to:
    1. Fetch the active season for the team.
    2. If type is 'practice' and location is not provided in DTO, set location to season.defaultPracticeLocation.
    3. Save the event with all practice-specific fields.
  </action>
  <verify>
    <automated>npx nx test api --testNamePattern="EventsService.create"</automated>
  </verify>
  <done>Practices can be created with automatic default location inheritance.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Scoped Fetching for Unified Schedule</name>
  <files>apps/api/src/events/events.service.ts, apps/api/src/events/events.controller.ts</files>
  <action>
    Update EventsService.findAllForTeam to support a 'scope' parameter:
    1. 'upcoming' (default): Fetch events where scheduledAt >= now, sorted ASC by scheduledAt.
    2. 'past': Fetch events where scheduledAt < now, sorted DESC by scheduledAt.
    Expose this in EventsController via @Query('scope') scope: 'upcoming' | 'past'.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/teams/{teamId}/events?scope=upcoming</automated>
  </verify>
  <done>API supports filtered chronological list of all team events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Controller | Entry point for untrusted client data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | EventsService | mitigate | Ensure team ownership verification is present in all EventsService CRUD methods. |
| T-11-02-02 | Elevation of Privilege | EventsController | mitigate | Use JWT guards to verify userId before processing requests. |
</threat_model>

<verification>
Run backend integration tests ensuring both games and practices can be managed and that the unified schedule returns events in the correct order.
</verification>

<success_criteria>
- Backend renamed to 'events'.
- Practice creation inherits season defaults.
- /api/teams/:id/events?scope=upcoming returns chronological list.
</success_criteria>

<output>
After completion, create .planning/phases/11-practice-management-schedule/11-02-SUMMARY.md
</output>
