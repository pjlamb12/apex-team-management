# Plan 13-04: Live Console Period Tracking

**Phase:** Phase 13: Advanced Game Management
**Goal:** Implement period tracking and game completion in the Live Console.

## Status
- [x] Add `currentPeriod` and `status` to `LiveGameStateService`
- [x] Include `period` in logged `GameEvent`s
- [x] Add `nextPeriod()` and `endGame()` methods to `LiveGameStateService`
- [x] Display current period in `ClockDisplay`
- [x] Add "Next Period" and "End Game" buttons to `ConsoleWrapper`
- [x] Sync `currentPeriod` and `status` to backend when they change

## Proposed Changes

### 1. `libs/client/feature/game-console/src/lib/live-game-state.service.ts`
- Track `currentPeriod` (default 1) and `status` ('in_progress').
- Tag each event with the current period.
- Add methods to transition periods and end the game.

### 2. `libs/client/feature/game-console/src/lib/clock-display/`
- Show current period label (e.g., "P1", "P2").

### 3. `libs/client/feature/game-console/src/lib/console-wrapper/`
- Add buttons for period control and game finalization.
- Sync changes to the backend `EventEntity`.

## Verification Plan

### Automated Tests
- Run `npm run test client-feature-game-console` to ensure no regressions.
- (Optional) Add unit tests for `LiveGameStateService` period tracking.

### Manual Verification
- Open Live Console.
- Log a few events in Period 1.
- Click "Next Period". Verify clock resets and new events are tagged with Period 2.
- Click "End Game". Verify redirection and game status update on backend.
