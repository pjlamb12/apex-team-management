# Plan 13-02: Backend Business Logic

**Phase:** Phase 13: Advanced Game Management
**Goal:** Implement inheritance logic for Game Format fields.

## Status
- [x] Inherit `periodCount` and `periodLengthMinutes` in `EventsService.create`
- [ ] Add unit test for inheritance logic

## Proposed Changes

### 1. `apps/api/src/events/events.service.ts`
- Update `create()` to check for `periodCount` and `periodLengthMinutes` in `activeSeason` and use them if not provided in `dto`.

## Verification Plan

### Automated Tests
- Update `apps/api/src/events/events.service.spec.ts` to include a test case for inheriting game format fields.
