# Plan 13-03: Frontend Season & Game Forms

**Phase:** Phase 13: Advanced Game Management
**Goal:** Add Game Format fields to Season and Game creation/edit forms.

## Status
- [x] Update `Season` and `Game` shared models
- [x] Add Game Format fields to `SeasonDetail` (create/edit)
- [x] Add Game Format fields to `CreateEvent`
- [x] Add Game Format fields to `EditEvent`

## Proposed Changes

### 1. `libs/shared/util/models/src/lib/season.model.ts` & `game.model.ts`
- Add `periodCount`, `periodLengthMinutes`, `currentPeriod`, `status` fields.

### 2. `apps/frontend/src/app/teams/seasons/season-detail/`
- Update form to include `periodCount` and `periodLengthMinutes`.
- Default to 2 periods of 45 minutes.

### 3. `apps/frontend/src/app/teams/events/create-event/`
- Update form and submission logic.
- Inherit defaults from `activeSeason`.

### 4. `apps/frontend/src/app/teams/events/edit-event/`
- Update form and submission logic.

## Verification Plan

### Automated Tests
- Run `npm run lint frontend` to ensure no template errors.
- Run `npm run build frontend` to ensure no compilation errors.

### Manual Verification
- Verify that creating a new season with custom game format works.
- Verify that creating a new game inherits the season defaults.
- Verify that editing a game allows overriding the format.
