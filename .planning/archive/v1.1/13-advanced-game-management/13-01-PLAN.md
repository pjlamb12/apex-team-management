# Plan 13-01: Backend Schema & DTOs

**Phase:** Phase 13: Advanced Game Management
**Goal:** Enhance Backend with Game Format support and period tracking.

## Status
- [x] Add Game Format fields to `EventEntity`
- [x] Add Game Format fields to `SeasonEntity` DTOs
- [x] Add Game Format fields to `EventEntity` DTOs
- [x] Create Database Migration

## Proposed Changes

### 1. `apps/api/src/entities/event.entity.ts`
- Add `periodCount: number | null` (nullable integer).
- Add `periodLengthMinutes: number | null` (nullable integer).
- Add `currentPeriod: number` (integer, default: 1).

### 2. `apps/api/src/teams/dto/create-season.dto.ts` & `update-season.dto.ts`
- Add `periodCount` and `periodLengthMinutes` (optional integers).

### 3. `apps/api/src/events/dto/create-event.dto.ts` & `update-event.dto.ts`
- Add `periodCount` and `periodLengthMinutes` (optional integers).
- Add `currentPeriod` (optional integer).

### 4. Database Migration
- Add `period_count`, `period_length_minutes`, `current_period` columns to `events` table.

## Verification Plan

### Automated Tests
- Run `npm run test api` to ensure no regressions in entities and services.
- Test `EventsService.create` and `update` with new fields.
- Test `SeasonsService.create` and `update` with new fields.

### Manual Verification
- Check `events` table schema in the database.
- Verify DTO validation with invalid inputs (e.g., negative period count).
