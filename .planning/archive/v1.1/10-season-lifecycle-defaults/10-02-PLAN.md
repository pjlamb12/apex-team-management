---
phase: 10-season-lifecycle-defaults
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - apps/api/src/teams/seasons.service.ts
  - apps/api/src/teams/seasons.service.spec.ts
autonomous: true
requirements:
  - SEAS-05
  - SEAS-06
must_haves:
  truths:
    - Only one season per team can be active at a time.
    - Season cannot be deleted if games exist.
  artifacts:
    - path: apps/api/src/teams/seasons.service.ts
      provides: Logic enforcement
    - path: apps/api/src/teams/seasons.service.spec.ts
      provides: Test coverage
---

<objective>
Implement core business rules for season lifecycle: transactional active state management and restricted deletion based on existing game data.

Purpose: Ensure data consistency and prevent orphaned data.
Output: Updated service and comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-season-lifecycle-defaults/10-CONTEXT.md
@.planning/phases/10-season-lifecycle-defaults/10-RESEARCH.md
@.planning/phases/10-season-lifecycle-defaults/10-VALIDATION.md
@apps/api/src/teams/seasons.service.ts
@apps/api/src/entities/game.entity.ts
</context>

<tasks>

<task type="auto">
  <name>Implement transactional isActive management</name>
  <files>apps/api/src/teams/seasons.service.ts</files>
  <action>
    - Inject `DataSource` in `SeasonsService`.
    - Update `create()` and `update()` methods to use `dataSource.transaction`.
    - If `isActive` is `true`, update all other seasons for the same `teamId` to `isActive = false` within the transaction.
  </action>
  <verify>
    <automated>npx nx build api</automated>
  </verify>
  <done>Active season policy enforced at the service level.</done>
</task>

<task type="auto">
  <name>Implement restricted delete policy</name>
  <files>apps/api/src/teams/seasons.service.ts</files>
  <action>
    - In `remove()`, check for existing games associated with the season (using `GameEntity`).
    - If games exist, throw a `ConflictException` with a clear message.
    - Otherwise, proceed with deletion.
  </action>
  <verify>
    <automated>npx nx build api</automated>
  </verify>
  <done>Delete protection implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Add unit tests for SeasonsService</name>
  <files>apps/api/src/teams/seasons.service.spec.ts</files>
  <behavior>
    - `update` setting `isActive=true` deactivates other seasons.
    - `remove` throws `ConflictException` if season has games.
    - `remove` succeeds if season has no games.
  </behavior>
  <action>
    Create comprehensive unit tests in `apps/api/src/teams/seasons.service.spec.ts` covering the new business logic.
  </action>
  <verify>
    <automated>npx nx test api --testFile=seasons.service.spec.ts</automated>
  </verify>
  <done>Service logic verified with tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal logic | Business rules are enforced regardless of the input source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Data Inconsistency | `SeasonsService.update` | mitigate | Use database transaction to prevent race conditions during active flag toggle. |
</threat_model>

<verification>
Run unit tests to ensure that setting a season to active correctly deactivates others and that deletion is blocked when games are present.
</verification>

<success_criteria>
- Only one active season per team at any time.
- No accidental deletion of seasons with historical game data.
</success_criteria>

<output>
After completion, create `.planning/phases/10-season-lifecycle-defaults/10-02-SUMMARY.md`
</output>
