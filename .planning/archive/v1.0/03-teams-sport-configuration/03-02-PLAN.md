---
phase: 03-teams-sport-configuration
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/frontend/src/app/teams/teams-list/teams-list.spec.ts
  - apps/frontend/src/app/teams/create-team/create-team.spec.ts
  - apps/frontend/src/app/shell/shell.spec.ts
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-05

must_haves:
  truths:
    - "Frontend test stub files exist for all three Wave 0 Angular components"
    - "Each stub file compiles with pending/todo test cases"
  artifacts:
    - path: "apps/frontend/src/app/teams/teams-list/teams-list.spec.ts"
      provides: "Test stubs for TEAM-05 delete flow"
      min_lines: 30
    - path: "apps/frontend/src/app/teams/create-team/create-team.spec.ts"
      provides: "Test stubs for TEAM-01 form submit and TEAM-02 sport dropdown"
      min_lines: 30
    - path: "apps/frontend/src/app/shell/shell.spec.ts"
      provides: "Smoke test stub for tab bar render"
      min_lines: 15
  key_links:
    - from: "apps/frontend/src/app/teams/teams-list/teams-list.spec.ts"
      to: "apps/frontend/src/app/teams/teams-list/teams-list.ts"
      via: "import TeamsList"
      pattern: "import.*TeamsList"
    - from: "apps/frontend/src/app/teams/create-team/create-team.spec.ts"
      to: "apps/frontend/src/app/teams/create-team/create-team.ts"
      via: "import CreateTeam"
      pattern: "import.*CreateTeam"
---

<objective>
Wave 0: Create failing test stub files for the Angular frontend components.

Purpose: Establish test contracts for the three Angular components before implementation. Per VALIDATION.md, these spec files must exist before Plan 06 implements the components.

Output: Three spec files — `teams-list.spec.ts`, `create-team.spec.ts`, and `shell.spec.ts` — all with Angular TestBed setup stubs and pending test cases that define the expected behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-teams-sport-configuration/03-VALIDATION.md
@.planning/phases/03-teams-sport-configuration/03-RESEARCH.md
@.planning/phases/03-teams-sport-configuration/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create teams-list.spec.ts stub (TEAM-05 delete flow)</name>
  <files>apps/frontend/src/app/teams/teams-list/teams-list.spec.ts</files>
  <read_first>
    - apps/frontend/src/app/auth/login/login.ts
    - apps/frontend/src/app/auth/auth.service.ts
  </read_first>
  <action>
Create `apps/frontend/src/app/teams/teams-list/teams-list.spec.ts`. The directory `apps/frontend/src/app/teams/teams-list/` does not yet exist — create it.

The spec tests the `TeamsList` component's delete flow (TEAM-05). Because `TeamsList` does not exist yet, the import will fail compilation until Plan 06 creates the component — this is intentional.

```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TeamsList } from './teams-list';
import { HttpClient } from '@angular/common/http';
import { AlertController } from '@ionic/angular/standalone';
import { RuntimeConfigLoaderService } from 'runtime-config-loader';
import { Router } from '@angular/router';
import { vi, describe, it, expect, beforeEach } from 'vitest';

describe('TeamsList', () => {
  let component: TeamsList;
  let fixture: ComponentFixture<TeamsList>;
  let alertCtrlMock: Partial<AlertController>;
  let httpMock: Partial<HttpClient>;

  beforeEach(async () => {
    alertCtrlMock = {
      create: vi.fn().mockResolvedValue({
        present: vi.fn().mockResolvedValue(undefined),
      }),
    };
    httpMock = {
      get: vi.fn().mockReturnValue({ pipe: vi.fn() }),
      delete: vi.fn().mockReturnValue({ pipe: vi.fn() }),
    };

    await TestBed.configureTestingModule({
      imports: [TeamsList],
      providers: [
        { provide: HttpClient, useValue: httpMock },
        { provide: AlertController, useValue: alertCtrlMock },
        { provide: RuntimeConfigLoaderService, useValue: { getConfigObjectKey: () => 'http://localhost:3000' } },
        { provide: Router, useValue: { navigate: vi.fn() } },
      ],
    }).compileComponents();

    fixture = TestBed.createComponent(TeamsList);
    component = fixture.componentInstance;
  });

  describe('delete team (TEAM-05)', () => {
    it('should call AlertController.create when delete is triggered', async () => {
      const team = { id: 'team-1', name: 'Thunder FC', sport: { name: 'Soccer' } };
      await (component as unknown as { confirmDelete: (t: unknown) => Promise<void> }).confirmDelete(team);
      expect(alertCtrlMock.create).toHaveBeenCalled();
    });

    it('should present an alert with header "Delete Team"', async () => {
      const team = { id: 'team-1', name: 'Thunder FC', sport: { name: 'Soccer' } };
      await (component as unknown as { confirmDelete: (t: unknown) => Promise<void> }).confirmDelete(team);
      expect(alertCtrlMock.create).toHaveBeenCalledWith(
        expect.objectContaining({ header: 'Delete Team' })
      );
    });
  });
});
```
  </action>
  <verify>
    <automated>test -f apps/frontend/src/app/teams/teams-list/teams-list.spec.ts && grep -q "confirmDelete" apps/frontend/src/app/teams/teams-list/teams-list.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/app/teams/teams-list/teams-list.spec.ts` exists
    - File contains `import.*TeamsList.*from.*'./teams-list'`
    - File contains `describe('TeamsList'` block
    - File contains `describe('delete team (TEAM-05)'` block
    - File contains `alertCtrlMock.create` assertion
    - File contains `header: 'Delete Team'` assertion (per UI-SPEC copywriting contract)
  </acceptance_criteria>
  <done>teams-list.spec.ts exists with delete flow stubs for TEAM-05</done>
</task>

<task type="auto">
  <name>Task 2: Create create-team.spec.ts and shell.spec.ts stubs (TEAM-01, TEAM-02)</name>
  <files>
    apps/frontend/src/app/teams/create-team/create-team.spec.ts
    apps/frontend/src/app/shell/shell.spec.ts
  </files>
  <read_first>
    - apps/frontend/src/app/auth/signup/signup.ts
  </read_first>
  <action>
Create two files:

**File 1: `apps/frontend/src/app/teams/create-team/create-team.spec.ts`**

Create the directory `apps/frontend/src/app/teams/create-team/` first.

```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CreateTeam } from './create-team';
import { HttpClient } from '@angular/common/http';
import { RuntimeConfigLoaderService } from 'runtime-config-loader';
import { Router } from '@angular/router';
import { vi, describe, it, expect, beforeEach } from 'vitest';

describe('CreateTeam', () => {
  let component: CreateTeam;
  let fixture: ComponentFixture<CreateTeam>;
  let httpMock: Partial<HttpClient>;
  let routerMock: { navigate: ReturnType<typeof vi.fn> };

  beforeEach(async () => {
    httpMock = {
      get: vi.fn().mockReturnValue({ pipe: vi.fn() }),
      post: vi.fn().mockReturnValue({ pipe: vi.fn() }),
    };
    routerMock = { navigate: vi.fn().mockResolvedValue(true) };

    await TestBed.configureTestingModule({
      imports: [CreateTeam],
      providers: [
        { provide: HttpClient, useValue: httpMock },
        { provide: RuntimeConfigLoaderService, useValue: { getConfigObjectKey: () => 'http://localhost:3000' } },
        { provide: Router, useValue: routerMock },
      ],
    }).compileComponents();

    fixture = TestBed.createComponent(CreateTeam);
    component = fixture.componentInstance;
  });

  describe('sport dropdown (TEAM-02)', () => {
    it('should load sports from GET /sports on init', async () => {
      const sports = [{ id: 'sport-1', name: 'Soccer', isEnabled: true }];
      (httpMock.get as ReturnType<typeof vi.fn>).mockReturnValue({
        // firstValueFrom-compatible mock
        subscribe: (fn: (v: unknown) => void) => fn(sports),
        [Symbol.observable ?? '@@observable']: vi.fn(),
      });
      // Trigger ngOnInit
      fixture.detectChanges();
      expect(httpMock.get).toHaveBeenCalledWith(expect.stringContaining('/sports'));
    });
  });

  describe('form submit (TEAM-01)', () => {
    it('should navigate to /teams after successful form submit', async () => {
      // Set up form with valid data
      component['form'].setValue({ name: 'Thunder FC', sportId: 'sport-1' });
      // Mock successful POST
      (httpMock.post as ReturnType<typeof vi.fn>).mockReturnValue({
        subscribe: (fn: (v: unknown) => void) => fn({ id: 'team-1', name: 'Thunder FC' }),
        [Symbol.observable ?? '@@observable']: vi.fn(),
      });

      await (component as unknown as { submit: () => Promise<void> }).submit();

      expect(routerMock.navigate).toHaveBeenCalledWith(['/teams']);
    });

    it('should not submit if form is invalid', async () => {
      component['form'].setValue({ name: '', sportId: '' });
      await (component as unknown as { submit: () => Promise<void> }).submit();
      expect(httpMock.post).not.toHaveBeenCalled();
    });
  });
});
```

**File 2: `apps/frontend/src/app/shell/shell.spec.ts`**

Create the directory `apps/frontend/src/app/shell/` first.

```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Shell } from './shell';
import { RouterTestingModule } from '@angular/router/testing';
import { describe, it, expect, beforeEach } from 'vitest';

describe('Shell', () => {
  let component: Shell;
  let fixture: ComponentFixture<Shell>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [Shell, RouterTestingModule],
    }).compileComponents();

    fixture = TestBed.createComponent(Shell);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create the shell component', () => {
    expect(component).toBeTruthy();
  });

  it('should render an ion-tabs element', () => {
    const el: HTMLElement = fixture.nativeElement;
    expect(el.querySelector('ion-tabs')).toBeTruthy();
  });

  it('should render an ion-tab-bar with the Teams tab button', () => {
    const el: HTMLElement = fixture.nativeElement;
    const tabBar = el.querySelector('ion-tab-bar');
    expect(tabBar).toBeTruthy();
  });
});
```
  </action>
  <verify>
    <automated>test -f apps/frontend/src/app/teams/create-team/create-team.spec.ts && test -f apps/frontend/src/app/shell/shell.spec.ts && grep -q "describe('CreateTeam'" apps/frontend/src/app/teams/create-team/create-team.spec.ts && grep -q "describe('Shell'" apps/frontend/src/app/shell/shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/app/teams/create-team/create-team.spec.ts` exists
    - `apps/frontend/src/app/shell/shell.spec.ts` exists
    - create-team.spec.ts contains `import.*CreateTeam.*from.*'./create-team'`
    - create-team.spec.ts contains test asserting `httpMock.get` is called with path containing `/sports`
    - create-team.spec.ts contains test asserting `routerMock.navigate` is called with `['/teams']` on success
    - shell.spec.ts contains `import.*Shell.*from.*'./shell'`
    - shell.spec.ts contains test checking `el.querySelector('ion-tabs')` is truthy
  </acceptance_criteria>
  <done>create-team.spec.ts (TEAM-01, TEAM-02) and shell.spec.ts (smoke) stubs exist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files only | Wave 0 creates test stubs — no production code, no trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W0-02 | Tampering | Frontend test stubs | accept | Test files have no production attack surface; all HTTP calls are mocked |
</threat_model>

<verification>
After both tasks:
- `test -f apps/frontend/src/app/teams/teams-list/teams-list.spec.ts`
- `test -f apps/frontend/src/app/teams/create-team/create-team.spec.ts`
- `test -f apps/frontend/src/app/shell/shell.spec.ts`
</verification>

<success_criteria>
- All three frontend spec files exist
- teams-list.spec.ts covers AlertController.create called with `header: 'Delete Team'`
- create-team.spec.ts covers sport dropdown load from /sports and form submit navigates to /teams
- shell.spec.ts has smoke test for ion-tabs render
- All Wave 0 items from VALIDATION.md are created (5 spec files total across Plans 01 and 02)
</success_criteria>

<output>
After completion, create `.planning/phases/03-teams-sport-configuration/03-02-SUMMARY.md`
</output>
