---
phase: 09-advanced-game-management
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - libs/shared/util/models/src/lib/season.model.ts
  - libs/shared/util/models/src/lib/game.model.ts
  - libs/shared/util/models/src/lib/lineup-entry.model.ts
  - apps/frontend/src/app/teams/edit-team/edit-team.ts
  - apps/frontend/src/app/teams/edit-team/edit-team.html
  - apps/frontend/src/app/teams/games/create-game/create-game.ts
  - apps/frontend/src/app/teams/games/create-game/create-game.html
autonomous: false
requirements: [SEAS-01, SEAS-02, GAME-05]
must_haves:
  truths:
    - "Coach can set season-level defaults in Settings"
    - "Game creation UI has Home/Away toggle"
    - "Location and Color fields auto-populate when Home/Away is toggled"
    - "Manual overrides in Game Creation are preserved unless toggle changes"
  artifacts:
    - path: "apps/frontend/src/app/teams/edit-team/edit-team.html"
      provides: "UI for season defaults"
    - path: "libs/shared/util/models/src/lib/lineup-entry.model.ts"
      provides: "Shared LineupEntry interface"
  key_links:
    - from: "apps/frontend/src/app/teams/games/create-game/create-game.ts"
      to: "Season settings"
      via: "reactive pre-population of fields"
---

<objective>
Implement the UI for managing season-level defaults and automate the game creation flow using these defaults.

Purpose: Reduce data entry effort for coaches by providing sensible, context-aware defaults.
Output: Updated season settings UI and a reactive game creation form.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-advanced-game-management/09-CONTEXT.md
@.planning/phases/09-advanced-game-management/09-RESEARCH.md
@apps/frontend/src/app/teams/edit-team/edit-team.ts
@apps/frontend/src/app/teams/games/create-game/create-game.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Frontend Models</name>
  <files>libs/shared/util/models/src/lib/season.model.ts, libs/shared/util/models/src/lib/game.model.ts, libs/shared/util/models/src/lib/lineup-entry.model.ts</files>
  <action>
    - Add `defaultHomeVenue`, `defaultHomeColor`, `defaultAwayColor` to `Season` interface in `season.model.ts`.
    - Add `isHomeGame` to `Game` interface in `game.model.ts`.
    - **CRITICAL**: Create `lineup-entry.model.ts` with `LineupEntry` interface (including `slotIndex: number`). Ensure it is exported from the library's index.
  </action>
  <verify>
    <automated>npx nx run shared-util-models:lint</automated>
  </verify>
  <done>Frontend models updated with consistent .model.ts naming and slotIndex support</done>
</task>

<task type="auto">
  <name>Task 2: Season Settings UI</name>
  <files>apps/frontend/src/app/teams/edit-team/edit-team.ts, apps/frontend/src/app/teams/edit-team/edit-team.html</files>
  <action>
    - Update `edit-team.html`: Add a new section "Season Defaults" with fields for Home Venue, Home Jersey Color, and Away Jersey Color.
    - Update `edit-team.ts`: Add form controls for new fields; ensure they are sent to backend in `save()` or equivalent method.
  </action>
  <verify>
    <automated>npx nx run frontend:lint</automated>
  </verify>
  <done>Season defaults UI implemented</done>
</task>

<task type="auto">
  <name>Task 3: Smart Game Creation UI</name>
  <files>apps/frontend/src/app/teams/games/create-game/create-game.ts, apps/frontend/src/app/teams/games/create-game/create-game.html</files>
  <action>
    - Update `create-game.html`: Add an `ion-segment` for Home/Away selection.
    - Update `create-game.ts`: 
        - Add `isHomeGame` form control (default true).
        - Use an `effect()` or `valueChanges.subscribe()` to watch `isHomeGame`.
        - **IMPORTANT**: Implementation MUST allow for manual user overrides. Only auto-fill fields when the `isHomeGame` toggle value actually changes, and do not reset manually edited fields unless the user toggles the segment again.
        - When `isHomeGame` changes:
            - If `Home`: Set `location` = `season.defaultHomeVenue`, `uniformColor` = `season.defaultHomeColor`.
            - If `Away`: Set `location` = "", `uniformColor` = `season.defaultAwayColor`.
        - Ensure current active season data is available for defaults.
  </action>
  <verify>
    <automated>npx nx test frontend -- --watch=false</automated>
  </verify>
  <done>Reactive game creation UI implemented with manual override support</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Season settings and smart game creation UI</what-built>
  <how-to-verify>
    1. Go to Team Settings -> Edit Season (or where season fields are).
    2. Set defaults (e.g., Venue: "Apex Stadium", Home: "Blue", Away: "White").
    3. Save settings.
    4. Create a new Game.
    5. Toggle segment between Home and Away.
    6. Verify Venue and Color fields update automatically based on selection.
    7. Manually change the Venue after it auto-fills.
    8. Toggle Home/Away and then toggle back to original choice.
    9. Verify Venue resets to default on toggle back (this confirms toggle-driven behavior).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → UI | All input validated via Reactive Forms. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Information Disclosure | Defaults | accept | Defaults are public configuration within the team context. |
</threat_model>

<verification>
Manual verification of form reactivity and persistence of season defaults.
</verification>

<success_criteria>
- Season defaults can be saved and retrieved.
- Create Game form reacts correctly to Home/Away toggle.
- Manual overrides are supported.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-game-management/09-02-SUMMARY.md`
</output>
