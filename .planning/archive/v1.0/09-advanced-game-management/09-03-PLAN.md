---
phase: 09-advanced-game-management
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - libs/client/feature/game-console/src/lib/live-game-state.service.ts
  - libs/client/feature/game-console/src/lib/event-sync.service.ts
autonomous: true
requirements: [LIVE-05, LIVE-09, LIVE-10]
must_haves:
  truths:
    - "Live game state tracks players by slot index (0-10) instead of just player ID"
    - "SUB events specify which slot is being subbed"
    - "Undo correctly restores players to their exact slot index"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/live-game-state.service.ts"
      provides: "Slot-based state management"
  key_links:
    - from: "LiveGameStateService"
      to: "POSITION_SWAP event"
      via: "new event handler"
---

<objective>
Refactor the live game state to use stable slot indices for tracking on-field players. 

Purpose: Enable precise position-based interactions and solve the ambiguity in the current ID-only tracking.
Output: Slot-aware `LiveGameStateService` and updated event syncing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-advanced-game-management/09-CONTEXT.md
@.planning/phases/09-advanced-game-management/09-RESEARCH.md
@libs/client/feature/game-console/src/lib/live-game-state.service.ts
@libs/client/feature/game-console/src/lib/event-sync.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slot-Based activePlayers Logic</name>
  <files>libs/client/feature/game-console/src/lib/live-game-state.service.ts, libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts</files>
  <behavior>
    - Initialize `activePlayers` from `initialLineup` using `slotIndex` (starting players MUST have a slot index 0-10).
    - Handle `SUB` events: the incoming player MUST take the `slotIndex` previously occupied by the outgoing player.
    - Handle `POSITION_SWAP` events: players at `slotIndexA` and `slotIndexB` should swap their slots in the computed state.
    - Ensure `undo` naturally restores state because it removes the event from the filtered stream used by the `computed`.
  </behavior>
  <action>
    - Update `LineupEntry` interface in the service to include `slotIndex`.
    - Refactor `activePlayers` computed signal:
      - Use a `Map<number, { player: Player; position: string }>` where the key is the 0-10 `slotIndex`.
      - Loop through events and update the Map.
      - Return the values of the Map, including their assigned `slotIndex`.
  </action>
  <verify>
    <automated>npx nx test client-feature-game-console --testFile=live-game-state.service.spec.ts</automated>
  </verify>
  <done>Active players tracked via stable slot indices</done>
</task>

<task type="auto">
  <name>Task 2: Update Event Syncing</name>
  <files>libs/client/feature/game-console/src/lib/event-sync.service.ts</files>
  <action>
    - Ensure `POSITION_SWAP` events are included in the sync loop.
    - Verify `SUB` events include the `slotIndex` in their payload (if needed by backend, though backend can derive it if only 1 player per slot is allowed).
  </action>
  <verify>
    <automated>npx nx test client-feature-game-console --testFile=event-sync.service.spec.ts</automated>
  </verify>
  <done>Event sync service supports new events and slot data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service → LocalStorage | Event stream persists locally; slot indices must be valid integers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Denial of Service | ActivePlayers Computed | mitigate | Limit slot count to 0-10 (standard lineup) to prevent memory issues. |
</threat_model>

<verification>
Run unit tests for LiveGameStateService to verify slot transitions and undo behavior.
</verification>

<success_criteria>
- activePlayers computed signal correctly reflects slot assignments.
- SUB events preserve the slot index.
- POSITION_SWAP correctly interchanges two players' slots.
- Undo works perfectly for all slot-modifying events.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-game-management/09-03-SUMMARY.md`
</output>
