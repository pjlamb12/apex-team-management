---
phase: 05-games-lineup
plan: 04
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - apps/api/src/games/games.service.ts
  - apps/api/src/games/lineup-entries.service.ts
  - apps/api/src/games/games.controller.ts
  - apps/api/src/games/games.module.ts
  - apps/api/src/app/app.module.ts
  - apps/api/src/games/games.service.spec.ts
  - apps/api/src/games/lineup-entries.service.spec.ts
autonomous: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - LIVE-01
  - LIVE-02

must_haves:
  truths:
    - "GamesService implementation handles auto-season creation correctly"
    - "GamesController uses AuthGuard('jwt') and ParseUUIDPipe for all endpoints"
    - "LineupEntriesService implements bulk replace (delete then insert) for lineups"
    - "GamesModule is registered in the main AppModule"
    - "Unit tests in games.service.spec.ts and lineup-entries.service.spec.ts are fully implemented and passing"
  artifacts:
    - path: "apps/api/src/games/games.service.ts"
      provides: "GamesService with CRUD and auto-season logic"
      contains: "this.seasonRepo.create"
    - path: "apps/api/src/games/lineup-entries.service.ts"
      provides: "LineupEntriesService with bulk replace logic"
      contains: "this.lineupRepo.delete({ gameId })"
    - path: "apps/api/src/games/games.controller.ts"
      provides: "REST endpoints for games and lineups"
      contains: "@Post(':gameId/lineup')"
    - path: "apps/api/src/games/games.module.ts"
      provides: "NestJS module wiring for games feature"
      contains: "TypeOrmModule.forFeature"
  key_links:
    - from: "apps/api/src/games/games.controller.ts"
      to: "apps/api/src/games/games.service.ts"
      via: "dependency injection"
      pattern: "inject(GamesService)"
    - from: "apps/api/src/games/games.controller.ts"
      to: "apps/api/src/games/lineup-entries.service.ts"
      via: "dependency injection"
      pattern: "inject(LineupEntriesService)"
---

<objective>
Implement the NestJS backend for Phase 5: Games & Lineup. This includes the `GamesService` (with auto-season creation logic), `LineupEntriesService` (with bulk replace logic), `GamesController` (REST endpoints), and the `GamesModule` wiring. It also involves completing the unit tests that were stubbed in Plan 02.

Purpose: Provide the server-side API required by the Angular frontend. The backend handles the core business logic (seasons, lineup storage, ownership verification).

Output: 4 new NestJS files, 1 modified app.module.ts, and 2 updated spec files with full test coverage.
</objective>

<execution_context>
@/Users/pjlamb12/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pjlamb12/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-games-lineup/05-CONTEXT.md
@.planning/phases/05-games-lineup/05-RESEARCH.md
@.planning/phases/05-games-lineup/05-PATTERNS.md
@.planning/phases/05-games-lineup/05-VALIDATION.md

<interfaces>
<!-- Pattern from PlayersService [VERIFIED] -->
```typescript
@Injectable()
export class PlayersService {
  constructor(
    @InjectRepository(PlayerEntity)
    private readonly playerRepo: Repository<PlayerEntity>
  ) {}
  // ...
}
```

<!-- Pattern from PlayersController [VERIFIED] -->
```typescript
@Controller('teams/:teamId/players')
@UseGuards(AuthGuard('jwt'))
export class PlayersController {
  constructor(private readonly playersService: PlayersService) {}
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GamesService and LineupEntriesService</name>
  <files>
    apps/api/src/games/games.service.ts
    apps/api/src/games/lineup-entries.service.ts
  </files>
  <read_first>
    - apps/api/src/players/players.service.ts (pattern for repository injection)
    - apps/api/src/entities/game.entity.ts (entity structure)
    - apps/api/src/entities/season.entity.ts (season structure)
    - apps/api/src/entities/lineup-entry.entity.ts (lineup structure)
  </read_first>
  <action>
Create the two services in `apps/api/src/games/`.

**File 1: `apps/api/src/games/games.service.ts`**
Implement CRUD for games.
- `create(teamId: string, dto: CreateGameDto)`: Check for active season on team. If none, create one named "YYYY Season" (D-01 to D-03). Then save the game.
- `findAllForTeam(teamId: string)`: Return all games for the team's active season (D-08). Join through SeasonEntity.
- `findOne(gameId: string)`: Return a single game.
- `update(gameId: string, dto: UpdateGameDto)`: Patch game fields.
- `remove(gameId: string)`: Delete game (cascades to lineup_entries in DB).

**File 2: `apps/api/src/games/lineup-entries.service.ts`**
Implement lineup persistence.
- `saveLineup(gameId: string, entries: SaveLineupEntryDto[])`: DELETE all existing entries for the gameId, then bulk INSERT the new set (Pattern 7).
- `findByGame(gameId: string)`: Return all entries for the gameId.

Use `inject(getRepositoryToken(Entity))` for constructor injection or standard `@InjectRepository` decorator.
  </action>
  <verify>
    <automated>npx tsc --project apps/api/tsconfig.app.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/games/games.service.ts` exists and implements `create`, `findAllForTeam`, `update`, `remove`
    - `games.service.ts` auto-creates a season if none exists for the team in `create`
    - `games.service.ts` filters games by active season in `findAllForTeam`
    - `apps/api/src/games/lineup-entries.service.ts` exists and implements `saveLineup` and `findByGame`
    - `lineup-entries.service.ts` uses delete-then-insert pattern for `saveLineup`
    - `npx tsc --project apps/api/tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Services implemented with all business logic (auto-seasons, bulk-lineup-replace). TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GamesController and GamesModule</name>
  <files>
    apps/api/src/games/games.controller.ts
    apps/api/src/games/games.module.ts
    apps/api/src/app/app.module.ts
  </files>
  <read_first>
    - apps/api/src/players/players.controller.ts (endpoint and guard patterns)
    - apps/api/src/app/app.module.ts (module registration list)
  </read_first>
  <action>
Create the controller and module, then register the module.

**File 1: `apps/api/src/games/games.controller.ts`**
Expose REST endpoints. Use `AuthGuard('jwt')`.
- `POST /teams/:teamId/games` -> `GamesService.create`
- `GET /teams/:teamId/games` -> `GamesService.findAllForTeam`
- `PATCH /games/:gameId` -> `GamesService.update` (use `ParseUUIDPipe`)
- `DELETE /games/:gameId` -> `GamesService.remove`
- `GET /games/:gameId/lineup` -> `LineupEntriesService.findByGame`
- `POST /games/:gameId/lineup` -> `LineupEntriesService.saveLineup`

**File 2: `apps/api/src/games/games.module.ts`**
- Import `TypeOrmModule.forFeature([GameEntity, SeasonEntity, LineupEntryEntity, TeamEntity, PlayerEntity])`.
- Register `GamesController` and both services.

**File 3: `apps/api/src/app/app.module.ts`**
- Import and register `GamesModule` in the `imports` array.
  </action>
  <verify>
    <automated>npx tsc --project apps/api/tsconfig.app.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/games/games.controller.ts` exists and uses `@UseGuards(AuthGuard('jwt'))`
    - `games.controller.ts` implements all 6 endpoints correctly mapped to services
    - `apps/api/src/games/games.module.ts` registers all entities and providers
    - `apps/api/src/app/app.module.ts` imports `GamesModule`
    - `npx tsc --project apps/api/tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Controller and Modules wired. API endpoints reachable and protected. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit Tests in spec files</name>
  <files>
    apps/api/src/games/games.service.spec.ts
    apps/api/src/games/lineup-entries.service.spec.ts
  </files>
  <read_first>
    - apps/api/src/games/games.service.ts (service methods to test)
    - apps/api/src/games/lineup-entries.service.ts (service methods to test)
  </read_first>
  <action>
Update the spec files created in Plan 02 to be fully functional unit tests.
- Use `Test.createTestingModule` to set up mocks for Repositories.
- Test `GamesService.create` behavior (saving game, auto-creating season).
- Test `GamesService.findAllForTeam` (joining through season).
- Test `LineupEntriesService.saveLineup` (delete then insert).
- Verify all 6 requirements (GAME-01..04, LIVE-01..02) are covered by passing tests.
  </action>
  <verify>
    <automated>npx nx test api --testPathPattern=games</automated>
  </verify>
  <acceptance_criteria>
    - `nx test api --testPathPattern=games` reports all tests passing
    - Tests cover the auto-season creation logic in `GamesService`
    - Tests cover the bulk-replace logic in `LineupEntriesService`
    - Tests cover ownership checks (if implemented)
  </acceptance_criteria>
  <done>Unit tests fully implemented and passing. Backend verified for functional correctness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GamesController | Authenticated JWT required; team ownership must be verified in service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-04-01 | Elevation of Privilege | GamesController | mitigate | `@UseGuards(AuthGuard('jwt'))` ensures only logged-in users access endpoints |
| T-5-04-02 | Elevation of Privilege | GamesService.create | mitigate | Verify team ownership before creating game — ensure coach_id on team matches user sub from JWT |
| T-5-04-03 | Tampering | LineupEntriesService.saveLineup | mitigate | Verify game belongs to user's team before allowing bulk replace |
</threat_model>

<verification>
Backend is verified when:
1. `npx nx test api --testPathPattern=games` — all 10+ unit tests pass
2. `npx tsc --project apps/api/tsconfig.app.json --noEmit` — TypeScript clean
3. `curl` or Postman tests (optional) confirm endpoints return correct status codes
</verification>

<success_criteria>
- GamesService handles auto-season creation and filtering by active season
- LineupEntriesService handles bulk-replace logic for lineups
- GamesController exposes 6 protected REST endpoints
- GamesModule registered in AppModule
- All backend unit tests passing with full coverage of Phase 5 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/05-games-lineup/05-04-SUMMARY.md`
</output>
