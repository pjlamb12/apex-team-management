---
phase: 05-games-lineup
plan: 06
type: execute
wave: 3
depends_on:
  - "05-05"
files_modified:
  - apps/frontend/src/app/teams/games/create-game/create-game.ts
  - apps/frontend/src/app/teams/games/create-game/create-game.html
  - apps/frontend/src/app/teams/games/edit-game/edit-game.ts
  - apps/frontend/src/app/teams/games/edit-game/edit-game.html
  - apps/frontend/src/app/app.routes.ts
autonomous: true
requirements:
  - GAME-01
  - GAME-03

must_haves:
  truths:
    - "Create Game form includes opponent, location, uniformColor, and scheduledAt fields"
    - "Edit Game form pre-populates existing game data"
    - "Forms use ReactiveFormsModule and ControlErrorsDisplayComponent"
    - "Navigation to lineup editor after successful game creation"
    - "Routes for create and edit are correctly registered in app.routes.ts"
  artifacts:
    - path: "apps/frontend/src/app/teams/games/create-game/create-game.ts"
      provides: "Component for creating a new game"
      contains: "formBuilder.group"
    - path: "apps/frontend/src/app/teams/games/edit-game/edit-game.ts"
      provides: "Component for editing an existing game"
      contains: "gamesService.updateGame"
    - path: "apps/frontend/src/app/app.routes.ts"
      provides: "Registered lazy-loaded routes for games"
      contains: "teams/:id/games/new"
  key_links:
    - from: "apps/frontend/src/app/app.routes.ts"
      to: "apps/frontend/src/app/teams/games/create-game/create-game.ts"
      via: "lazy loading"
      pattern: "loadComponent"
---

<objective>
Implement the game creation and edit forms in Angular. This includes the `CreateGame` component (with `IonDatetime` picker), the `EditGame` component, and registering the new routes in `app.routes.ts`.

Purpose: Provide the UI for the coach to schedule and manage individual games.

Output: 2 new Angular page components, modified app.routes.ts.
</objective>

<execution_context>
@/Users/pjlamb12/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pjlamb12/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-games-lineup/05-CONTEXT.md
@.planning/phases/05-games-lineup/05-RESEARCH.md
@.planning/phases/05-games-lineup/05-PATTERNS.md
@.planning/phases/05-games-lineup/05-UI-SPEC.md

<interfaces>
<!-- Form pattern from CreateTeam [VERIFIED] -->
```typescript
@Component({ ... })
export class CreateTeam {
  protected form = inject(FormBuilder).group({
    name: ['', [Validators.required]],
    // ...
  });
}
```

<!-- Route pattern from app.routes.ts [VERIFIED] -->
```typescript
{
  path: 'teams/new',
  loadComponent: () => import('./teams/create-team/create-team').then(m => m.CreateTeam)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Games Routes in app.routes.ts</name>
  <files>apps/frontend/src/app/app.routes.ts</files>
  <read_first>
    - apps/frontend/src/app/app.routes.ts (current routes)
  </read_first>
  <action>
Modify `apps/frontend/src/app/app.routes.ts` to add three new lazy-loaded routes under the authenticated shell (or wherever appropriate):
1. `teams/:id/games/new` -> `CreateGame`
2. `teams/:id/games/:gameId/edit` -> `EditGame`
3. `teams/:id/games/:gameId/lineup` -> `LineupEditor` (registered now, implemented in Plan 07)
  </action>
  <verify>
    <automated>npx tsc --project apps/frontend/tsconfig.app.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app.routes.ts` contains the 3 new route definitions
    - Routes use `loadComponent: () => import(...).then(...)` pattern
    - Routes are correctly nested or placed after existing team routes
    - `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Routes registered. Lazy loading correctly configured. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CreateGame and EditGame components</name>
  <files>
    apps/frontend/src/app/teams/games/create-game/create-game.ts
    apps/frontend/src/app/teams/games/create-game/create-game.html
    apps/frontend/src/app/teams/games/edit-game/edit-game.ts
    apps/frontend/src/app/teams/games/edit-game/edit-game.html
  </files>
  <read_first>
    - apps/frontend/src/app/teams/create-team/create-team.ts (form pattern)
    - apps/frontend/src/app/teams/create-team/create-team.html (form template pattern)
  </read_first>
  <action>
**Step A — Implement `CreateGame`:**
- Create the folder `apps/frontend/src/app/teams/games/create-game/`.
- Component: use `FormBuilder` to create a form with `opponent`, `location`, `uniformColor`, and `scheduledAt`.
- Template: Use `IonDatetime` in an `IonModal` triggered by `IonDatetimeButton` (Pattern 8).
- Use `ControlErrorsDisplayComponent` for validation errors.
- On success, navigate to the lineup editor for the new game.

**Step B — Implement `EditGame`:**
- Create the folder `apps/frontend/src/app/teams/games/edit-game/`.
- Load game data via `GamesService` using `gameId` from route params.
- Pre-populate the form (same structure as CreateGame).
- On success, navigate back to Team Dashboard (Games segment).
  </action>
  <verify>
    <automated>npx tsc --project apps/frontend/tsconfig.app.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `CreateGame` and `EditGame` components exist and export standalone classes
    - Forms use `ReactiveFormsModule` and handle all 4 game fields
    - `IonDatetime` is used for `scheduledAt`
    - `ControlErrorsDisplayComponent` is imported and used in templates
    - Navigation logic is correct for both components
    - `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Game creation and edit forms implemented with validation and navigation. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Reactive Form | Client-side validation prevents submitting obviously invalid data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-06-01 | Tampering | Form Data | mitigate | Angular Validators (required, minLength, etc.) enforce input constraints before API call |
</threat_model>

<verification>
Forms are verified when:
1. `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` — TypeScript clean
2. Manual verification: Navigate to /new, fill form, verify redirect to /lineup; verify edit pre-populates correctly
</verification>

<success_criteria>
- CreateGame component implemented with full form and date picker
- EditGame component implemented with data loading and pre-population
- Both forms use standard project error display components
- Routes registered correctly in app.routes.ts
- Post-creation navigation to lineup editor is functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-games-lineup/05-06-SUMMARY.md`
</output>
