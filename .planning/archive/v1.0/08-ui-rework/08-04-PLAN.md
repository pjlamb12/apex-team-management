---
phase: 08-ui-rework
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified: []
autonomous: false
requirements:
  - INFR-02

must_haves:
  truths:
    - "No screen has an unintended solid black (#000000) background"
    - "All 13 screens render with AP slate palette backgrounds (#0f172a, #1e293b, #263044)"
    - "Cards are visually distinct from page backgrounds on all screens"
    - "Event log panel loads without white flash"
    - "Game console scoreboard shows against a visible surface background (not near-black #111111)"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification across all 13 screens defined in the UI-SPEC. This is a blocking checkpoint — no automated test can confirm CSS variable resolution and visual rendering. The coach verifies the app looks correct before Phase 8 is signed off.

Purpose: All CSS changes from Plans 01–03 affect visual rendering only. The only way to confirm correctness is to load the app in a browser with dark mode active and inspect each screen.

Output: Human approval that all 13 screens pass the visual contract, or a list of issues to address before sign-off.
</objective>

<execution_context>
@/Users/pjlamb12/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pjlamb12/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ui-rework/08-UI-SPEC.md
@.planning/phases/08-ui-rework/08-VALIDATION.md
@.planning/phases/08-ui-rework/08-01-SUMMARY.md
@.planning/phases/08-ui-rework/08-02-SUMMARY.md
@.planning/phases/08-ui-rework/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run full test suite</name>
  <files></files>
  <read_first>
    - .planning/phases/08-ui-rework/08-VALIDATION.md
  </read_first>
  <action>
Run the test suite first to confirm no regressions from Plans 01–03:

```bash
npx nx test frontend
```

Then start the dev server in the background:

```bash
npx nx serve frontend
```

The app will be available at http://localhost:4200. Confirm the server starts successfully and report the URL to the user.
  </action>
  <verify>
    <automated>npx nx test frontend</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx test frontend` exits 0 with no failures
    - Dev server starts without build errors
    - App is accessible at http://localhost:4200
  </acceptance_criteria>
  <done>Full test suite passes. Dev server running. App ready for visual inspection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plans 01–03 applied the complete Athletic Professional dark theme:
- theme.css filled with .ion-palette-dark override block (AP slate palette replaces Ionic #000000 defaults)
- styles.scss updated with @theme block (9 AP color tokens for Tailwind utilities) and theme.css import
- Game console hardcodes removed (console-wrapper, event-log, soccer-pitch-view)
- Per-page redundant ion-card overrides removed (auth pages, home, create-team, edit-team)
- teams-list border and teams-list fallback chain cleaned up
- lineup-editor list-header border fixed
  </what-built>
  <how-to-verify>
Visit http://localhost:4200 with dark mode active (ThemeService applies dark mode automatically on load).

Navigate through all 13 screens and confirm each passes:

**Screen 1 — Auth Pages:**
- [ ] /auth/login — page background is dark slate (NOT pure black #000000)
- [ ] /auth/signup — page background is dark slate
- [ ] /auth/reset-password — page background is dark slate
- [ ] Auth cards have a slightly lighter background than the page (#1e293b cards on #0f172a page)

**Screen 2 — Teams List (/teams):**
- [ ] Page background is dark slate
- [ ] Team cards are visually distinct from the page background (card is slightly lighter — #1e293b on #0f172a)
- [ ] Card borders are visible (not invisible like before — should be a faint white outline)

**Screen 3 — Team Dashboard (/teams/:id):**
- [ ] Page background is dark slate (NOT pure black)
- [ ] ion-item rows in the roster and games list are dark slate (NOT pure black)

**Screen 4 — Create/Edit Team:**
- [ ] Page background is dark slate
- [ ] Form cards have visible rounded corners (border-radius: 12px preserved)
- [ ] Card background is #1e293b (distinct from page)

**Screen 5 — Create/Edit Game:**
- [ ] Page background is dark slate
- [ ] ion-datetime modal (if triggered) has dark background

**Screen 6 — Lineup Editor:**
- [ ] Page background is dark slate
- [ ] Section headers (On the Field / Bench) have a visible white separator line below them
- [ ] ion-select rows are dark slate, not black

**Screen 7 — Game Console (Console Wrapper):**
- [ ] Main background is #0f172a dark slate (NOT pure black)
- [ ] Top toolbar/header bar is dark slate (NOT pure black)
- [ ] Scoreboard container has a visible elevated background (dark blue-gray, not near-black)
- [ ] Team names in scoreboard are legible

**Screen 8 — Soccer Pitch View:**
- [ ] Green pitch is visible (bg-green-700 preserved)
- [ ] Player name labels have a warmer dark slate background (NOT pure black)
- [ ] Player circles are blue (unselected) and yellow (selected)

**Screen 9 — Bench View:**
- [ ] Bench cards are visible with surface background (#1e293b)
- [ ] Bench panel background matches console surface color

**Screen 10 — Event Log:**
- [ ] Event log panel loads without a white flash
- [ ] Panel background is dark slate
- [ ] Section headers have a subtle bottom border (visible but not harsh)
- [ ] Panel left border is visible but subtle

**Screen 11 — Clock Display:**
- [ ] Clock text is white and clearly legible

**Screen 12 — Player Action Menu (Popover):**
- [ ] Tap a player during a game to open the action menu
- [ ] Popover background is dark slate (NOT pure black)

**Screen 13 — Player Modal:**
- [ ] If a player modal opens, background is dark slate

**Overall checks:**
- [ ] No screen has a solid pure-black (#000000) background
- [ ] Text is legible (white/light gray on dark backgrounds) on all screens
- [ ] The app feels cohesive — similar backgrounds across all pages
  </how-to-verify>
  <resume-signal>
Type "approved" if all 13 screens pass the visual contract.
If issues are found, describe which screens and what problem (e.g., "Screen 7 toolbar still black") — Claude will address them before sign-off.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visual rendering | CSS correctness can only be verified visually; this checkpoint is the final gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Denial of Service | Regression discovered during visual check | mitigate | If issues found, user describes them in resume-signal and Claude addresses per-issue before re-verifying. This plan is the safety net that catches anything the grep-based acceptance criteria missed. |
</threat_model>

<verification>
Phase 8 sign-off requires:

1. `npx nx test frontend` — exits 0 (automated)
2. All 13 screens pass human visual inspection (manual)
3. No screen has a solid pure-black background
4. Text/icon elements have sufficient contrast on all screens

```bash
# Final grep-level sanity check before human inspection:
grep -rn "#000000\|#111111\|#ffffff" \
  apps/frontend/src/app/auth/ \
  apps/frontend/src/app/home/ \
  apps/frontend/src/app/teams/ \
  libs/client/feature/game-console/src/lib/console-wrapper/ \
  libs/client/feature/game-console/src/lib/event-log/
# Expected: 0 results
```
</verification>

<success_criteria>
1. All 13 screens verified by human inspection — no solid black backgrounds
2. Cards are visually distinct from page backgrounds on all screens (depth visible)
3. Game console scoreboard, event log, and toolbar render with AP slate palette
4. Existing unit test suite passes (no regressions from CSS changes)
5. User approves with "approved" in the resume signal
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-rework/08-04-SUMMARY.md` using the summary template.
</output>
