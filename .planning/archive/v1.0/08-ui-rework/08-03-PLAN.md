---
phase: 08-ui-rework
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/frontend/src/app/auth/login/login.scss
  - apps/frontend/src/app/auth/signup/signup.scss
  - apps/frontend/src/app/auth/reset-password/reset-password.scss
  - apps/frontend/src/app/home/home.scss
  - apps/frontend/src/app/teams/create-team/create-team.scss
  - apps/frontend/src/app/teams/edit-team/edit-team.scss
  - apps/frontend/src/app/teams/teams-list/teams-list.scss
  - apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.scss
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - "Auth pages and home page have no redundant per-component ion-card --background overrides"
    - "Create/edit team pages retain border-radius: 12px but no redundant --background override"
    - "Teams list card background uses clean var(--ion-card-background) without deep fallback chain"
    - "Teams list card border is visible at rgba(255,255,255,0.08) opacity"
    - "Lineup editor list header has a visible white separator instead of near-invisible dark border"
  artifacts:
    - path: "apps/frontend/src/app/auth/login/login.scss"
      provides: "Redundant ion-card rule removed"
      contains: ""
    - path: "apps/frontend/src/app/teams/teams-list/teams-list.scss"
      provides: "Simplified fallback chain"
      contains: "var(--ion-card-background)"
    - path: "apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.scss"
      provides: "Visible list-header border"
      contains: "rgba(255, 255, 255, 0.12)"
  key_links:
    - from: "login/signup/reset-password/home SCSS files"
      to: "global --ion-card-background: #1e293b"
      via: "removal of per-component override — global token now controls card bg"
      pattern: "ion-card.*--background"
    - from: "teams-list.scss --background"
      to: "--ion-card-background: #1e293b (from theme.css)"
      via: "simplified var(--ion-card-background) — single-level lookup"
      pattern: "var\\(--ion-card-background\\)"
---

<objective>
Remove all redundant per-component `ion-card { --background }` overrides from auth, home, and team pages; fix the teams-list card border opacity; and fix the lineup editor list-header border color.

Purpose: After Plan 01 establishes `--ion-card-background: #1e293b` and `--ion-color-step-50: #1e293b` globally in `theme.css`, the per-component `ion-card { --background: var(--ion-color-step-50, #1a1a1a) }` rules in auth, home, create-team, and edit-team pages become redundant. Keeping them adds noise and could confuse future maintainers. The deep fallback chain in teams-list and the near-invisible border in lineup-editor are also cleaned up.

Output: 8 SCSS files updated — 6 with redundant `ion-card { --background }` rules removed, 1 with simplified fallback chain + increased border opacity, 1 with fixed list-header border color.

**These files are independent of Plan 02** — zero `files_modified` overlap. Plans 02 and 03 can execute in parallel (same wave).
</objective>

<execution_context>
@/Users/pjlamb12/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pjlamb12/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ui-rework/08-RESEARCH.md
@.planning/phases/08-ui-rework/08-PATTERNS.md
@.planning/phases/08-ui-rework/08-UI-SPEC.md
@.planning/phases/08-ui-rework/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant ion-card --background rules from auth, home, create-team, and edit-team</name>
  <files>
    apps/frontend/src/app/auth/login/login.scss,
    apps/frontend/src/app/auth/signup/signup.scss,
    apps/frontend/src/app/auth/reset-password/reset-password.scss,
    apps/frontend/src/app/home/home.scss,
    apps/frontend/src/app/teams/create-team/create-team.scss,
    apps/frontend/src/app/teams/edit-team/edit-team.scss
  </files>
  <read_first>
    - apps/frontend/src/app/auth/login/login.scss (read before editing; confirm current content)
    - apps/frontend/src/app/auth/signup/signup.scss
    - apps/frontend/src/app/auth/reset-password/reset-password.scss
    - apps/frontend/src/app/home/home.scss
    - apps/frontend/src/app/teams/create-team/create-team.scss
    - apps/frontend/src/app/teams/edit-team/edit-team.scss
    - .planning/phases/08-ui-rework/08-PATTERNS.md § "login.scss", "signup.scss", "reset-password.scss", "home.scss", "create-team.scss", "edit-team.scss"
  </read_first>
  <action>
Apply the following changes to each file. Read each file first to confirm the current content matches the known state before editing.

---

**login.scss** — current content (lines 1–5):
```scss
// Login page styles — dark theme applied globally via ThemeService
// ion-card inherits dark mode variables from Ionic theming
ion-card {
  --background: var(--ion-color-step-50, #1a1a1a);
}
```
Action: REMOVE lines 3–5 (the entire `ion-card { }` rule). Keep the comments on lines 1–2 if present; if the file had only the rule with no comments, the file becomes empty.

---

**signup.scss** — current content (lines 1–3):
```scss
ion-card {
  --background: var(--ion-color-step-50, #1a1a1a);
}
```
Action: REMOVE the entire `ion-card { }` rule. File becomes empty.

---

**reset-password.scss** — current content (lines 1–3):
```scss
ion-card {
  --background: var(--ion-color-step-50, #1a1a1a);
}
```
Action: REMOVE the entire `ion-card { }` rule. File becomes empty.

---

**home.scss** — current content (lines 1–3):
```scss
ion-card {
  --background: var(--ion-color-step-50, #1a1a1a);
}
```
Action: REMOVE the entire `ion-card { }` rule. File becomes empty.

---

**create-team.scss** — current content (lines 1–5):
```scss
// Create team page styles
ion-card {
  --background: var(--ion-color-step-50, #1a1a1a);
  border-radius: 12px;
}
```
Action: REMOVE the `--background` line ONLY. KEEP the `ion-card` block and `border-radius: 12px`.
Result:
```scss
// Create team page styles
ion-card {
  border-radius: 12px;
}
```

---

**edit-team.scss** — current content (lines 1–5):
```scss
// Edit team page styles
ion-card {
  --background: var(--ion-color-step-50, #1a1a1a);
  border-radius: 12px;
}
```
Action: REMOVE the `--background` line ONLY. KEEP the `ion-card` block and `border-radius: 12px`.
Result:
```scss
// Edit team page styles
ion-card {
  border-radius: 12px;
}
```

**CRITICAL for create-team and edit-team:** Do NOT delete the entire `ion-card` block — `border-radius: 12px` is intentional custom styling that must be preserved. Only the `--background` line is removed.
  </action>
  <verify>
    <automated>grep -rn "ion-color-step-50" /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/auth/ /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/home/ /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/teams/create-team/ /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/teams/edit-team/</automated>
  </verify>
  <acceptance_criteria>
    - login.scss does NOT contain `--background: var(--ion-color-step-50`
    - signup.scss does NOT contain `--background: var(--ion-color-step-50`
    - reset-password.scss does NOT contain `--background: var(--ion-color-step-50`
    - home.scss does NOT contain `--background: var(--ion-color-step-50`
    - create-team.scss does NOT contain `--background: var(--ion-color-step-50`
    - create-team.scss STILL contains `border-radius: 12px`
    - edit-team.scss does NOT contain `--background: var(--ion-color-step-50`
    - edit-team.scss STILL contains `border-radius: 12px`
    - `grep -rn "ion-color-step-50" apps/frontend/src/app/auth/ apps/frontend/src/app/home/ apps/frontend/src/app/teams/create-team/ apps/frontend/src/app/teams/edit-team/` returns 0 results
  </acceptance_criteria>
  <done>Six SCSS files no longer have redundant per-component ion-card --background overrides. Global --ion-card-background: #1e293b from theme.css now controls card backgrounds on auth pages, home, create-team, and edit-team. border-radius: 12px preserved on create/edit-team cards.</done>
</task>

<task type="auto">
  <name>Task 2: Fix teams-list.scss fallback chain and border opacity; fix lineup-editor.scss list-header border</name>
  <files>
    apps/frontend/src/app/teams/teams-list/teams-list.scss,
    apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.scss
  </files>
  <read_first>
    - apps/frontend/src/app/teams/teams-list/teams-list.scss (read entire file; the ion-card rule has a deep fallback chain on --background and 0.05 border opacity)
    - apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.scss (read entire file; ion-list-header has border-bottom: 2px solid var(--ion-color-light) which resolves to #222428 in dark mode)
    - .planning/phases/08-ui-rework/08-PATTERNS.md § "teams-list.scss" and "lineup-editor.scss"
    - .planning/phases/08-ui-rework/08-RESEARCH.md § Complete Hardcode Audit rows for teams-list.scss and lineup-editor.scss
  </read_first>
  <action>
**teams-list.scss — 2 targeted changes in the `ion-card` rule:**

1. CHANGE the `--background` fallback chain:
   - FROM: `--background: var(--ion-card-background, var(--ion-item-background, #1e1e1e));`
   - TO:   `--background: var(--ion-card-background);`

2. CHANGE the card border opacity:
   - FROM: `border: 1px solid rgba(255, 255, 255, 0.05);`
   - TO:   `border: 1px solid rgba(255, 255, 255, 0.08);`

ALL other rules in teams-list.scss (`--color`, `border-radius`, `box-shadow`, `margin-bottom`, `overflow`, `ion-card-header`, `ion-card-content`, `ion-card-title`, `ion-badge`, `ion-fab-button`, `.max-w-2xl`) must remain completely unchanged.

---

**lineup-editor.scss — 1 targeted change in the `ion-list-header` rule:**

1. CHANGE the border:
   - FROM: `border-bottom: 2px solid var(--ion-color-light);`
   - TO:   `border-bottom: 2px solid rgba(255, 255, 255, 0.12);`

ALL other rules in lineup-editor.scss (`:host`, `.slot-row`, `ion-list-header font-weight/font-size/--color/margin-bottom`, `ion-item`, `ion-badge`) must remain completely unchanged.

**Why rgba(255,255,255,0.12) for lineup-editor:** In Ionic dark mode, `--ion-color-light` resolves to `#222428` — a dark charcoal that creates a nearly invisible 2px line on the `#0f172a` background. `rgba(255,255,255,0.12)` is a semi-transparent white separator with sufficient contrast to clearly delineate sections.
  </action>
  <verify>
    <automated>npx nx test frontend</automated>
  </verify>
  <acceptance_criteria>
    - teams-list.scss contains `--background: var(--ion-card-background);` (NOT the long fallback chain)
    - teams-list.scss does NOT contain `var(--ion-item-background, #1e1e1e)`
    - teams-list.scss contains `border: 1px solid rgba(255, 255, 255, 0.08)` (NOT 0.05)
    - teams-list.scss still contains `border-radius: 16px` (unchanged)
    - teams-list.scss still contains `box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2)` (unchanged)
    - lineup-editor.scss contains `border-bottom: 2px solid rgba(255, 255, 255, 0.12)`
    - lineup-editor.scss does NOT contain `var(--ion-color-light)` in the border rule
    - lineup-editor.scss still contains `font-weight: 700` on ion-list-header (unchanged)
    - lineup-editor.scss still contains `border-radius: 12px` — wait, confirm this is not in lineup-editor, only in create/edit-team. Just verify no unintended changes.
    - `npx nx test frontend` exits 0
  </acceptance_criteria>
  <done>teams-list.scss has a clean single-variable --background lookup and visible card borders at 8% opacity. lineup-editor.scss list-header has a visible white separator. All existing unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component SCSS modification | Per-component style changes affect all instances of those components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | create-team.scss / edit-team.scss border-radius removal risk | mitigate | Acceptance criteria explicitly verify `border-radius: 12px` is preserved. The action text explicitly warns: "CRITICAL — Do NOT delete the entire ion-card block." |
| T-08-03-02 | Denial of Service | Unit test regression from SCSS changes | mitigate | Final acceptance criterion in Task 2: `npx nx test frontend` must exit 0. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -rn "ion-color-step-50" /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/` — returns 0 matches (all redundant rules removed)
2. `grep "ion-item-background, #1e1e1e" /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/teams/teams-list/teams-list.scss` — returns 0 matches
3. `grep "rgba(255, 255, 255, 0.08)" /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/teams/teams-list/teams-list.scss` — returns 1 match (card border)
4. `grep "rgba(255, 255, 255, 0.12)" /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.scss` — returns 1 match (list-header border)
5. `grep "border-radius: 12px" /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/teams/create-team/create-team.scss` — returns 1 match
6. `grep "border-radius: 12px" /Users/pjlamb12/projects/apex-team/apps/frontend/src/app/teams/edit-team/edit-team.scss` — returns 1 match
7. `npx nx test frontend` — exits 0
</verification>

<success_criteria>
1. All 6 redundant `ion-card { --background: var(--ion-color-step-50, ...) }` rules removed from auth, home, create-team, and edit-team pages
2. create-team and edit-team cards retain `border-radius: 12px`
3. teams-list card background uses clean single-variable `var(--ion-card-background)` lookup
4. teams-list card borders visible at 8% white opacity (up from 5%)
5. lineup-editor section headers have visible white separator (`rgba(255,255,255,0.12)`)
6. All existing unit tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-rework/08-03-SUMMARY.md` using the summary template.
</output>
