---
wave: 2
depends_on: ["04-03-PLAN.md"]
files_modified:
  - "apps/frontend/src/app/teams/player-modal/player-modal.ts"
  - "apps/frontend/src/app/teams/player-modal/player-modal.html"
  - "apps/frontend/src/app/teams/player-modal/player-modal.scss"
  - "apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts"
  - "apps/frontend/src/app/teams/team-dashboard/team-dashboard.html"
autonomous: true
---

# Wave 2: UI Add/Edit Player Modal

Implement the `IonModal` overlay to allow fast player data entry directly from the dashboard, preventing navigation overhead.

## Requirements Covered
- **ROST-01**: Coach can add a player with name, jersey number, and contact email
- **ROST-02**: Coach can edit player details

## Tasks

```xml
<task>
  <id>04-04-01</id>
  <read_first>
    - apps/frontend/src/app/teams/player-modal/player-modal.ts
  </read_first>
  <action>
    Create standalone `PlayerModal` component (`apps/frontend/src/app/teams/player-modal/player-modal.ts`).
    Implement a Reactive Form with: `firstName` (required), `lastName` (required), `jerseyNumber` (required, number), `parentEmail` (required, email).
    Use `@Input()` properties to receive an optional `Player` for editing. If passed, patch the form.
    Inject `ModalController` to dismiss and pass back the payload.
  </action>
  <acceptance_criteria>
    - `grep -q "ModalController" apps/frontend/src/app/teams/player-modal/player-modal.ts`
  </acceptance_criteria>
</task>

<task>
  <id>04-04-02</id>
  <read_first>
    - apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts
    - apps/frontend/src/app/teams/team-dashboard/team-dashboard.html
  </read_first>
  <action>
    Integrate `PlayerModal` into `TeamDashboard`. 
    Add an `<ion-fab>` button for "Add Player". 
    In the TypeScript class, inject `ModalController`. Define `openPlayerModal(player?)` method to invoke the modal.
    Upon dismissal, if data is returned, call `PlayersService.addPlayer` or `updatePlayer` based on presence of ID, then update the local Signals array to reflect the UI change seamlessly.
  </action>
  <acceptance_criteria>
    - `grep -q "ModalController" apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts`
    - `grep -q "ion-fab" apps/frontend/src/app/teams/team-dashboard/team-dashboard.html`
  </acceptance_criteria>
</task>
```
