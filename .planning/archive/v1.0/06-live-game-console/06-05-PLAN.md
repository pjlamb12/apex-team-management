---
phase: 06-live-game-console
plan: 05
type: execute
wave: 4
depends_on: ["01", "04"]
files_modified: [libs/client/feature/game-console/src/lib/event-log/event-log.ts, libs/client/feature/game-console/src/lib/player-action-menu/player-action-menu.ts, libs/client/feature/game-console/src/lib/live-game-state.service.ts, libs/client/feature/game-console/src/lib/event-sync.service.ts]
autonomous: true
requirements: [LIVE-04, LIVE-06, LIVE-07, LIVE-08]

must_haves:
  truths:
    - "Coach can log goals and assists by tapping active players"
    - "Coach can view a historical log of all game events with timestamps"
    - "Coach can undo any event, reverting both local and remote state"
    - "Events are optimistically synced to the backend"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/event-log/event-log.ts"
      provides: "Visual history of game actions"
    - path: "libs/client/feature/game-console/src/lib/player-action-menu/player-action-menu.ts"
      provides: "Contextual player stats logging"
    - path: "libs/client/feature/game-console/src/lib/event-sync.service.ts"
      provides: "Backend synchronization"
---

<objective>
Implement sport-specific event logging (Goals, Assists) and background synchronization with the API.

Purpose: Provide the detailed stat-tracking capabilities and ensure the game record is permanently stored on the server.
Output: Event Log UI, Action Menu UI, and EventSyncService.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-live-game-console/06-CONTEXT.md
@.planning/phases/06-live-game-console/06-RESEARCH.md
@libs/client/feature/game-console/src/lib/live-game-state.service.ts
@apps/api/src/games/games.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PlayerActionMenuComponent</name>
  <files>libs/client/feature/game-console/src/lib/player-action-menu/player-action-menu.ts, libs/client/feature/game-console/src/lib/player-action-menu/player-action-menu.html</files>
  <action>
    Create a contextual menu (e.g., using `ion-popover`) that appears when an active player is tapped.
    - Show actions for: Goal, Assist, Yellow Card, Red Card.
    - When an action is selected, trigger the corresponding event in `LiveGameStateService`.
    - Automatically include the `minuteOccurred` from `LiveClockService`.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>Action menu allows logging goals, assists, and cards for active players</done>
</task>

<task type="auto">
  <name>Task 2: Implement EventLogViewComponent</name>
  <files>libs/client/feature/game-console/src/lib/event-log/event-log.ts, libs/client/feature/game-console/src/lib/event-log/event-log.html</files>
  <action>
    Create a component that displays a scrollable list of all events in the current game session.
    - Format: `[Minute] EventType: PlayerName (Details)`.
    - Add an "Undo" button next to each event (or a single global "Undo Last" button).
    - Use the `events` signal from `LiveGameStateService`.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>Event log view provides a clear history of game events and undo support</done>
</task>

<task type="auto">
  <name>Task 3: Implement EventSyncService</name>
  <files>libs/client/feature/game-console/src/lib/event-sync.service.ts</files>
  <action>
    Create a service that listens for changes in `LiveGameStateService.events`.
    - When a new event is added, POST it to `/api/games/:gameId/events`.
    - Support "Optimistic UI": update locally first, sync in background.
    - Handle retry logic for failed syncs.
    - When an undo is performed, delete or mark the event as reverted on the backend.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>Background synchronization with the API implemented and robust</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Synchronization channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Availability | Sync Failure | mitigate | Implement retry-with-exponential-backoff in EventSyncService |
| T-06-05-02 | Integrity | Desync after Undo | mitigate | Ensure undo action sends explicit DELETE/Compensating event to API |
</threat_model>

<verification>
1. Log a goal in the UI.
2. Check browser network tab for POST request to /events.
3. Tap "Undo" and verify the local list updates and another request is sent to revert.
</verification>

<planned_enhancements>
<!-- Captured post-execution — apply when revisiting undo logic -->
- LIVE-05 bug: "Undo Last" currently leaves the undone event visible in the event log. The correct behavior is to remove the event from the log entirely when undone, not just revert state silently. LiveGameStateService.undo() must splice the event out of the events array; EventSyncService must also delete/revert it on the backend.
</planned_enhancements>

<success_criteria>
1. Player action menu correctly logs sport-specific events.
2. Event log correctly displays full history.
3. Backend successfully receives and stores events from the sync service.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-game-console/06-05-SUMMARY.md`
</output>
