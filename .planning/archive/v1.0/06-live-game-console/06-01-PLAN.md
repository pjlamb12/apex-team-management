---
phase: 06-live-game-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, apps/api/src/entities/sport.entity.ts, apps/api/src/migrations/1776510000000-AddEventDefinitionsToSport.ts, apps/api/src/games/games.controller.ts, apps/api/src/games/games.service.ts]
autonomous: true
requirements: [LIVE-06, LIVE-07, LIVE-08, INFR-03]

must_haves:
  truths:
    - "SportEntity has eventDefinitions JSONB field"
    - "Soccer sport has Goals, Assists, Cards, and Substitutions defined"
    - "API accepts POST /api/games/:id/events to store game events"
  artifacts:
    - path: "apps/api/src/entities/sport.entity.ts"
      provides: "Event definitions schema"
    - path: "apps/api/src/games/games.controller.ts"
      provides: "Event logging endpoint"
  key_links:
    - from: "POST /api/games/:id/events"
      to: "GameEventEntity"
      via: "GamesService.logEvent"
---

<objective>
Update the backend foundation to support sport-specific event logging and dynamic schema-driven validation.

Purpose: Provide the server-side infrastructure for capturing goals, assists, and substitutions with sport-specific payloads.
Output: Extended SportEntity, updated Soccer seed, and a new incremental event sync endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-live-game-console/06-CONTEXT.md
@.planning/phases/06-live-game-console/06-RESEARCH.md
@apps/api/src/entities/sport.entity.ts
@apps/api/src/entities/game-event.entity.ts
@apps/api/src/games/games.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install event validation and date utilities</name>
  <files>package.json</files>
  <action>
    Install `ajv`, `ajv-formats`, and `date-fns` as dependencies.
    `ajv` and `ajv-formats` will be used for runtime validation of sport-specific JSON payloads.
    `date-fns` will assist with clock and interval math.
  </action>
  <verify>
    <automated>npm list ajv ajv-formats date-fns</automated>
  </verify>
  <done>Dependencies installed and listed in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Update SportEntity and Migration</name>
  <files>apps/api/src/entities/sport.entity.ts, apps/api/src/migrations/1776510000000-AddEventDefinitionsToSport.ts</files>
  <action>
    1. Update `SportEntity` to include `eventDefinitions` property with `type: 'jsonb', default: []`.
    2. Create a migration that:
       - Adds `event_definitions` column to `sports` table.
       - Updates the "Soccer" sport entry to include definitions for:
         - `GOAL`: { payloadSchema: { scorerId: uuid, assistorId?: uuid } }
         - `ASSIST`: { payloadSchema: { assistorId: uuid } }
         - `SUB`: { payloadSchema: { inPlayerId: uuid, outPlayerId: uuid, positionName: string } }
         - `CARD`: { payloadSchema: { playerId: uuid, color: 'yellow' | 'red' } }
  </action>
  <verify>
    <automated>npx ts-node -P apps/api/tsconfig.app.json -r tsconfig-paths/register apps/api/src/data-source.ts migration:run</automated>
  </verify>
  <done>Database schema updated and Soccer sport seeded with event definitions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Event Logging Endpoint</name>
  <files>apps/api/src/games/games.controller.ts, apps/api/src/games/games.service.ts, apps/api/src/games/dto/create-event.dto.ts</files>
  <behavior>
    - POST /api/games/:gameId/events should create a new GameEventEntity
    - Payload should include eventType, minuteOccurred, and JSON payload
    - Should validate that the gameId exists and belongs to the user (via coach_id on team)
  </behavior>
  <action>
    1. Create `CreateEventDto`.
    2. Add `logEvent` method to `GamesService`.
    3. Add `POST /api/games/:gameId/events` endpoint to `GamesController` (protected by JWT).
  </action>
  <verify>
    <automated>npm run test apps/api/src/games/games.service.spec.ts</automated>
  </verify>
  <done>API endpoint implemented and verified with unit tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | External input in JSONB payload crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | POST /api/games/:id/events | mitigate | Validate eventType against SportEntity.eventDefinitions schema using AJV |
| T-06-01-02 | Information Disclosure | Event data sync | mitigate | Ensure game and team ownership is verified before allowing event logging |
</threat_model>

<verification>
1. Run migrations: `npx nx run api:migration:run`
2. Verify schema: `SELECT event_definitions FROM sports WHERE name = 'Soccer'`
3. Run unit tests for GamesService: `npx nx test api`
</verification>

<success_criteria>
1. Database schema includes `event_definitions` on `sports`.
2. Soccer sport has valid event definitions.
3. API endpoint `/api/games/:id/events` successfully persists events.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-game-console/06-01-SUMMARY.md`
</output>
