---
phase: 06-live-game-console
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified: [apps/frontend/src/app/app.routes.ts, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts, libs/client/feature/game-console/src/lib/clock-display/clock-display.ts]
autonomous: true
requirements: [LIVE-08]

must_haves:
  truths:
    - "Live Game Console is accessible via /teams/:id/games/:gameId/console"
    - "Console shell displays the current game time from LiveClockService"
    - "Console shell correctly identifies the game and team context"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts"
      provides: "Main layout shell"
    - path: "libs/client/feature/game-console/src/lib/clock-display/clock-display.ts"
      provides: "Formatted time visualization"
---

<objective>
Establish the UI shell and routing for the Live Game Console.

Purpose: Provide a navigable entry point for coaches to access the "Live Mode" and view the running clock.
Output: Integrated route and foundational UI components for the console.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-live-game-console/06-CONTEXT.md
@apps/frontend/src/app/app.routes.ts
@libs/client/feature/game-console/src/lib/live-clock.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add console route</name>
  <files>apps/frontend/src/app/app.routes.ts</files>
  <action>
    Add a new route `teams/:id/games/:gameId/console` that loads the `ConsoleWrapper` component.
    Ensure it is nested within the authenticated shell and the `teams/:id` context.
  </action>
  <verify>
    <automated>grep "console" apps/frontend/src/app/app.routes.ts</automated>
  </verify>
  <done>Route added and pointing to the console feature library</done>
</task>

<task type="auto">
  <name>Task 2: Create ClockDisplayComponent</name>
  <files>libs/client/feature/game-console/src/lib/clock-display/clock-display.ts, libs/client/feature/game-console/src/lib/clock-display/clock-display.html</files>
  <action>
    Create a standalone component that uses `LiveClockService` to display the current elapsed time.
    Format the output as `MM:SS` or `HH:MM:SS` using `date-fns`.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>Formatted clock display component created</done>
</task>

<task type="auto">
  <name>Task 3: Implement ConsoleWrapper component shell</name>
  <files>libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.html</files>
  <action>
    Implement the main layout for the console.
    - Top Bar: Game info (Opponent), Game Clock, and Start/Stop controls.
    - Center Area: Empty placeholder for Field View.
    - Bottom: Empty placeholder for Bench View.
    Load game and team data using route parameters.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>Console shell layout implemented with functional clock controls</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → Component | Parameter extraction (ids) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Elevation of Privilege | Console Route Access | mitigate | authGuard already protects the parent route; console component must verify coach ownership of game |
</threat_model>

<verification>
Start the frontend app and navigate to the console route for an existing game. Verify the clock starts/stops correctly.
</verification>

<success_criteria>
1. /console route loads the wrapper component.
2. The game clock starts and increments when "Start" is tapped.
3. The game clock pauses when "Stop" is tapped.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-game-console/06-03-SUMMARY.md`
</output>
