---
phase: 06-live-game-console
plan: 06
type: execute
wave: 5
depends_on: ["05"]
files_modified: [apps/frontend-e2e/src/live-console.spec.ts]
autonomous: true
requirements: [LIVE-03, LIVE-04, LIVE-05, LIVE-06, LIVE-07, LIVE-08, INFR-03]

must_haves:
  truths:
    - "Full game flow from lineup to substitution to scoring is verifiable"
    - "Console state survives page refresh via localStorage"
    - "All actions are correctly synchronized to the backend"
  artifacts:
    - path: "apps/frontend-e2e/src/live-console.spec.ts"
      provides: "Automated regression suite"
---

<objective>
Execute end-to-end verification of the Live Game Console to ensure reliability during actual match play.

Purpose: Guarantee that the coach's data is safe, even across refreshes, and that the UI is responsive and accurate.
Output: Playwright E2E test suite covering critical "Game Day" paths.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-live-game-console/06-CONTEXT.md
@.planning/phases/06-live-game-console/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test for live console flow</name>
  <files>apps/frontend-e2e/src/live-console.spec.ts</files>
  <action>
    Write a Playwright test that:
    1. Logins and navigates to an existing game's console.
    2. Starts the clock.
    3. Performs a substitution (Bench -> Pitch).
    4. Logs a goal for a player.
    5. Refreshes the page.
    6. Verifies the clock is still running (from the correct time) and the goal/sub are still visible.
    7. Undos the goal.
    8. Verifies goal is removed from log.
  </action>
  <verify>
    <automated>npx nx run frontend-e2e:e2e</automated>
  </verify>
  <done>E2E suite verifies persistence, sync, and core game interactions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 6 Live Console feature set</what-built>
  <how-to-verify>
    1. Open the app on a mobile device (or responsive emulator).
    2. Open a game console.
    3. Verify touch targets for player cards are large enough (min 44x44px).
    4. Verify contrast of selected players.
    5. Perform 10 fast substitutions and check if any "ghost clicks" occur.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Mobile UI | Physical interaction surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Availability | Offline usage | accept | v1 assumes active internet connection for sync; local persistence provides best-effort resiliency |
</threat_model>

<verification>
Run the Playwright E2E suite and perform the manual touch target audit.
</verification>

<success_criteria>
1. All E2E tests pass consistently.
2. Manual audit confirms "Athletic Professional" aesthetic and mobile responsiveness.
3. Refreshing the browser mid-match never loses data.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-game-console/06-06-SUMMARY.md`
</output>
