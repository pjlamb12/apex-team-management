---
phase: 06-live-game-console
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [libs/client/feature/game-console/project.json, libs/client/feature/game-console/src/lib/live-clock.service.ts, libs/client/feature/game-console/src/lib/live-game-state.service.ts]
autonomous: true
requirements: [INFR-03, LIVE-05]

must_haves:
  truths:
    - "LiveClockService provides a reactive elapsed time signal without interval drift"
    - "LiveGameStateService persists events to localStorage and restores them on load"
    - "LiveGameStateService supports undo() by popping the last event from the history index"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/live-clock.service.ts"
      provides: "Precise game timer"
    - path: "libs/client/feature/game-console/src/lib/live-game-state.service.ts"
      provides: "Event-sourced game state"
---

<objective>
Implement the core logic services for the Live Game Console using Angular 21 Signals and Event Sourcing patterns.

Purpose: Manage the real-time clock and game state (lineup + events) with crash recovery and undo support.
Output: A new Angular library containing LiveClockService and LiveGameStateService.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-live-game-console/06-CONTEXT.md
@.planning/phases/06-live-game-console/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold live-console library</name>
  <files>libs/client/feature/game-console/project.json</files>
  <action>
    Generate a new Angular library `client-feature-game-console` at `libs/client/feature/game-console`.
    Use the domain-driven structure `libs/client/feature/game-console`.
    Configure tags: `domain:client`, `type:feature`.
  </action>
  <verify>
    <automated>npx nx show project client-feature-game-console</automated>
  </verify>
  <done>Library scaffolded and registered in Nx workspace</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LiveClockService</name>
  <files>libs/client/feature/game-console/src/lib/live-clock.service.ts, libs/client/feature/game-console/src/lib/live-clock.service.spec.ts</files>
  <behavior>
    - `elapsedMs` signal should reflect time passed since `start()` using `Date.now()`
    - `stop()` should pause the clock and accumulate elapsed time
    - `reset()` should clear both start time and accumulated time
    - Clock must not drift over time (use absolute timestamps)
  </behavior>
  <action>
    Implement a Signal-based stopwatch service as described in the Research document.
  </action>
  <verify>
    <automated>npx vitest libs/client/feature/game-console/src/lib/live-clock.service.spec.ts</automated>
  </verify>
  <done>LiveClockService implemented and verified for precision and reactivity</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement LiveGameStateService</name>
  <files>libs/client/feature/game-console/src/lib/live-game-state.service.ts, libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts</files>
  <behavior>
    - `events` signal should store a list of game events
    - `pushEvent(event)` should add to history and sync to localStorage
    - `undo()` should remove the last event and update localStorage
    - On initialization, it should load state from localStorage using gameId as key
  </behavior>
  <action>
    Implement an event-sourced state service that manages the game history stack.
  </action>
  <verify>
    <automated>npx vitest libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts</automated>
  </verify>
  <done>LiveGameStateService implemented with persistence and undo functionality</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → LocalStorage | State persistence boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | LocalStorage State | accept | Low risk; local state only used for crash recovery, backend remains source of truth |
</threat_model>

<verification>
Run unit tests for both services in the newly created library.
</verification>

<success_criteria>
1. `LiveClockService` provides precise, reactive timing.
2. `LiveGameStateService` persists and restores events from localStorage.
3. `undo()` successfully reverts the last event.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-game-console/06-02-SUMMARY.md`
</output>
