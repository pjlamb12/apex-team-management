# Plan 07-03: Haptics and Native Branding

**Phase:** 07-pwa-native-builds
**Plan:** 03
**Goal:** Implement haptic feedback for timer transitions and set up native splash screens and icons.

## Requirements
- **TRD:** Ionic/Capacitor haptic feedback for timer transitions.
- **SUCCESS-CR-3/4:** Working APK/IPA with correct branding.

## Context
- Phase 6 `LiveClockService` in `libs/client/feature/game-console/src/lib/live-clock.service.ts`.
- Capacitor platforms added (07-02).

## Tasks
1. **Install Haptics Plugin:** `npm install @capacitor/haptics`.
2. **Add Haptic Feedback:** Inject `Haptics` in `LiveClockService` and call `vibrate()` or `impact()` on start/stop/reset.
3. **Setup Branding Assets:** Use placeholder branding assets for icons and splash screen if real ones are not available.
4. **Sync Changes:** Run `npx cap sync`.

## Implementation Strategy
- Register the Haptics plugin in the Angular app if necessary (usually just `import` and use).
- Update `LiveClockService.start()`, `stop()`, and `reset()` to trigger feedback.

## Verification Plan
- Verify Haptics plugin is in `package.json`.
- Verify `LiveClockService.ts` contains `Haptics` calls.
- Verify `npx cap sync` runs without errors.
