---
phase: 14-drill-library-foundation
plan: 04
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - libs/client/feature/drill-library/src/lib/drill-detail/drill-detail.ts
  - libs/client/feature/drill-library/src/lib/drill-detail/drill-detail.html
  - libs/client/feature/drill-library/src/lib/drill-editor/drill-editor.ts
  - libs/client/feature/drill-library/src/lib/drill-editor/drill-editor.html
  - libs/client/feature/drill-library/src/lib/drill-list/drill-list.ts
  - apps/frontend/src/app/app.routes.ts
autonomous: true
requirements: [DRIL-01, DRIL-02]

must_haves:
  truths:
    - "Coach can view drill details including video and structured steps"
    - "Coach can create/edit drills using a multi-step instruction form"
    - "Drill editor allows adding tags via the custom TagInput"
  artifacts:
    - path: "libs/client/feature/drill-library/src/lib/drill-detail/drill-detail.ts"
      provides: "Full drill viewer with media support"
    - path: "libs/client/feature/drill-library/src/lib/drill-editor/drill-editor.ts"
      provides: "Reactive form for multi-step drill creation"
  key_links:
    - from: "libs/client/feature/drill-library/src/lib/drill-editor/drill-editor.ts"
      to: "libs/client/ui/drill/tag-input"
      via: "component reference"
---

<objective>
Complete the drill library by implementing the drill viewer and a robust editor for creating multi-step coaching content.

Purpose: Allow coaches to document and review their library content with rich instructions and media.
Output: DrillDetail and DrillEditor components with reactive instruction steps.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-drill-library-foundation/14-RESEARCH.md
@.planning/phases/14-drill-library-foundation/14-CONTEXT.md
@.planning/phases/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DrillDetail Component</name>
  <files>libs/client/feature/drill-library/src/lib/drill-detail/drill-detail.ts, libs/client/feature/drill-library/src/lib/drill-detail/drill-detail.html</files>
  <action>
    Implement the drill viewer component.
    - Media Section: Use the `VideoEmbed` component (or logic from 14-02) to display the YouTube/Vimeo/Source URL.
    - Instructions Section: Render the `jsonb` steps from the backend as a numbered list of headings and descriptions.
    - Info Section: Display name, description, and chips for tags.
    - Actions: Add "Edit" button if viewing own drill (always true for v1.2).
  </action>
  <verify>
    <automated>ls libs/client/feature/drill-library/src/lib/drill-detail/drill-detail.ts</automated>
  </verify>
  <done>Full drill details rendered with media and structured steps.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DrillEditor Component</name>
  <files>libs/client/feature/drill-library/src/lib/drill-editor/drill-editor.ts, libs/client/feature/drill-library/src/lib/drill-editor/drill-editor.html</files>
  <action>
    Implement the drill editor component using `ReactiveFormsModule`.
    - Instructions: Use `FormArray` to allow adding/removing instruction steps dynamically. Each step has `title` and `description`.
    - Tagging: Use the `TagInput` component from `libs/client/ui/drill`.
    - Basic Fields: `ion-input` for name and sourceUrl, `ion-textarea` for description.
    - Validation: Required fields (name), sourceUrl format, and at least one instruction step (optional but recommended).
  </action>
  <verify>
    <automated>ls libs/client/feature/drill-library/src/lib/drill-editor/drill-editor.ts</automated>
  </verify>
  <done>Reactive drill editor implemented with dynamic steps and tagging.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Editor and Detail to Navigation</name>
  <files>libs/client/feature/drill-library/src/lib/drill-list/drill-list.ts, apps/frontend/src/app/app.routes.ts</files>
  <action>
    - Update `app.routes.ts` to add routes for detail (`:id`) and editor (`new`, `:id/edit`).
    - Add "Add Drill" FAB (Floating Action Button) to `DrillList` that navigates to `/new`.
    - Update `DrillList` cards to navigate to the detail view on click.
    - Wire "Save" action in `DrillEditor` to `DrillService.createDrill()` / `updateDrill()`.
  </action>
  <verify>
    <automated>grep "edit" apps/frontend/src/app/app.routes.ts</automated>
  </verify>
  <done>Full navigation lifecycle for drills (List -> Create -> Detail -> Edit) functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | User input in editor is untrusted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering | Drill Content | mitigate | Use standard Angular sanitization for rendered content (already default in templates). |
| T-14-04-02 | Elevation of Privilege | Drill Edit | mitigate | Backend coachId check (from 14-01) prevents editing drills owned by others. |
</threat_model>

<verification>
1. Serve the app: `nx serve`.
2. Create a new drill with 3 steps and 2 tags.
3. View the drill and verify the steps and video render.
4. Edit the drill and verify changes persist.
</verification>

<success_criteria>
- Coach can successfully create a multi-step drill with tags and a video link.
- Drill detail correctly renders all metadata.
- "Add Drill" flow is accessible from the library tab.
</success_criteria>

<output>
After completion, create `.planning/phases/14-drill-library-foundation/14-04-SUMMARY.md`
</output>
