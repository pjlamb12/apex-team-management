---
phase: 14-drill-library-foundation
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - libs/client/feature/drill-library/src/lib/drill-list/drill-list.ts
  - libs/client/feature/drill-library/src/lib/drill-list/drill-list.html
  - apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts
  - apps/frontend/src/app/teams/team-dashboard/team-dashboard.html
  - apps/frontend/src/app/app.routes.ts
autonomous: true
requirements: [DRIL-04]

must_haves:
  truths:
    - "Coach can browse drills in a dedicated tab on the Team Dashboard"
    - "Drill list supports real-time search and tag filtering"
    - "Tag filters use a horizontal scroll of chips for easy navigation"
  artifacts:
    - path: "libs/client/feature/drill-library/src/lib/drill-list/drill-list.ts"
      provides: "Library browse interface"
  key_links:
    - from: "apps/frontend/src/app/app.routes.ts"
      to: "libs/client/feature/drill-library"
      via: "lazy-loaded child route"
---

<objective>
Implement the main drill library browsing interface and integrate it into the coach's team dashboard.

Purpose: Allow coaches to easily find and view their drills within their team context.
Output: DrillList component with search/filter and updated TeamDashboard navigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-drill-library-foundation/14-RESEARCH.md
@.planning/phases/14-drill-library-foundation/14-CONTEXT.md
@.planning/phases/14-02-SUMMARY.md

<interfaces>
From libs/client/data-access/drill/src/lib/drill.model.ts (Created in 14-02):
```typescript
export interface Drill {
  id: string;
  name: string;
  description: string;
  tags: Tag[];
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DrillList Component</name>
  <files>libs/client/feature/drill-library/src/lib/drill-list/drill-list.ts, libs/client/feature/drill-library/src/lib/drill-list/drill-list.html</files>
  <action>
    Generate the library: `nx g @nx/angular:library client/feature/drill-library --directory=libs/client/feature/drill-library`.
    Implement `DrillList` component:
    - Search: `ion-searchbar` with Signal-based filtering.
    - Tag Filters: Horizontal scroll of `ion-chip` components representing available tags. Clicking toggles the tag filter.
    - Drill Cards: Display drill name, first few lines of description, and tags.
    - Use `DrillService.getDrills()` to fetch data, passing active tags as filters.
  </action>
  <verify>
    <automated>ls libs/client/feature/drill-library/src/lib/drill-list/drill-list.ts</automated>
  </verify>
  <done>Drill library browser implemented with search and tag filtering.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate with TeamDashboard</name>
  <files>apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts, apps/frontend/src/app/teams/team-dashboard/team-dashboard.html, apps/frontend/src/app/app.routes.ts</files>
  <action>
    - Add "Drills" segment to `TeamDashboard` (segment value: `drills`).
    - Update `app.routes.ts` to add the `drills` child route to `teams/:id`.
    - Use `loadComponent` for lazy loading the `DrillList`.
    - Ensure segment change in `TeamDashboard` navigates correctly to `/teams/:id/drills`.
  </action>
  <verify>
    <automated>grep "drills" apps/frontend/src/app/app.routes.ts</automated>
  </verify>
  <done>Drill Library integrated into the main navigation flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Route Params | teamId from URL used for navigation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Information Disclosure | Drill List | mitigate | Drills are filtered by coachId in the API (already handled in 14-01). UI just requests what it's allowed to see. |
</threat_model>

<verification>
1. Serve the app: `nx serve`.
2. Navigate to a team and click the "Drills" segment.
3. Verify the list renders and search/tags work.
</verification>

<success_criteria>
- "Drills" tab appears in Team Dashboard.
- Search and tag chips filter the list in real-time.
- Clicking a drill (placeholder for now) shows intent to navigate to detail.
</success_criteria>

<output>
After completion, create `.planning/phases/14-drill-library-foundation/14-03-SUMMARY.md`
</output>
