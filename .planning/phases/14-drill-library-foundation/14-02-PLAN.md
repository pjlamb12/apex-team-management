---
phase: 14-drill-library-foundation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - libs/client/data-access/drill/src/lib/drill.service.ts
  - libs/client/data-access/drill/src/lib/drill.model.ts
  - libs/client/ui/drill/tag-input/tag-input.ts
  - libs/client/ui/drill/tag-input/tag-input.html
  - package.json
autonomous: true
requirements: [DRIL-01, DRIL-02]
user_setup:
  - service: npm
    why: "Install YouTube player support"
    dashboard_config:
      - task: "Install @angular/youtube-player"
        location: "CLI"

must_haves:
  truths:
    - "Frontend can fetch drills and tags via Signal-based services"
    - "TagInput component supports adding and removing tags via ion-chips"
    - "External URLs are sanitized for safe iframe embedding"
  artifacts:
    - path: "libs/client/data-access/drill/src/lib/drill.service.ts"
      provides: "Signal-powered data access for drills"
    - path: "libs/client/ui/drill/tag-input/tag-input.ts"
      provides: "Reusable tagging UX"
  key_links:
    - from: "libs/client/ui/drill/tag-input/tag-input.ts"
      to: "ionic/angular"
      via: "IonChip and IonInput"
---

<objective>
Build the client-side data access layer and shared UI components required for the Drill Library.

Purpose: Provide a robust infrastructure for the library's feature modules.
Output: Signal-based DrillService, reusable TagInput component, and YouTube integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-drill-library-foundation/14-RESEARCH.md
@.planning/phases/14-drill-library-foundation/14-CONTEXT.md
@.planning/phases/14-drill-library-foundation/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Drill Data Access Library</name>
  <files>libs/client/data-access/drill/project.json, libs/client/data-access/drill/src/lib/drill.service.ts, libs/client/data-access/drill/src/lib/drill.model.ts</files>
  <action>
    Generate the library: `nx g @nx/angular:library client/data-access/drill --directory=libs/client/data-access/drill`.
    - Define `Drill` and `Tag` interfaces matching the backend entities.
    - Implement `DrillService` using Angular's `inject(HttpClient)`.
    - Provide Signal-based methods: `getDrills(tags?: string[])`, `getTags()`, `createDrill()`, `updateDrill()`, `deleteDrill()`.
    - Use `toSignal` for list fetching where appropriate.
  </action>
  <verify>
    <automated>nx test client-data-access-drill</automated>
  </verify>
  <done>Library created with functional Signal-based API service.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TagInput Component</name>
  <files>libs/client/ui/drill/tag-input/tag-input.ts, libs/client/ui/drill/tag-input/tag-input.html</files>
  <action>
    Create a new UI library `libs/client/ui/drill` if it doesn't exist.
    Implement `TagInput` as a standalone component.
    - Props: `selectedTags` (Signal or Input), `availableTags` (Signal or Input).
    - Template: Uses `ion-chip` for active tags and an `ion-input` for adding new ones (as per RESEARCH Code Example).
    - Logic: Emits events for add/remove. Handles Enter key for adding.
    - Style: Use Tailwind for layout (flex-wrap, border, rounded).
  </action>
  <verify>
    <automated>ls libs/client/ui/drill/tag-input/tag-input.ts</automated>
  </verify>
  <done>Reusable TagInput component implemented with Ionic chips.</done>
</task>

<task type="auto">
  <name>Task 3: Install YouTube Player and Setup Video Sanitization</name>
  <files>package.json, libs/client/ui/drill/video-embed/video-embed.ts</files>
  <action>
    - Install dependency: `npm install @angular/youtube-player@~21.2.0`.
    - Create a small `VideoEmbed` component (or shared utility) that takes a `sourceUrl` and returns a sanitized `SafeResourceUrl` (per RESEARCH Pattern 2).
    - Handle YouTube IDs vs. Vimeo IDs vs. generic URLs.
    - Use `DomSanitizer` with `computed` signals to avoid re-sanitization on every cycle.
  </action>
  <verify>
    <automated>grep "@angular/youtube-player" package.json</automated>
  </verify>
  <done>Video support integrated with secure sanitization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → External Media | Video IFrames are untrusted content. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | Video URLs | mitigate | Use `DomSanitizer.bypassSecurityTrustResourceUrl` only on validated/computed URLs. |
</threat_model>

<verification>
1. Run data-access unit tests: `nx test client-data-access-drill`.
2. Build UI components to check for compilation errors.
</verification>

<success_criteria>
- Drills can be fetched from API using the new service.
- TagInput renders and handles interactions correctly.
- Video URLs are sanitized using Signals.
</success_criteria>

<output>
After completion, create `.planning/phases/14-drill-library-foundation/14-02-SUMMARY.md`
</output>
