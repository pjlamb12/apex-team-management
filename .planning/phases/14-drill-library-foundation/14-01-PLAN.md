---
phase: 14-drill-library-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/entities/drill.entity.ts
  - apps/api/src/entities/tag.entity.ts
  - apps/api/src/drills/drills.service.ts
  - apps/api/src/drills/drills.controller.ts
  - apps/api/src/drills/drills.module.ts
  - apps/api/src/app/app.module.ts
autonomous: true
requirements: [DRIL-01, DRIL-02]

must_haves:
  truths:
    - "Drills can be persisted with structured JSONB instructions"
    - "Drills can be associated with multiple coach-level tags"
    - "API enforces coach ownership for all drill operations"
  artifacts:
    - path: "apps/api/src/entities/drill.entity.ts"
      provides: "Drill data model with JSONB instructions"
    - path: "apps/api/src/entities/tag.entity.ts"
      provides: "Coach-level tag model"
    - path: "apps/api/src/drills/drills.service.ts"
      provides: "Business logic for drills and tag filtering"
  key_links:
    - from: "apps/api/src/drills/drills.controller.ts"
      to: "apps/api/src/drills/drills.service.ts"
      via: "dependency injection"
---

<objective>
Establish the backend foundation for the Drill Library by creating the necessary database entities, migrations, and CRUD API endpoints.

Purpose: Provide a secure, searchable repository for coach-specific drills and tags.
Output: Drill and Tag entities, migrations, and functional API endpoints for coach-level library management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-drill-library-foundation/14-RESEARCH.md
@.planning/phases/14-drill-library-foundation/14-CONTEXT.md

<interfaces>
From apps/api/src/entities/user.entity.ts:
```typescript
@Entity('users')
export class UserEntity {
  @PrimaryGeneratedColumn('uuid')
  id: string;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Drill and Tag Entities</name>
  <files>apps/api/src/entities/drill.entity.ts, apps/api/src/entities/tag.entity.ts</files>
  <action>
    Create the TypeORM entities for drills and tags.
    - DrillEntity: id (uuid), coachId (uuid, FK to users), name (string), description (text), sourceUrl (string, nullable), instructions (jsonb).
    - TagEntity: id (uuid), coachId (uuid, FK to users), name (string, unique per coach).
    - Establish a Many-to-Many relationship between Drill and Tag using a join table `drill_tags`.
    - Per RESEARCH, add a GIN index to the instructions column in a manual migration (or note for future optimization).
  </action>
  <verify>
    <automated>ls apps/api/src/entities/drill.entity.ts apps/api/src/entities/tag.entity.ts</automated>
  </verify>
  <done>Entities defined with Many-to-Many relationship and JSONB support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DrillsService with Tag Filtering</name>
  <files>apps/api/src/drills/drills.service.ts, apps/api/src/drills/drills.service.spec.ts</files>
  <behavior>
    - findAll(coachId, tagIds[]): Returns drills for coach. If tagIds provided, uses QueryBuilder with double-join (per RESEARCH Pitfall 1) to filter by tags while returning all tags for the drill.
    - create(coachId, data): Persists new drill and tags.
    - findOne(id, coachId): Returns drill if owned by coach, else throws 404.
    - update(id, coachId, data): Updates drill if owned by coach.
    - delete(id, coachId): Deletes drill if owned by coach.
  </behavior>
  <action>
    Implement the service logic. Use TypeORM QueryBuilder for filtering to avoid the "Selected Tag Ghosting" pitfall.
    Ensure `coachId` is used in all queries to prevent cross-coach access.
  </action>
  <verify>
    <automated>nx test api --testFile=drills.service.spec.ts</automated>
  </verify>
  <done>Service implemented with ownership checks and efficient tag filtering.</done>
</task>

<task type="auto">
  <name>Task 3: Implement DrillsController and Module</name>
  <files>apps/api/src/drills/drills.controller.ts, apps/api/src/drills/drills.module.ts, apps/api/src/app/app.module.ts</files>
  <action>
    Create the REST controller with standard CRUD endpoints.
    - GET /api/drills: Supports query param `tags` (array of IDs).
    - POST /api/drills: Accepts name, description, sourceUrl, instructions, tags[].
    - GET /api/drills/tags: Returns all unique tags for the coach.
    - Use `@UseGuards(JwtAuthGuard)` and `@CurrentUser()` (or equivalent existing pattern) to get the `coachId`.
    - Register DrillsModule in AppModule.
  </action>
  <verify>
    <automated>curl -u coach:password http://localhost:3000/api/drills (Mocking auth if possible, or verify unit tests pass)</automated>
  </verify>
  <done>API endpoints exposed and authenticated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted JSON (instructions) and tag IDs cross here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | Drill instructions | mitigate | Validate instruction schema using AJV in service or class-validator on DTO. |
| T-14-01-02 | Spoofing | Drill Ownership | mitigate | Ensure every query includes coachId filter from JWT context. |
| T-14-01-03 | Tampering | Tag Injection | mitigate | Use TypeORM QueryBuilder parameterized inputs. |
</threat_model>

<verification>
1. Run API unit tests: `nx test api`.
2. Verify migration success: Check database schema for `drills`, `tags`, and `drill_tags`.
</verification>

<success_criteria>
- Drills can be created, read, updated, and deleted via API.
- Drills can be filtered by multiple tags.
- Ownership is strictly enforced by coachId.
</success_criteria>

<output>
After completion, create `.planning/phases/14-drill-library-foundation/14-01-SUMMARY.md`
</output>
