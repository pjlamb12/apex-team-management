---
phase: 11-practice-management-schedule
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/migrations/*-EventsRefactor.ts
  - apps/api/src/entities/event.entity.ts
  - apps/api/src/entities/game.entity.ts
  - apps/api/src/entities/game-event.entity.ts
  - apps/api/src/entities/lineup-entry.entity.ts
  - apps/api/src/data-source.ts
  - apps/api/src/games/games.module.ts
  - apps/api/src/games/games.service.ts
  - apps/api/src/games/games.service.spec.ts
  - apps/api/src/games/lineup-entries.service.ts
  - apps/api/src/teams/seasons.service.ts
  - apps/api/src/teams/seasons.service.spec.ts
autonomous: true
requirements:
  - PRAC-01
  - PRAC-02
  - SCHD-02

must_haves:
  truths:
    - "Database 'games' table is renamed to 'events'"
    - "EventEntity replaces GameEntity throughout backend code"
    - "Related tables use event_id instead of game_id"
  artifacts:
    - path: "apps/api/src/entities/event.entity.ts"
      provides: "Unified event model"
      contains: "@Entity('events')"
    - path: "apps/api/src/migrations/*-EventsRefactor.ts"
      provides: "Data layer transformation"
  key_links:
    - from: "apps/api/src/entities/event.entity.ts"
      to: "database table events"
      via: "@Entity decorator"
---

<objective>
Transition the database and core entities from "Games" to "Events" to support both games and practices.

Purpose: Provide a unified data model that can represent any team event, distinguished by a 'type' field.
Output: Renamed database tables, renamed entities, and updated backend references.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-practice-management-schedule/11-CONTEXT.md
@.planning/phases/11-practice-management-schedule/11-RESEARCH.md

<interfaces>
From apps/api/src/entities/game.entity.ts:
```typescript
@Entity('games')
export class GameEntity {
  @PrimaryGeneratedColumn('uuid')
  id: string;
  @Column({ name: 'season_id' })
  seasonId: string;
  @Column({ nullable: true })
  opponent: string;
  @Column({ name: 'scheduled_at', type: 'timestamp', nullable: true })
  scheduledAt: Date;
  @Column({ nullable: true })
  location: string | null;
  @Column({ name: 'uniform_color', nullable: true })
  uniformColor: string | null;
  @Column({ name: 'is_home_game', default: true })
  isHomeGame: boolean;
  @Column({ default: 'scheduled' })
  status: 'scheduled' | 'in_progress' | 'completed';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Migration for Events</name>
  <files>apps/api/src/migrations/*-EventsRefactor.ts</files>
  <action>
    Generate and implement a TypeORM migration to:
    1. Rename 'games' table to 'events'.
    2. Add 'type' column (enum: 'game' | 'practice', default: 'game').
    3. Add 'duration_minutes' (int, nullable) and 'notes' (text, nullable).
    4. Rename 'game_id' to 'event_id' in 'lineup_entries' and 'game_events'.
    5. Update all related foreign key constraints and indexes to reflect the rename.
    Ensure 'opponent', 'uniform_color', and 'is_home_game' remain but are nullable for practices.
    6. Explicitly verify the 'down' migration functionality for reversibility.
  </action>
  <verify>
    <automated>npx nx run api:typeorm migration:run && npx nx run api:typeorm migration:revert && npx nx run api:typeorm migration:run</automated>
  </verify>
  <done>Migration executed successfully, table and columns renamed, and reversibility verified.</done>
</task>

<task type="auto">
  <name>Task 2: Rename GameEntity to EventEntity</name>
  <files>
    apps/api/src/entities/event.entity.ts,
    apps/api/src/entities/game.entity.ts,
    apps/api/src/entities/lineup-entry.entity.ts,
    apps/api/src/entities/game-event.entity.ts
  </files>
  <action>
    1. Rename apps/api/src/entities/game.entity.ts to event.entity.ts.
    2. Rename 'GameEntity' class to 'EventEntity'.
    3. Update @Entity('games') to @Entity('events').
    4. Add 'type', 'durationMinutes', and 'notes' properties per D-01.
    5. Update 'LineupEntryEntity' and 'GameEventEntity' to use 'EventEntity' and @JoinColumn({ name: 'event_id' }).
    6. Rename 'gameId' fields to 'eventId' in related entities.
  </action>
  <verify>
    <automated>npx nx test api --testNamePattern="Entity"</automated>
  </verify>
  <done>Entities renamed and property names updated.</done>
</task>

<task type="auto">
  <name>Task 3: Update Backend References to EventEntity</name>
  <files>
    apps/api/src/data-source.ts,
    apps/api/src/games/games.module.ts,
    apps/api/src/games/games.service.ts,
    apps/api/src/games/games.service.spec.ts,
    apps/api/src/games/lineup-entries.service.ts,
    apps/api/src/teams/seasons.service.ts,
    apps/api/src/teams/seasons.service.spec.ts
  </files>
  <action>
    Refactor all occurrences of 'GameEntity' to 'EventEntity' across the backend.
    Update imports and repository injections.
    Update 'gameRepo' variable names to 'eventRepo' where appropriate for consistency.
    Update 'apps/api/src/games/lineup-entries.service.ts' to use the renamed 'eventId' property (from 'gameId').
    Ensure existing tests pass with the renamed entity.
  </action>
  <verify>
    <automated>npx nx test api</automated>
  </verify>
  <done>Backend code uses EventEntity consistently, all tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Migration | High-privileged operation modifying core schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | Database | mitigate | Use TypeORM migrations for traceable schema changes. |
| T-11-01-02 | Information Disclosure | Event data | mitigate | Ensure foreign key constraints are correctly preserved during rename to prevent orphaned data. |
</threat_model>

<verification>
Verify that the 'events' table exists with the new columns and that the application starts and passes existing tests using EventEntity.
</verification>

<success_criteria>
- Migration applied successfully.
- Codebase uses EventEntity.
- 'npx nx test api' passes.
</success_criteria>

<output>
After completion, create .planning/phases/11-practice-management-schedule/11-01-SUMMARY.md
</output>
