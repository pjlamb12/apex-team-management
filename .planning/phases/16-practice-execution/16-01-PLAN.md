---
phase: 16-practice-execution
plan: 01
type: execute
wave: 1
depends_on: [15-05]
files_modified:
  - libs/client/data-access/drill/src/lib/practice-pacer.service.ts
  - libs/client/data-access/drill/src/index.ts
autonomous: true
requirements: [PRAC-04]
must_haves:
  truths:
    - "A pacer service exists to track live practice execution state."
    - "Service supports start, pause, next, previous, and reset operations."
    - "Service emits alerts (haptics) when a drill duration is reached."
    - "Execution state is persisted to localStorage."
  artifacts:
    - path: "libs/client/data-access/drill/src/lib/practice-pacer.service.ts"
      provides: "PracticePacerService for live tracking"
---

<objective>
Implement the core logic for the practice pacer timer and state management.
Purpose: Provide the engine that drives the live practice execution UI.
Output: PracticePacerService.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-practice-execution/16-RESEARCH.md
@libs/client/data-access/drill/src/lib/drill.model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PracticePacerService</name>
  <files>libs/client/data-access/drill/src/lib/practice-pacer.service.ts</files>
  <action>
    - Create `PracticePacerService` in `libs/client/data-access/drill/src/lib/`.
    - Use `signal` for state: `drills`, `activeDrillIndex`, `remainingSeconds`, `isRunning`, `isFinished`.
    - Implement `initialize(practiceId: string, drills: PracticeDrill[])`:
        - Load state from localStorage if available.
        - Otherwise, start at index 0 with first drill's duration.
    - Implement `start()`, `pause()`, `reset()`.
    - Implement `next()`: Advance to next drill, reset timer.
    - Implement `previous()`: Go back, reset timer.
    - Timer logic:
        - Decriment `remainingSeconds` every second.
        - If `remainingSeconds` hits 0, trigger haptics (`Haptics.vibrate()`).
        - Continue into negative seconds (overtime) but keep vibrating periodically (e.g., every 30s).
    - Persist state on change.
  </action>
  <verify>
    <automated>npx nx test client-data-access-drill</automated>
  </verify>
  <done>Service implemented with full tracking and persistence logic.</done>
</task>

<task type="auto">
  <name>Task 2: Export service</name>
  <files>libs/client/data-access/drill/src/index.ts</files>
  <action>
    - Export `PracticePacerService` from the library entry point.
  </action>
  <verify>
    <automated>npx nx build client-data-access-drill</automated>
  </verify>
  <done>Service exported and ready for use in features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → LocalStorage | State persistence occurs here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | LocalStorage | mitigate | Ensure state is validated when loaded from storage to prevent crashes if users manually edit data. |
</threat_model>

<verification>
Run unit tests for the pacer service.
</verification>

<success_criteria>
- Timer correctly decrements and advances drills.
- State persists across page reloads.
- Haptics are triggered at zero.
</success_criteria>

<output>
After completion, create `.planning/phases/16-practice-execution/16-01-SUMMARY.md`
</output>
