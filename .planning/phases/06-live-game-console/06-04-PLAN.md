---
phase: 06-live-game-console
plan: 04
type: execute
wave: 3
depends_on: ["03"]
files_modified: [libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts, libs/client/feature/game-console/src/lib/bench-view/bench-view.ts, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts]
autonomous: true
requirements: [LIVE-03, LIVE-02]

must_haves:
  truths:
    - "Coach can see players in their assigned positions on a soccer pitch visualization"
    - "Coach can see bench players in a dedicated scrollable list"
    - "Tapping a bench player then a pitch player triggers a SUB event"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts"
      provides: "Tactical soccer field UI"
    - path: "libs/client/feature/game-console/src/lib/bench-view/bench-view.ts"
      provides: "Bench player list"
---

<objective>
Implement the sport-specific field visualization and the core 2-tap substitution interaction.

Purpose: Provide the visual context for the game and a fast, touch-optimized way to swap players.
Output: Pitch View, Bench View, and integration for the 2-tap swap logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-live-game-console/06-CONTEXT.md
@.planning/phases/06-live-game-console/06-RESEARCH.md
@libs/client/feature/game-console/src/lib/live-game-state.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BenchViewComponent</name>
  <files>libs/client/feature/game-console/src/lib/bench-view/bench-view.ts, libs/client/feature/game-console/src/lib/bench-view/bench-view.html</files>
  <action>
    Create a component that renders a horizontal or vertical list of players currently on the bench.
    - Each player card shows name and jersey number.
    - Support selection (via an output event or signal).
    - Use `LiveGameStateService` to determine the current bench roster.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>Bench view component renders available players and handles selection</done>
</task>

<task type="auto">
  <name>Task 2: Implement SoccerPitchViewComponent</name>
  <files>libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts, libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.html</files>
  <action>
    Create a component that visualizes a soccer pitch with slots for active positions.
    - Use SVG or Tailwind to create the pitch lines.
    - Position players based on their assigned position types.
    - Support selection for swaps.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>Soccer pitch visualization renders active players in tactical positions</done>
</task>

<task type="auto">
  <name>Task 3: Implement 2-tap swap logic</name>
  <files>libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.html</files>
  <action>
    Integrate `BenchView` and `SoccerPitchView` into the `ConsoleWrapper`.
    - Maintain a `selectedBenchPlayer` signal in `ConsoleWrapper`.
    - When a bench player is selected, highlight them.
    - When a pitch player/slot is tapped while a bench player is selected:
      1. Trigger `SUB` event in `LiveGameStateService`.
      2. Clear the selection.
    - Add haptic feedback (if available via Capacitor later, use a mock for now).
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:test</automated>
  </verify>
  <done>2-tap substitution flow functional and logs SUB events</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Interaction → State | Input events triggering state changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | Invalid Substitution | mitigate | LiveGameStateService should prevent multiple players in same position or using players not on roster |
</threat_model>

<verification>
1. Mock starting lineup in test.
2. Verify selection of bench player.
3. Verify tap on field player replaces the player and logs SUB event.
</verification>

<success_criteria>
1. Bench list displays all players not on the field.
2. Pitch view displays all starting players.
3. Substitution successfully updates local state.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-game-console/06-04-SUMMARY.md`
</output>
