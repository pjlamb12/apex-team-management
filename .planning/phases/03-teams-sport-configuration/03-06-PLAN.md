---
phase: "03-teams-sport-configuration"
plan: "06"
type: "execute"
wave: 3
depends_on: 
  - "03-04-PLAN.md"
  - "03-05-PLAN.md"
files_modified:
  - "apps/frontend/src/app/teams/teams-list/teams-list.ts"
  - "apps/frontend/src/app/teams/teams-list/teams-list.html"
  - "apps/frontend/src/app/teams/teams-list/teams-list.scss"
  - "apps/frontend/src/app/teams/create-team/create-team.ts"
  - "apps/frontend/src/app/teams/create-team/create-team.html"
  - "apps/frontend/src/app/teams/create-team/create-team.scss"
  - "apps/frontend/src/app/teams/edit-team/edit-team.ts"
  - "apps/frontend/src/app/teams/edit-team/edit-team.html"
  - "apps/frontend/src/app/teams/edit-team/edit-team.scss"
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-04
  - TEAM-05

must_haves:
  truths:
    - "TeamsList component renders teams cards and an empty state when no teams exist."
    - "TeamsList has a Create Team FAB button."
    - "TeamsList deletes team with AlertController confirmation."
    - "CreateTeam form renders with name and active sports dropdown, navigating back to /teams upon successful POST."
    - "EditTeam form renders name with read-only sport badge, and can PATCH name."
---

<objective>
Wave 3: Implement the 3 frontend UI components (teams-list, create-team, edit-team) matching the specification in CONTEXT.md and RESEARCH.md. 

This plan:
1. Implements `TeamsList` standalone component which fetches list of teams and renders cards.
2. Implements `CreateTeam` standalone component to POST new team data with a Sport dropdown.
3. Implements `EditTeam` standalone component to PATCH a team's name and read the team data via ID param.

All HTTP requests use `inject(HttpClient)` with `firstValueFrom` and Signals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-teams-sport-configuration/03-CONTEXT.md
@.planning/phases/03-teams-sport-configuration/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TeamsList Component (TEAM-05)</name>
  <files>
    apps/frontend/src/app/teams/teams-list/teams-list.ts
    apps/frontend/src/app/teams/teams-list/teams-list.html
    apps/frontend/src/app/teams/teams-list/teams-list.scss
  </files>
  <read_first>
    - apps/frontend/src/app/teams/teams-list/teams-list.spec.ts
    - apps/frontend/src/app/auth/login/login.ts
  </read_first>
  <action>
Implement `TeamsList` component.
  
**In `apps/frontend/src/app/teams/teams-list/teams-list.ts`:**
- Implement as a standalone component importing `IonContent, IonList, IonCard, IonCardHeader, IonCardTitle, IonCardContent, IonButton, IonIcon, IonFab, IonFabButton, IonLabel, IonSpinner, RouterLink` and `NgClass` if needed.
- Define `apiUrl` from `RuntimeConfigLoaderService`.
- Declare `teams` signal, `isLoading` signal.
- In `ngOnInit()`, call `loadTeams()`.
- `loadTeams()` method triggers `GET /teams`.
- `confirmDelete(team: any)` method triggers `AlertController` as mocked in `teams-list.spec.ts`, and upon confirmation triggers `DELETE /teams/:id` and reloads.

**In `apps/frontend/src/app/teams/teams-list/teams-list.html`:**
- Ensure an `ion-content` tag wrapper.
- Empty state: Show when `teams().length === 0 && !isLoading()`, displaying an icon, "No teams yet — create your first team", and a Create button.
- Sub-components for Cards with `[routerLink]="['/teams', team.id]"` for editing, and an explicitly visible Sport Badge (`ion-badge`).
- Add an `ion-fab` fixed button with `routerLink="/teams/new"`.
  </action>
  <verify>
    <automated>grep -q "\.delete" apps/frontend/src/app/teams/teams-list/teams-list.ts && grep -q "ion-fab" apps/frontend/src/app/teams/teams-list/teams-list.html</automated>
  </verify>
  <acceptance_criteria>
    - `teams-list.ts` accesses HTTP with `firstValueFrom`
    - `teams-list.ts` uses `inject(AlertController)` and `header: 'Delete Team'` identically to spec mock
    - `teams-list.html` has a `.length === 0` empty state
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement CreateTeam Component (TEAM-01, TEAM-02)</name>
  <files>
    apps/frontend/src/app/teams/create-team/create-team.ts
    apps/frontend/src/app/teams/create-team/create-team.html
    apps/frontend/src/app/teams/create-team/create-team.scss
  </files>
  <read_first>
    - apps/frontend/src/app/teams/create-team/create-team.spec.ts
  </read_first>
  <action>
Implement `CreateTeam` component.

**In `create-team.ts`:**
- Build a reactive form using `inject(FormBuilder)`: `name` (required), `sportId` (required).
- `ngOnInit` does `GET /sports` and loads `sports` signal. It preselects Soccer (or first available sport).
- `submit()` method checks validity, `POST /teams` the form values, and uses `router.navigate(['/teams'])` on success.

**In `create-team.html`:**
- Provide form inputs: `ion-input` for name, and `ion-select` for Sport dropdown looping `sports()`.
- Use `ngx-control-errors-display` wrapped appropriately inside or next to `ion-item`.
- Form submit button logic matching auth components layout.
  </action>
  <verify>
    <automated>grep -q "router.navigate.*teams" apps/frontend/src/app/teams/create-team/create-team.ts && grep -q "ion-select" apps/frontend/src/app/teams/create-team/create-team.html</automated>
  </verify>
  <acceptance_criteria>
    - `create-team.ts` loads sports dynamically and binds to form
    - Form posts effectively, then navigates to `/teams`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement EditTeam Component (TEAM-04)</name>
  <files>
    apps/frontend/src/app/teams/edit-team/edit-team.ts
    apps/frontend/src/app/teams/edit-team/edit-team.html
    apps/frontend/src/app/teams/edit-team/edit-team.scss
  </files>
  <read_first>
    - apps/frontend/src/app/app.routes.ts
  </read_first>
  <action>
Implement `EditTeam` component.

**In `edit-team.ts`:**
- Inject `ActivatedRoute`. On init, get `id` parameter.
- Fetch `GET /teams/:id` and prefill the reactive form `name`.
- Team name is editable. Sport is set to read-only label (no dropdown per D-08).
- `submit()` PATCHes `/teams/:id` with `{name}` and navigates to `/teams` or displays success toast.

**In `edit-team.html`:**
- Similar form to Create but sport is displayed only as text (e.g. `{{ team()?.sport?.name }}`).
- Allow save.
  </action>
  <verify>
    <automated>grep -q "\.patch" apps/frontend/src/app/teams/edit-team/edit-team.ts && grep -q "ActivatedRoute" apps/frontend/src/app/teams/edit-team/edit-team.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ActivatedRoute` accessed using `inject`
    - `PATH /teams/:id` logic inside submit
    - Sport is intentionally rendered read-only
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser | Client side form logic |
| API | All validations performed and repeated via server side DTOs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W3-01 | Tampering | User patches sportId | accept | DTO in Backend UpdateTeamDto ignores sportId, only exposes name. Frontend EditTeam disables it visually. |
</threat_model>

<verification>
After all tasks:
- `nx build frontend` exits 0 (TypeScript compilation)
- `nx test frontend` passes all stubs created in Plan 02 and completes without errors.
</verification>

<success_criteria>
- Frontend properly renders a teams list.
- Creating a team form triggers dynamic sports list accurately.
- Editing page shows team information safely.
- No compilation/type errors for tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-teams-sport-configuration/03-06-SUMMARY.md`
</output>
