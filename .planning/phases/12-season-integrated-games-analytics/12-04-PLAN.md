---
phase: 12
plan: 04
type: execute
wave: 3
depends_on: ["12-02"]
files_modified: [apps/frontend/src/app/teams/events/schedule/schedule.ts, apps/frontend/src/app/teams/events/schedule/schedule.html]
autonomous: true
requirements: [GAME-06]
must_haves:
  truths:
    - "Season picker appears on the Schedule tab"
    - "Changing the season updates the event list"
    - "Active season is selected by default"
  artifacts:
    - path: "apps/frontend/src/app/teams/events/schedule/schedule.ts"
      provides: "Season selection logic"
    - path: "apps/frontend/src/app/teams/events/schedule/schedule.html"
      provides: "Season picker UI"
---

<objective>
Implement a season filter on the Schedule tab to allow coaches to view events for specific seasons.

Purpose: Provide a way to browse past and present game schedules without leaving the main view.
Output: Integrated season picker in the Schedule component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-season-integrated-games-analytics/12-CONTEXT.md
@.planning/phases/12-season-integrated-games-analytics/12-RESEARCH.md
@.planning/phases/12-season-integrated-games-analytics/12-UI-SPEC.md
@apps/frontend/src/app/teams/events/schedule/schedule.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add season loading and selection logic to Schedule component</name>
  <files>apps/frontend/src/app/teams/events/schedule/schedule.ts</files>
  <action>
    - Inject `SeasonsService`.
    - Add `seasons = signal<Season[]>([])` and `selectedSeasonId = signal<string | null>(null)`.
    - Add `isLoadingSeasons = signal(false)`.
    - Implement `loadSeasons(teamId)`:
      - Fetch all seasons.
      - Set `seasons` signal.
      - Set `selectedSeasonId` to the `active` season by default (or the first one if none active).
    - Modify `loadEvents` effect/method to watch `selectedSeasonId` and pass it to `getEvents`.
    - Add `onSeasonChange(event)` handler to update `selectedSeasonId`.
  </action>
  <verify>
    <automated>nx test frontend --testFile=apps/frontend/src/app/teams/events/schedule/schedule.spec.ts</automated>
  </verify>
  <done>Component logic handles season loading and reacts to selection changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add ion-select season picker to Schedule template</name>
  <files>apps/frontend/src/app/teams/events/schedule/schedule.html</files>
  <action>
    - Add the `ion-select` picker above the `ion-segment`.
    - Match the UI-SPEC design:
      - Use `ion-item` wrapper.
      - Floating label "Season".
      - Render `seasons()` as options.
      - Show "(Active)" suffix for the active season option.
    - Show `ion-spinner` while loading seasons.
    - Handle the "no seasons" edge case with a muted text message.
  </action>
  <verify>
    <automated>nx build frontend</automated>
  </verify>
  <done>Season picker is visible and styled according to UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Service | Selected seasonId passed to getEvents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | Schedule Component | accept | Frontend filtering; backend still verifies team ownership of requested events |
</threat_model>

<verification>
Manual verification: Open the app, navigate to Schedule, verify the season picker is present and defaults to the active season. Change season and ensure the list updates.
</verification>

<success_criteria>
- Coach can select a season from a dropdown on the Schedule tab.
- Selecting a season filters the games shown in both "Upcoming" and "Past" segments.
- Selection defaults to the active season on initial load.
</success_criteria>

<output>
After completion, create `.planning/phases/12-season-integrated-games-analytics/12-04-SUMMARY.md`
</output>
