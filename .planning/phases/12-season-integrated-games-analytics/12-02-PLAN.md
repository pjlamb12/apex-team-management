---
phase: 12
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: [apps/api/src/events/events.service.ts, apps/api/src/events/events.controller.ts, apps/frontend/src/app/teams/events/events.service.ts, apps/api/src/events/events.service.spec.ts]
autonomous: true
requirements: [GAME-06, GAME-08]
must_haves:
  truths:
    - "Events can be filtered by seasonId in the API"
    - "GET /events/:id returns goalEventCount for games"
    - "Frontend EventsService matches backend filtering and score capabilities"
  artifacts:
    - path: "apps/api/src/events/events.service.ts"
      provides: "Season filter and Goal pre-fill logic"
    - path: "apps/frontend/src/app/teams/events/events.service.ts"
      provides: "Updated EventEntity interface and getEvents signature"
---

<objective>
Implement the backend business logic for season-based filtering and score pre-fill data. Update frontend service contracts to match.

Purpose: Provide the data foundation for the frontend schedule and score confirmation.
Output: Updated backend service and controller; updated frontend service and entity interface.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-season-integrated-games-analytics/12-CONTEXT.md
@.planning/phases/12-season-integrated-games-analytics/12-RESEARCH.md
@apps/api/src/events/events.service.ts
@apps/frontend/src/app/teams/events/events.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update EventsService.findAllForTeam with season filter</name>
  <files>apps/api/src/events/events.service.ts, apps/api/src/events/events.controller.ts</files>
  <action>
    - Update `EventsService.findAllForTeam(teamId, scope, seasonId?)` signature to accept an optional `seasonId?: string`.
    - Modify the query logic to use `seasonId` if provided.
    - If `seasonId` is NOT provided, fall back to the existing "find active season" logic to preserve backward compatibility (D-05 fallback).
    - Update `EventsController.findAllForTeam` to accept `seasonId` from `@Query('seasonId')`.
  </action>
  <verify>
    <automated>nx test api --testFile=apps/api/src/events/events.service.spec.ts</automated>
  </verify>
  <done>findAllForTeam filters by seasonId when provided, defaults to active season otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Augment findOne with goalEventCount for pre-fill (D-08)</name>
  <files>apps/api/src/events/events.service.ts</files>
  <action>
    Modify `EventsService.findOne(eventId)`:
    - If the event `type === 'game'`, count the associated `GameEventEntity` records where `eventType === 'GOAL'`.
    - Return the count as a virtual `goalEventCount` property on the response object.
    - Use `this.gameEventRepo.count({ where: { eventId, eventType: 'GOAL' } })`.
    - Ensure the return type allows for this optional field.
  </action>
  <verify>
    <automated>nx test api --testFile=apps/api/src/events/events.service.spec.ts</automated>
  </verify>
  <done>findOne returns the number of goals logged for the game.</done>
</task>

<task type="auto">
  <name>Task 3: Update frontend EventsService and Entity interface</name>
  <files>apps/frontend/src/app/teams/events/events.service.ts</files>
  <action>
    - Update `EventEntity` interface: add `goalsFor: number | null`, `goalsAgainst: number | null`, and `goalEventCount?: number`.
    - Update `getEvents(teamId, scope, seasonId?)` signature to accept optional `seasonId?: string`.
    - Append `seasonId` to HTTP query parameters if provided.
  </action>
  <verify>
    <automated>nx build frontend</automated>
  </verify>
  <done>Frontend EventsService supports season-based filtering and new score fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller -> Service | Query parameters passed to services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Information Disclosure | EventsService.findAllForTeam | mitigate | Ensure teamId is verified for the requested seasonId if provided (implicit via season lookup in existing logic) |
</threat_model>

<verification>
Ensure backend tests pass: `nx test api --testFile=apps/api/src/events/events.service.spec.ts`
</verification>

<success_criteria>
- Backend API supports filtering events by season.
- Backend API provides pre-fill data for game scores.
- Frontend service is aligned with API changes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-season-integrated-games-analytics/12-02-SUMMARY.md`
</output>
