---
phase: 12
plan: 05
type: execute
wave: 4
depends_on: ["12-03", "12-04"]
files_modified: [apps/frontend/src/app/teams/events/edit-event/edit-event.ts, apps/frontend/src/app/teams/events/edit-event/edit-event.html, apps/frontend/src/app/teams/seasons/season-detail/season-detail.ts, apps/frontend/src/app/teams/seasons/season-detail/season-detail.html]
autonomous: true
requirements: [GAME-08]
must_haves:
  truths:
    - "goalsFor and goalsAgainst fields appear in Edit Event for games"
    - "goalsFor pre-fills with goalEventCount on completed games"
    - "Season stats grid appears on the Season Detail screen"
    - "Goal difference is correctly color-coded"
  artifacts:
    - path: "apps/frontend/src/app/teams/events/edit-event/edit-event.ts"
      provides: "Score field pre-fill logic"
    - path: "apps/frontend/src/app/teams/seasons/season-detail/season-detail.html"
      provides: "Stats card grid UI"
---

<objective>
Implement score tracking in the event editor and display aggregate stats on the season detail screen.

Purpose: Enable coaches to track game outcomes and view season-level performance summaries.
Output: Updated Edit Event form and new Stats section on Season Detail screen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-season-integrated-games-analytics/12-CONTEXT.md
@.planning/phases/12-season-integrated-games-analytics/12-RESEARCH.md
@.planning/phases/12-season-integrated-games-analytics/12-UI-SPEC.md
@apps/frontend/src/app/teams/events/edit-event/edit-event.ts
@apps/frontend/src/app/teams/seasons/season-detail/season-detail.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add score fields and pre-fill logic to EditEvent component</name>
  <files>apps/frontend/src/app/teams/events/edit-event/edit-event.ts, apps/frontend/src/app/teams/events/edit-event/edit-event.html</files>
  <action>
    - Update the `FormGroup` to include `goalsFor` and `goalsAgainst`.
    - Apply `Validators.min(0)` to both.
    - Implement the pre-fill logic in `loadEvent` (or equivalent):
      - If `status === 'completed'` and `type === 'game'` and `goalsFor === null`:
        - Patch `goalsFor` with `goalEventCount` from the event data.
    - Update the template:
      - Add `ion-input` fields for `goalsFor` and `goalsAgainst` inside an `@if (event()?.type === 'game')` block.
      - Add the informational pre-fill note below `goalsFor` when pre-filled.
  </action>
  <verify>
    <automated>nx test frontend --testFile=apps/frontend/src/app/teams/events/edit-event/edit-event.spec.ts</automated>
  </verify>
  <done>Scores can be entered and confirmed; goalsFor pre-fills automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stats loading and signal state in SeasonDetail component</name>
  <files>apps/frontend/src/app/teams/seasons/season-detail/season-detail.ts</files>
  <action>
    - Inject `SeasonsService`.
    - Add `stats = signal<SeasonStats | null>(null)`.
    - Add `isLoadingStats = signal(false)` and `statsError = signal(false)`.
    - Implement `loadStats(teamId, seasonId)`:
      - Call `getSeasonStats`.
      - Update signals.
    - Add `statCards` computed signal to transform raw stats into the 3-column grid structure (W/L/D/GF/GA/GD).
  </action>
  <verify>
    <automated>nx build frontend</automated>
  </verify>
  <done>Component logic handles loading and state management for stats.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Season Stats UI in SeasonDetail template</name>
  <files>apps/frontend/src/app/teams/seasons/season-detail/season-detail.html</files>
  <action>
    - Add the "Season Stats" section below the existing form.
    - Match the UI-SPEC stat card grid:
      - `ion-card` wrapper.
      - 3-column grid.
      - 3px blue border-left on each cell.
      - Large display values, small labels.
    - Handle loading, empty (all zeros), and error states as defined in the UI-SPEC.
    - Color-code Goal Difference (GD).
  </action>
  <verify>
    <automated>nx build frontend</automated>
  </verify>
  <done>Season stats are displayed correctly in a grid on the season detail screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> UI | Stats data displayed in component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | Edit Event Form | mitigate | Backend validation (@Min(0)) and frontend Validators.min(0) |
</threat_model>

<verification>
Manual verification: Open Season Detail for a season with completed games. Verify the stats grid shows correct counts. Edit a game, set status to completed, and verify `goalsFor` is pre-filled.
</verification>

<success_criteria>
- Edit Event form supports entering goals for/against for games.
- Completed games pre-fill `goalsFor` with the number of logged goal events.
- Season Detail screen displays a grid with W/L/D/GF/GA/GD.
- GD is color-coded based on its value.
</success_criteria>

<output>
After completion, create `.planning/phases/12-season-integrated-games-analytics/12-05-SUMMARY.md`
</output>
