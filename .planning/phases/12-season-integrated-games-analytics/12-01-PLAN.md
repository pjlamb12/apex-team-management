---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/api/src/migrations/1776800000000-AddScoreToEvents.ts, apps/api/src/entities/event.entity.ts, apps/api/src/events/dto/create-event.dto.ts, apps/api/src/events/dto/update-event.dto.ts]
autonomous: true
requirements: [GAME-08]
must_haves:
  truths:
    - "Database schema includes goals_for and goals_against columns on events table"
    - "EventEntity includes goalsFor and goalsAgainst fields"
    - "Create and Update Event DTOs support goalsFor and goalsAgainst with non-negative validation"
  artifacts:
    - path: "apps/api/src/migrations/1776800000000-AddScoreToEvents.ts"
      provides: "Score columns migration"
    - path: "apps/api/src/entities/event.entity.ts"
      provides: "Score fields on EventEntity"
  key_links:
    - from: "apps/api/src/entities/event.entity.ts"
      to: "events table"
      via: "TypeORM decorators"
---

<objective>
Prepare the database and backend data models to store game outcomes (scores).

Purpose: Enable aggregate season stats calculation by persisting goals for and against per game.
Output: TypeORM migration and updated DTOs/Entities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-season-integrated-games-analytics/12-CONTEXT.md
@.planning/phases/12-season-integrated-games-analytics/12-RESEARCH.md
@apps/api/src/entities/event.entity.ts
@apps/api/src/events/dto/create-event.dto.ts
@apps/api/src/events/dto/update-event.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeORM migration for score columns</name>
  <files>apps/api/src/migrations/1776800000000-AddScoreToEvents.ts</files>
  <action>
    Create a new TypeORM migration file named `1776800000000-AddScoreToEvents.ts`.
    The migration should:
    - In `up()`: Add `goals_for` (int, nullable) and `goals_against` (int, nullable) to the `events` table.
    - In `down()`: Drop these two columns.
    
    Use the existing migration pattern from `apps/api/src/migrations/1776700000000-EventsRefactor.ts`.
    Wait: Verify the table name is "events".
  </action>
  <verify>
    <automated>ls apps/api/src/migrations/1776800000000-AddScoreToEvents.ts</automated>
  </verify>
  <done>Migration file exists with correct SQL commands.</done>
</task>

<task type="auto">
  <name>Task 2: Update EventEntity with goalsFor/Against fields</name>
  <files>apps/api/src/entities/event.entity.ts</files>
  <action>
    Add `goalsFor` and `goalsAgainst` fields to `EventEntity`.
    - Both should be `number | null`.
    - Use `@Column({ name: 'goals_for', type: 'int', nullable: true })` for `goalsFor`.
    - Use `@Column({ name: 'goals_against', type: 'int', nullable: true })` for `goalsAgainst`.
    - Place them after the `notes` column.
  </action>
  <verify>
    <automated>grep -E "goalsFor|goalsAgainst" apps/api/src/entities/event.entity.ts</automated>
  </verify>
  <done>EventEntity contains goalsFor and goalsAgainst columns mapped to the database.</done>
</task>

<task type="auto">
  <name>Task 3: Update Event DTOs with non-negative validation</name>
  <files>apps/api/src/events/dto/create-event.dto.ts, apps/api/src/events/dto/update-event.dto.ts</files>
  <action>
    Add `goalsFor` and `goalsAgainst` to `CreateEventDto` and `UpdateEventDto`.
    - Use `@IsOptional()`, `@IsInt()`, and `@Min(0)` decorators.
    - Type: `number`.
    - Ensure `class-validator` and `class-transformer` decorators are used consistently with existing fields.
  </action>
  <verify>
    <automated>grep -E "goalsFor|goalsAgainst" apps/api/src/events/dto/create-event.dto.ts apps/api/src/events/dto/update-event.dto.ts</automated>
  </verify>
  <done>DTOs include score fields with non-negative integer validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Score inputs cross here via Create/Update Event DTOs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | CreateEventDto / UpdateEventDto | mitigate | Enforce non-negative integer validation with @Min(0) and @IsInt() per ASVS V5 |
</threat_model>

<verification>
Run `nx build api` to ensure no TypeScript or DTO errors.
</verification>

<success_criteria>
- Backend models and database schema are ready to receive and store score data.
- Migration is created and consistent with entity changes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-season-integrated-games-analytics/12-01-SUMMARY.md`
</output>
