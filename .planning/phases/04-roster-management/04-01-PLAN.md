---
wave: 0
depends_on: []
files_modified:
  - "apps/api/src/entities/player.entity.ts"
  - "apps/api/src/migrations/AddParentEmailToPlayer.ts"
  - "apps/api/src/players/players.controller.spec.ts"
  - "apps/api/src/players/players.service.spec.ts"
  - "apps/api/src/players/players.module.ts"
autonomous: true
---

# Wave 0: Data Model and API Foundation (Roster)

This plan adds the required database fields for Roster Management per ROST-01, generates the TypeORM migration, and sets up the backend module foundation.

## Requirements Covered
- **ROST-01**: Coach can add a player with name, jersey number, and contact email

## Verification Validation
- Automated tests stubbed for API logic.
- TypeORM migration ran cleanly.

## Tasks

```xml
<task>
  <id>04-01-01</id>
  <read_first>
    - apps/api/src/entities/player.entity.ts
  </read_first>
  <action>
    Modify `PlayerEntity` in `apps/api/src/entities/player.entity.ts` to add the new `parentEmail` column. Define it as `@Column({ name: 'parent_email', nullable: true })` (allow nullable in DB for older records, APIs can strictly validate).
  </action>
  <acceptance_criteria>
    - `grep -q "parentEmail" apps/api/src/entities/player.entity.ts` exits 0.
  </acceptance_criteria>
</task>

<task>
  <id>04-01-02</id>
  <read_first>
    - apps/api/src/entities/player.entity.ts
    - package.json
  </read_first>
  <action>
    Generate a new TypeORM migration named `AddParentEmailToPlayer`.
    Run: `npx typeorm-ts-node-commonjs migration:generate apps/api/src/migrations/AddParentEmailToPlayer -d apps/api/src/data-source.ts`
  </action>
  <acceptance_criteria>
    - `ls apps/api/src/migrations/*AddParentEmailToPlayer.ts` exits 0.
  </acceptance_criteria>
</task>

<task>
  <id>04-01-03</id>
  <read_first>
    - apps/api/src/migrations/*AddParentEmailToPlayer.ts
  </read_first>
  <action>
    [BLOCKING] Schema Push Required.
    Apply the database schema changes so the live PostgreSQL instance is updated.
    Run: `npx typeorm-ts-node-commonjs migration:run -d apps/api/src/data-source.ts`
  </action>
  <acceptance_criteria>
    - The output of `psql $DATABASE_URL -c "\d players"` contains `parent_email`. OR the migration run command exits 0 successfully.
  </acceptance_criteria>
</task>

<task>
  <id>04-01-04</id>
  <read_first>
    - apps/api/src/app/app.module.ts
  </read_first>
  <action>
    Create the NestJS `PlayersModule` (`apps/api/src/players/players.module.ts`) encompassing `PlayersController` and `PlayersService`, and add it to the imports array in `apps/api/src/app/app.module.ts`. Register `PlayerEntity` using `TypeOrmModule.forFeature([PlayerEntity])`.
  </action>
  <acceptance_criteria>
    - `grep -q "PlayersModule" apps/api/src/app/app.module.ts`
  </acceptance_criteria>
</task>

<task>
  <id>04-01-05</id>
  <read_first>
    - apps/api/src/players/players.module.ts
  </read_first>
  <action>
    Create test stubs `players.controller.spec.ts` and `players.service.spec.ts` inside `apps/api/src/players/` returning placeholder `it('should be defined')` tests to satisfy Wave 0 test scaffolding needs.
  </action>
  <acceptance_criteria>
    - `npx nx test api --testFile=players.controller` exits 0.
  </acceptance_criteria>
</task>
```
