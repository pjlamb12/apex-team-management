---
wave: 2
depends_on: ["04-02-PLAN.md"]
files_modified:
  - "apps/frontend/src/app/teams/players.service.ts"
  - "apps/frontend/src/app/app.routes.ts"
  - "apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts"
  - "apps/frontend/src/app/teams/team-dashboard/team-dashboard.html"
  - "apps/frontend/src/app/teams/team-dashboard/team-dashboard.scss"
autonomous: true
---

# Wave 2: UI Team Dashboard & Roster Service

Implement the frontend service for Player fetching and convert the `/teams/:id` route into the Team Dashboard displaying the roster list.

## Requirements Covered
- **ROST-04**: Coach can view the full roster at a glance
- **ROST-03**: Coach can remove a player from the roster

## Tasks

```xml
<task>
  <id>04-03-01</id>
  <read_first>
    - apps/frontend/src/app/teams/players.service.ts
  </read_first>
  <action>
    Create `PlayersService` in `apps/frontend/src/app/teams/players.service.ts` using Angular's `HttpClient`. Keep it standalone-compatible.
    Implement methods `getPlayers(teamId)`, `addPlayer(teamId, data)`, `updatePlayer(teamId, playerId, data)`, `deletePlayer(teamId, playerId)`. Type the responses accurately based on `PlayerEntity`.
  </action>
  <acceptance_criteria>
    - `grep -q "getPlayers" apps/frontend/src/app/teams/players.service.ts`
  </acceptance_criteria>
</task>

<task>
  <id>04-03-02</id>
  <read_first>
    - apps/frontend/src/app/app.routes.ts
    - apps/frontend/src/app/teams/edit-team/edit-team.ts
  </read_first>
  <action>
    Create `apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts` (along with `.html` and `.scss`) as a standalone component similar in structure to `edit-team.ts`. 
    Update `app.routes.ts`: Change the `/teams/:id` route to load `TeamDashboard`. Move the `EditTeam` component route to `/teams/:id/settings`.
  </action>
  <acceptance_criteria>
    - `grep -q "team-dashboard" apps/frontend/src/app/app.routes.ts`
    - `grep -q "settings" apps/frontend/src/app/app.routes.ts`
  </acceptance_criteria>
</task>

<task>
  <id>04-03-03</id>
  <read_first>
    - apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts
    - apps/frontend/src/app/teams/team-dashboard/team-dashboard.html
  </read_first>
  <action>
    Implement `TeamDashboard` logic. Fetch the `team` details and the `players` list on `ngOnInit()` using Signals.
    In the HTML template, use an `IonHeader` with the Team Name and a settings cog icon linking to `/teams/:id/settings`.
    Use an `IonContent` block containing an `IonList`. Iterate over the fetched players using an `IonItem` for each, displaying their jersey number (in an `IonBadge`), full name, and email. Add an "Edit/Delete" button context menu or an `IonItemSliding` to support deleting per ROST-03.
  </action>
  <acceptance_criteria>
    - `grep -q "IonItemSliding" apps/frontend/src/app/teams/team-dashboard/team-dashboard.html` OR `grep -q "IonIcon" apps/frontend/src/app/teams/team-dashboard/team-dashboard.html` showing delete options.
  </acceptance_criteria>
</task>
```
