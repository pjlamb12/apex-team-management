---
wave: 1
depends_on: ["04-01-PLAN.md"]
files_modified:
  - "apps/api/src/players/players.controller.ts"
  - "apps/api/src/players/players.service.ts"
  - "apps/api/src/players/dto/create-player.dto.ts"
  - "apps/api/src/players/dto/update-player.dto.ts"
autonomous: true
---

# Wave 1: API Backend (PlayersModule)

Implement the CRUD actions for Roster Management per the CONTEXT.md decisions. Endpoints map to `/teams/:teamId/players`.

## Requirements Covered
- **ROST-01**: Coach can add a player with name, jersey number, and contact email
- **ROST-02**: Coach can edit player details
- **ROST-03**: Coach can remove a player from the roster
- **ROST-04**: Coach can view the full roster at a glance

## Tasks

```xml
<task>
  <id>04-02-01</id>
  <read_first>
    - apps/api/src/entities/player.entity.ts
  </read_first>
  <action>
    Create input DTOs in `apps/api/src/players/dto/`:
    `create-player.dto.ts` with fields `firstName` (string, required), `lastName` (string, required), `jerseyNumber` (number, required), `parentEmail` (email, required). Use `@IsEmail()`, `@IsNotEmpty()`, and `@IsNumber()`.
    `update-player.dto.ts` mapping partial fields (making them all optional for patching).
  </action>
  <acceptance_criteria>
    - `grep -q "parentEmail" apps/api/src/players/dto/create-player.dto.ts`
  </acceptance_criteria>
</task>

<task>
  <id>04-02-02</id>
  <read_first>
    - apps/api/src/players/players.service.ts
    - apps/api/src/entities/player.entity.ts
  </read_first>
  <action>
    Implement `PlayersService` with methods:
    `findAllForTeam(teamId: string)` - returns `PlayerEntity[]` ordered by jersey number or name.
    `create(teamId: string, data: CreatePlayerDto)` - instantiates and saves a player linking `teamId`.
    `update(teamId: string, playerId: string, data: UpdatePlayerDto)` - patches existing player.
    `remove(teamId: string, playerId: string)` - deletes the player.
  </action>
  <acceptance_criteria>
    - `grep -q "findAllForTeam" apps/api/src/players/players.service.ts`
    - `npx nx lint api` exits 0.
  </acceptance_criteria>
</task>

<task>
  <id>04-02-03</id>
  <read_first>
    - apps/api/src/players/players.controller.ts
    - apps/api/src/players/players.service.ts
  </read_first>
  <action>
    Implement `PlayersController` decorated with `@Controller('teams/:teamId/players')`.
    Use `UseGuards(JwtAuthGuard)` to protect the routes.
    Map `GET /` -> `findAllForTeam(@Param('teamId') teamId)`
    Map `POST /` -> `create(@Param('teamId') teamId, @Body() data)`
    Map `PATCH /:id` -> `update(...)`
    Map `DELETE /:id` -> `remove(...)`
  </action>
  <acceptance_criteria>
    - `grep -q "@Controller('teams/:teamId/players')" apps/api/src/players/players.controller.ts`
    - `npx nx run api:build` exits 0 (verifying no DI/syntax errors).
  </acceptance_criteria>
</task>
```
