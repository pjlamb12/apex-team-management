---
phase: 10-season-lifecycle-defaults
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/entities/season.entity.ts
  - apps/api/src/teams/dto/create-season.dto.ts
  - apps/api/src/teams/dto/update-season.dto.ts
  - libs/shared/util/models/src/lib/season.model.ts
autonomous: true
requirements:
  - SEAS-07
must_haves:
  truths:
    - Season model includes defaultPracticeLocation field in both API and shared lib.
    - Database schema has default_practice_location column in seasons table.
  artifacts:
    - path: apps/api/src/entities/season.entity.ts
      provides: Entity update
    - path: libs/shared/util/models/src/lib/season.model.ts
      provides: Shared interface update
---

<objective>
Update the Season model across the stack to include the `defaultPracticeLocation` field and generate the necessary database migration.

Purpose: Support Phase 11 practice management by providing a default location for new practices.
Output: Updated entity, DTOs, shared interface, and migration file.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-season-lifecycle-defaults/10-CONTEXT.md
@.planning/phases/10-season-lifecycle-defaults/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Update Season models and DTOs</name>
  <files>
    - apps/api/src/entities/season.entity.ts
    - apps/api/src/teams/dto/create-season.dto.ts
    - apps/api/src/teams/dto/update-season.dto.ts
    - libs/shared/util/models/src/lib/season.model.ts
  </files>
  <action>
    - Add `defaultPracticeLocation` (string, nullable) to `SeasonEntity` (column name: `default_practice_location`).
    - Add `defaultPracticeLocation` (optional string) to `CreateSeasonDto` and `UpdateSeasonDto`.
    - Add `defaultPracticeLocation` (optional string) to `Season` interface in shared lib.
  </action>
  <verify>
    - `npx nx lint api` passes
    - `npx nx build api` passes
  </verify>
  <done>Models and DTOs updated with the new field.</done>
</task>

<task type="auto">
  <name>Generate and run migration</name>
  <files>apps/api/src/migrations/*-AddPracticeLocationToSeason.ts</files>
  <action>
    - Generate a TypeORM migration to add the `default_practice_location` column to the `seasons` table.
    - Run the migration.
  </action>
  <verify>
    - Migration file exists in `apps/api/src/migrations/`.
    - Check database schema for the new column (e.g. `\d seasons` in psql).
  </verify>
  <done>Database schema updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | DTO validation ensures the new field is a string of reasonable length. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `SeasonEntity` | mitigate | Ensure the new field is included in serialization for the client. |
</threat_model>

<verification>
Verify that the `defaultPracticeLocation` field is available in the API response for seasons and persisted in the database.
</verification>

<success_criteria>
- Backend and Shared models are in sync regarding `defaultPracticeLocation`.
- Database schema matches the entity definition.
</success_criteria>

<output>
After completion, create `.planning/phases/10-season-lifecycle-defaults/10-01-SUMMARY.md`
</output>
