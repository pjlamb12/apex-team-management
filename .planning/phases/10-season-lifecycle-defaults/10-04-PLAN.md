---
phase: 10-season-lifecycle-defaults
plan: 04
type: execute
wave: 4
depends_on:
  - "10-03"
files_modified:
  - apps/frontend/src/app/teams/seasons/season-detail/season-detail.ts
  - apps/frontend/src/app/app.routes.ts
autonomous: true
requirements:
  - SEAS-03
  - SEAS-04
  - SEAS-05
  - SEAS-07
must_haves:
  truths:
    - Coach can create and edit seasons.
    - Default Practice Location is editable.
    - Active toggle is functional.
  artifacts:
    - path: apps/frontend/src/app/teams/seasons/season-detail/season-detail.ts
      provides: View detail
---

<objective>
Implement the season detail view for creating and editing individual seasons, featuring a reactive form and Signal-based state management.

Purpose: Provide full CRUD capabilities for seasons.
Output: Detail component and form.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-season-lifecycle-defaults/10-CONTEXT.md
@.planning/phases/10-season-lifecycle-defaults/10-RESEARCH.md
@.planning/phases/10-season-lifecycle-defaults/10-VALIDATION.md
@apps/frontend/src/app/teams/seasons/seasons.service.ts
</context>

<tasks>

<task type="auto">
  <name>Create SeasonDetail component</name>
  <files>apps/frontend/src/app/teams/seasons/season-detail/season-detail.ts</files>
  <action>
    - Create a standalone Ionic/Angular component for the detail view.
    - Determine if it's "Create" or "Edit" based on the `seasonId` route parameter.
    - Use `ReactiveFormsModule` to build a form for:
      - Name (required).
      - Start Date and End Date.
      - `isActive` (toggle).
      - `defaultPracticeLocation`.
    - Implement `submit()` logic using `SeasonsService`.
    - Use Signals for state management (`isLoading`, `isSaving`, `errorMessage`).
    - After success, navigate back to the Seasons list.
  </action>
  <verify>
    <automated>npx nx build frontend</automated>
  </verify>
  <done>Detail component implemented.</done>
</task>

<task type="auto">
  <name>Register detail routes</name>
  <files>apps/frontend/src/app/app.routes.ts</files>
  <action>
    - Add the season detail route to `app.routes.ts` under `/teams/:id/settings/seasons/:seasonId`.
    - Ensure it lazy loads or direct references `SeasonDetail`.
  </action>
  <verify>
    <automated>npx nx build frontend</automated>
  </verify>
  <done>Detail routes registered.</done>
</task>

<task type="auto">
  <name>Implement active toggle behavior</name>
  <files>apps/frontend/src/app/teams/seasons/season-detail/season-detail.ts</files>
  <action>
    - Ensure the `isActive` toggle is bound to the form.
    - Since the backend handles auto-deactivation of others, the UI just needs to send the `isActive: true` value.
  </action>
  <verify>
    <automated>npx nx build frontend</automated>
  </verify>
  <done>Active toggle integrated.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Detail form input | mitigate | Validate inputs using `ReactiveFormsModule` on the frontend and `class-validator` on the backend. |
</threat_model>

<verification>
Manually verify that creating a new season and editing an existing one (including setting it as active) works as expected.
</verification>

<success_criteria>
- Seasons can be created and edited.
- Active toggle correctly updates backend state and auto-deactivates other seasons.
</success_criteria>

<output>
After completion, create `.planning/phases/10-season-lifecycle-defaults/10-04-SUMMARY.md`
</output>
