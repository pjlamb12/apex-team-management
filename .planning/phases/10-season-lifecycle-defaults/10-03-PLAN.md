---
phase: 10-season-lifecycle-defaults
plan: 03
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - apps/frontend/src/app/teams/seasons/seasons.service.ts
  - apps/frontend/src/app/teams/seasons/seasons-list/seasons-list.ts
  - apps/frontend/src/app/teams/edit-team/edit-team.html
  - apps/frontend/src/app/teams/edit-team/edit-team.ts
  - apps/frontend/src/app/app.routes.ts
autonomous: true
requirements:
  - SEAS-03
  - SEAS-06
must_haves:
  truths:
    - Coach can navigate to the Seasons list from Team Settings.
    - Seasons list displays all seasons for the team with an active indicator.
    - Delete button in the list handles success and error cases.
  artifacts:
    - path: apps/frontend/src/app/teams/seasons/seasons.service.ts
      provides: API integration
    - path: apps/frontend/src/app/teams/seasons/seasons-list/seasons-list.ts
      provides: View list
---

<objective>
Scaffold the frontend season management service and list view, and integrate it into the existing team settings navigation.

Purpose: Provide the entry point for season management.
Output: Frontend service, list component, and updated navigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-season-lifecycle-defaults/10-CONTEXT.md
@.planning/phases/10-season-lifecycle-defaults/10-RESEARCH.md
@apps/frontend/src/app/teams/edit-team/edit-team.ts
</context>

<tasks>

<task type="auto">
  <name>Create frontend SeasonsService</name>
  <files>apps/frontend/src/app/teams/seasons/seasons.service.ts</files>
  <action>
    - Create a standard Angular service using `HttpClient` and `inject()`.
    - Implement methods: `findAllForTeam(teamId)`, `findOne(id)`, `create(teamId, data)`, `update(id, data)`, `remove(id)`.
    - Use the `Season` interface from `@apex-team/shared/util/models`.
  </action>
  <verify>
    Service file exists and defines all CRUD methods.
  </verify>
  <done>Service implemented.</done>
</task>

<task type="auto">
  <name>Create SeasonsList component</name>
  <files>apps/frontend/src/app/teams/seasons/seasons-list/seasons-list.ts</files>
  <action>
    - Create a standalone Ionic/Angular component.
    - Fetch seasons for the current team using `SeasonsService`.
    - Display seasons in an `ion-list` with:
      - Season name and dates.
      - "Active" badge if `isActive` is true.
      - Delete button for each season.
    - Use a FAB (Floating Action Button) to navigate to the "Create Season" view.
    - Implement delete logic with confirmation and error handling (for blocked deletes).
  </action>
  <verify>
    Component compiles and displays list mock data or real data if API is available.
  </verify>
  <done>List component implemented.</done>
</task>

<task type="auto">
  <name>Integrate navigation and routes</name>
  <files>
    - apps/frontend/src/app/teams/edit-team/edit-team.html
    - apps/frontend/src/app/app.routes.ts
  </files>
  <action>
    - Add routes to `app.routes.ts` under `/teams/:id/settings/seasons` (list) and `/teams/:id/settings/seasons/:seasonId` (detail - next plan).
    - In `edit-team.html`, add an `ion-item` button to the "Management" list that links to the seasons list.
  </action>
  <verify>
    - Navigation to `/teams/:id/settings/seasons` works.
    - The new button appears in Team Settings.
  </verify>
  <done>Navigation integrated.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | Frontend API | mitigate | Backend already enforces team ownership; frontend uses proper route parameters. |
</threat_model>

<verification>
Manually verify that the "Manage Seasons" link appears in Team Settings and leads to the seasons list.
</verification>

<success_criteria>
- Seamless navigation from settings to season management.
- List view correctly displays team seasons and active status.
</success_criteria>

<output>
After completion, create `.planning/phases/10-season-lifecycle-defaults/10-03-SUMMARY.md`
</output>
