---
phase: 05-games-lineup
plan: 05
type: execute
wave: 3
depends_on:
  - "05-04"
files_modified:
  - apps/frontend/src/app/teams/games/games.service.ts
  - apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts
  - apps/frontend/src/app/teams/team-dashboard/team-dashboard.html
autonomous: true
requirements:
  - GAME-02
  - GAME-04

must_haves:
  truths:
    - "GamesService (Angular) implements all API methods using inject(HttpClient)"
    - "Team Dashboard includes IonSegment for Roster/Games toggle"
    - "Games list in Team Dashboard correctly groups upcoming and past games"
    - "Delete game uses AlertController confirmation"
    - "TypeScript compilation for frontend is clean"
  artifacts:
    - path: "apps/frontend/src/app/teams/games/games.service.ts"
      provides: "Angular service for games API communication"
      contains: "inject(HttpClient)"
    - path: "apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts"
      provides: "Updated Team Dashboard with games loading and segment logic"
      contains: "selectedSegment = signal"
    - path: "apps/frontend/src/app/teams/team-dashboard/team-dashboard.html"
      provides: "Updated template with IonSegment and Games list"
      contains: "<ion-segment"
  key_links:
    - from: "apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts"
      to: "apps/frontend/src/app/teams/games/games.service.ts"
      via: "dependency injection"
      pattern: "inject(GamesService)"
---

<objective>
Implement the Angular service for games and update the Team Dashboard to include the "Games" segment. This includes the `GamesService` (following the `PlayersService` pattern), the `IonSegment` tab strip in `TeamDashboard`, and the games list view with upcoming/past grouping and swipe-to-delete.

Purpose: Enable the coach to switch between Roster and Games views within a team and manage the list of games.

Output: 1 new Angular service, modified TeamDashboard component and template.
</objective>

<execution_context>
@/Users/pjlamb12/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pjlamb12/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-games-lineup/05-CONTEXT.md
@.planning/phases/05-games-lineup/05-RESEARCH.md
@.planning/phases/05-games-lineup/05-PATTERNS.md
@.planning/phases/05-games-lineup/05-UI-SPEC.md

<interfaces>
<!-- Pattern from PlayersService (Angular) [VERIFIED] -->
```typescript
@Injectable({ providedIn: 'root' })
export class PlayersService {
  private readonly http = inject(HttpClient);
  private readonly config = inject(RuntimeConfigLoaderService);
  // ...
}
```

<!-- Pattern from TeamDashboard [VERIFIED] -->
```typescript
@Component({
  standalone: true,
  imports: [CommonModule, IonicModule, ...],
  templateUrl: './team-dashboard.html',
  styleUrl: './team-dashboard.scss'
})
export class TeamDashboard implements OnInit {
  protected players = signal<Player[]>([]);
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Angular GamesService</name>
  <files>apps/frontend/src/app/teams/games/games.service.ts</files>
  <read_first>
    - apps/frontend/src/app/teams/players.service.ts (pattern to replicate)
  </read_first>
  <action>
Create `apps/frontend/src/app/teams/games/games.service.ts`.
Implement all 6 API calls defined in Pattern 2 of `RESEARCH.md`:
- `getGames(teamId: string)`
- `createGame(teamId: string, data: CreateGameDto)`
- `updateGame(gameId: string, data: UpdateGameDto)`
- `deleteGame(gameId: string)`
- `getLineup(gameId: string)`
- `saveLineup(gameId: string, entries: SaveLineupDto)`

Use `inject(HttpClient)` and `inject(RuntimeConfigLoaderService)`. Follow the existing `apiBaseUrl` derivation pattern.
  </action>
  <verify>
    <automated>npx tsc --project apps/frontend/tsconfig.app.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/app/teams/games/games.service.ts` exists and exports `GamesService`
    - Service implements all 6 API methods
    - Service uses `inject(HttpClient)` and `inject(RuntimeConfigLoaderService)`
    - Service correctly derives `apiUrl` from config
    - `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Angular GamesService implemented and ready for use in components. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update Team Dashboard with Games segment and list</name>
  <files>
    apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts
    apps/frontend/src/app/teams/team-dashboard/team-dashboard.html
  </files>
  <read_first>
    - apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts (current state)
    - apps/frontend/src/app/teams/team-dashboard/team-dashboard.html (current state)
  </read_first>
  <action>
**Step A — Update `team-dashboard.ts`:**
- Import `GamesService`.
- Add `selectedSegment = signal<'roster' | 'games'>('roster')`.
- Add `games = signal<Game[]>([])` and related loading/error signals.
- Implement `onSegmentChange(event: CustomEvent)` to toggle view and lazily load games.
- Add `upcomingGames` and `pastGames` computed signals (following Pattern grouping).
- Implement `onDeleteGame(game: Game)` using `AlertController`.

**Step B — Update `team-dashboard.html`:**
- Wrap existing roster content in `@if (selectedSegment() === 'roster')`.
- Add `<ion-segment>` with two buttons: Roster and Games.
- Add Games content in `@if (selectedSegment() === 'games')`.
- Games content includes an `<ion-list>` with two groups: "Upcoming" and "Past".
- Use `<ion-item-sliding>` for games list to allow swipe-to-delete.
- Add an `<ion-fab>` or header button to navigate to `/teams/:id/games/new`.
  </action>
  <verify>
    <automated>npx tsc --project apps/frontend/tsconfig.app.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `team-dashboard.ts` imports and injects `GamesService`
    - `team-dashboard.ts` has `selectedSegment` signal and `onSegmentChange` handler
    - `team-dashboard.ts` has `upcomingGames` and `pastGames` computed signals
    - `team-dashboard.html` has `<ion-segment>` bound to the signal
    - `team-dashboard.html` displays the grouped games list when segment is 'games'
    - `team-dashboard.html` includes a "Create Game" action
    - `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Team Dashboard updated with dual-view segment. Games list implemented with grouping and deletion. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI Component | Input events (segment change, delete button) trigger application logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-05-01 | Tampering | Segment State | accept | UI-only state; no security impact if tampered |
| T-5-05-02 | Tampering | deleteGame call | mitigate | AlertController confirmation prevents accidental deletion |
</threat_model>

<verification>
Frontend updates verified when:
1. `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` — TypeScript clean
2. Manual verification: Tab switch between Roster/Games works; Games list renders correctly grouped (requires mock or backend data)
</verification>

<success_criteria>
- Angular GamesService implemented for all 6 API endpoints
- Team Dashboard has IonSegment for view switching
- Games list renders grouped upcoming/past games with swipe-to-delete
- Delete game action requires coach confirmation
- Navigation to "New Game" route is wired
</success_criteria>

<output>
After completion, create `.planning/phases/05-games-lineup/05-05-SUMMARY.md`
</output>
