---
phase: 05-games-lineup
plan: 07
type: execute
wave: 4
depends_on:
  - "05-06"
files_modified:
  - apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.ts
  - apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.html
  - apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.scss
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02

must_haves:
  truths:
    - "Lineup Editor displays 11 positional slots"
    - "Each slot has a position-type picker and a player picker"
    - "Player picker excludes players already assigned to other slots"
    - "Bench section correctly displays players not assigned to field positions"
    - "Save button correctly calls GamesService.saveLineup with all entries"
  artifacts:
    - path: "apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.ts"
      provides: "Lineup assignment logic and player filtering"
      contains: "assignedPlayerIds = computed"
    - path: "apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.html"
      provides: "Lineup editor UI with positional slots and bench section"
      contains: "<ion-select"
  key_links:
    - from: "apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.ts"
      to: "apps/frontend/src/app/teams/games/games.service.ts"
      via: "API call"
      pattern: "gamesService.saveLineup"
---

<objective>
Implement the Lineup Editor in Angular. This is the most complex UI component of Phase 5, involving 11 slots with paired pickers, reactive player exclusion logic, and a derived bench display.

Purpose: Allow the coach to set the starting lineup and bench for a specific game.

Output: 1 new Angular page component (TS/HTML/SCSS).
</objective>

<execution_context>
@/Users/pjlamb12/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pjlamb12/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-games-lineup/05-CONTEXT.md
@.planning/phases/05-games-lineup/05-RESEARCH.md
@.planning/phases/05-games-lineup/05-PATTERNS.md
@.planning/phases/05-games-lineup/05-UI-SPEC.md

<interfaces>
<!-- Data shape for a slot -->
```typescript
interface LineupSlot {
  slotIndex: number;
  positionName: string | null;
  playerId: string | null;
}
```

<!-- Position types from sport configuration (soccer) -->
<!-- Derived from SportEntity/PositionTypeEntity seeds — use standard soccer labels: GK, DEF, MID, FWD -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Lineup Editor component and logic</name>
  <files>
    apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.ts
    apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.html
    apps/frontend/src/app/teams/games/lineup-editor/lineup-editor.scss
  </files>
  <read_first>
    - apps/frontend/src/app/teams/team-dashboard/team-dashboard.ts (signal patterns)
  </read_first>
  <action>
Create the folder `apps/frontend/src/app/teams/games/lineup-editor/`.

**Step A — Implement `LineupEditor.ts`:**
- Inject `GamesService`, `PlayersService`, `ActivatedRoute`, and `Router`.
- `slots = signal<LineupSlot[]>(...)` — initialize with 11 empty slots.
- `players = signal<Player[]>([])` — load roster for the team.
- `assignedPlayerIds = computed(() => ...)` — reactive set of IDs already in slots.
- `benchPlayers = computed(() => ...)` — players NOT in `assignedPlayerIds`.
- `onSave()`: Map slots and bench players to `SaveLineupDto` and call `saveLineup`.
- `availablePlayers(currentSlotIndex)`: Helper to return players available for a specific slot (roster minus IDs in OTHER slots).

**Step B — Implement `LineupEditor.html`:**
- Display 11 rows. Each row has:
  - `IonSelect` for position name (GK, DEF, MID, FWD).
  - `IonSelect` for player (options filtered via `availablePlayers(i)`).
- Add a "Bench" section below the 11 slots using `@for` over `benchPlayers()`.
- Header has a "Save" button.

**Step C — Implement `LineupEditor.scss`:**
- Add styling for the positional slots grid/list to ensure it looks "Athletic Professional".
- Ensure pickers have enough tap target area for mobile use.
  </action>
  <verify>
    <automated>npx tsc --project apps/frontend/tsconfig.app.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `LineupEditor` component exists and implements all reactive signals
    - `assignedPlayerIds` and `benchPlayers` are implemented as `computed()` signals
    - Player pickers correctly filter out already-assigned players (no duplicates)
    - Save action correctly bundles field + bench players and sends to API
    - Template handles 11 positional slots and a dynamic bench section
    - `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Lineup Editor fully implemented with complex reactive logic. Bench and starters correctly synced. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Lineup State | Reactive computation ensures UI-level consistency (no duplicates) before submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-07-01 | Tampering | Lineup Payload | accept | Backend (Plan 04) validates player ownership and array size; UI-level filtering is for UX |
</threat_model>

<verification>
Lineup Editor is verified when:
1. `npx tsc --project apps/frontend/tsconfig.app.json --noEmit` — TypeScript clean
2. Manual verification: Assign Player A to Slot 1, verify Player A is removed from Slot 2 options; verify Save correctly persists state
</verification>

<success_criteria>
- 11 positional slots displayed with paired pickers
- Reactive filtering prevents duplicate player assignments across slots
- Bench section automatically updates as players are assigned/unassigned
- Save functionality persists both starters and bench players to the database
- Professional mobile-friendly layout for lineup assignment
</success_criteria>

<output>
After completion, create `.planning/phases/05-games-lineup/05-07-SUMMARY.md`
</output>
