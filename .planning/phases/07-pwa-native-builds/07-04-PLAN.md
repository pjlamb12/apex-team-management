# Plan 07-04: Phase Verification & Build Artifacts

**Phase:** 07-pwa-native-builds
**Plan:** 04
**Goal:** Verify all Phase 7 success criteria and ensure the project is ready for native build execution.

## Requirements
- **SUCCESS-CR-1/2:** Valid PWA manifest and installable state.
- **SUCCESS-CR-3/4:** Capacitor project structure ready for Android/iOS builds.

## Context
- Plans 07-01, 07-02, 07-03 complete.
- Build artifacts ready in `dist/apps/frontend/browser`.

## Tasks
1. **Full Production Build:** `npx nx build frontend --configuration=production`.
2. **Capacitor Sync:** `npx nx cap frontend` (which runs `npx cap sync`).
3. **PWA Validation:** Verify `manifest.webmanifest`, `ngsw.json` are present in build output.
4. **Native Project Validation:** Verify `android/` and `ios/` folders are updated.
5. **Phase Sign-off:** Run `/gsd:verify-work phase 7` and update `STATE.md`.

## Implementation Strategy
- Final build and sync pass.
- Manual file existence checks for key artifacts.

## Verification Plan
- Build passes.
- Sync passes.
- All key files exist in their target directories.
