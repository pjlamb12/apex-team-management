---
phase: 20-team-join-codes-assistant-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - libs/client/data-access/team/src/lib/team.service.ts
  - libs/client/data-access/team/src/index.ts
  - apps/frontend/src/app/teams/teams-list/teams-list.ts
  - apps/frontend/src/app/teams/teams-list/teams-list.html
  - apps/frontend/src/app/teams/join-team/join-team-modal.ts
  - apps/frontend/src/app/teams/edit-team/edit-team.ts
  - apps/frontend/src/app/teams/edit-team/edit-team.html
  - apps/frontend-e2e/src/team-join-flow.spec.ts
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "User can click 'Join Team' on the dashboard and enter a 6-char code"
    - "Join code is visible in Team Settings for Head Coaches"
    - "Head Coach can regenerate the join code from settings"
    - "Delete Team button is hidden for Assistant Coaches"
  artifacts:
    - path: "apps/frontend/src/app/teams/join-team/join-team-modal.ts"
      provides: "User interface for joining teams"
    - path: "libs/client/data-access/team/src/lib/team.service.ts"
      provides: "Unified team API client"
  key_links:
    - from: "apps/frontend/src/app/teams/teams-list/teams-list.ts"
      to: "apps/frontend/src/app/teams/join-team/join-team-modal.ts"
      via: "IonModalController"
---

<objective>
Implement the frontend user experience for joining teams and managing join codes. Ensure the UI respects Role-Based Access Control by hiding administrative actions from Assistant Coaches.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-team-join-codes-assistant-onboarding/20-02-SUMMARY.md
@apps/frontend/src/app/teams/teams-list/teams-list.ts
@apps/frontend/src/app/teams/edit-team/edit-team.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Frontend Team Service and Dashboard Action</name>
  <files>libs/client/data-access/team/src/lib/team.service.ts, libs/client/data-access/team/src/index.ts, apps/frontend/src/app/teams/teams-list/teams-list.ts, apps/frontend/src/app/teams/teams-list/teams-list.html</files>
  <action>
    - Create `libs/client/data-access/team/src/lib/team.service.ts`:
      - Methods: `getTeams()`, `joinTeam(code)`, `regenerateCode(teamId)`.
      - Use `HttpClient` and follow project patterns (Signals/inject).
    - Export from `libs/client/data-access/team/src/index.ts`.
    - Update `TeamsList` component:
      - Replace direct `HttpClient` usage with the new `TeamService`.
      - Add a "Join Team" button to the header or dashboard.
  </action>
  <verify>
    - Check that `TeamsList` correctly displays teams using the new service.
  </verify>
  <done>Frontend data access is centralized and the Join Team action is accessible.</done>
</task>

<task type="auto">
  <name>Create Join Team Modal and Flow</name>
  <files>apps/frontend/src/app/teams/join-team/join-team-modal.ts, apps/frontend/src/app/teams/teams-list/teams-list.ts</files>
  <action>
    - Create `apps/frontend/src/app/teams/join-team/join-team-modal.ts` (standalone component):
      - Simple form with 1 input for the 6-character code.
      - Uppercase the input automatically.
      - Call `teamService.joinTeam(code)` on submit.
      - Close modal and refresh teams list on success.
    - Integrate modal trigger in `TeamsList`.
  </action>
  <verify>
    - Verify modal appears and input is constrained to alphanumeric/6 chars.
  </verify>
  <done>Users can join teams via the alphanumeric code modal.</done>
</task>

<task type="auto">
  <name>Update Team Settings and Implement RBAC UI</name>
  <files>apps/frontend/src/app/teams/edit-team/edit-team.ts, apps/frontend/src/app/teams/edit-team/edit-team.html</files>
  <action>
    - Update `EditTeam` component:
      - Add a "Join Code" section showing the current code.
      - Add a "Regenerate" button next to the code (requires confirmation).
      - Conditional Visibility: Wrap "Delete Team" button and "Regenerate Code" button in checks for `team.role === 'HEAD_COACH'`.
    - Ensure `Team` model in frontend includes `role` (may need to update shared model or component-local state).
  </action>
  <verify>
    - Log in as an assistant and verify "Delete Team" and "Regenerate" are NOT visible.
    - Log in as head coach and verify they ARE visible and functional.
  </verify>
  <done>Team management UI is secure and functional for different roles.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify Full Join Flow E2E</name>
  <what-built>The complete Join Code flow from generation to usage and RBAC enforcement.</what-built>
  <how-to-verify>
    1. Create a team as Coach A.
    2. Go to Team Settings and copy the Join Code.
    3. Log out and log in as Coach B.
    4. Click "Join Team" and enter the code.
    5. Verify Coach B sees the team and can manage games.
    6. Verify Coach B CANNOT delete the team or regenerate the code.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | UI must not show destructive actions to unauthorized users, even if API also checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Information Disclosure | Team Settings | mitigate | Only show join code to `HEAD_COACH` if it's considered sensitive, or allow all members to see it for sharing. (D-04 says assistants have full access except deletion, so they can likely see the code). |
</threat_model>

<verification>
`npx playwright test apps/frontend-e2e/src/team-join-flow.spec.ts` (once implemented).
Manual verification of the flow in the browser.
</verification>

<success_criteria>
- Users can join teams via code.
- Head coaches can manage codes.
- Assistants are restricted from team deletion.
</success_criteria>

<output>
After completion, create `.planning/phases/20-team-join-codes-assistant-onboarding/20-03-SUMMARY.md`
</output>
