---
phase: 20-team-join-codes-assistant-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - apps/api/src/memberships/membership.service.ts
  - apps/api/src/memberships/memberships.module.ts
  - apps/api/src/auth/guards/team-role.guard.ts
  - apps/api/src/auth/decorators/team-role.decorator.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "MembershipService can verify if a user belongs to a team with a specific role"
    - "TeamRoleGuard can protect endpoints based on team membership and role"
  artifacts:
    - path: "apps/api/src/memberships/membership.service.ts"
      provides: "Shared membership check logic"
    - path: "apps/api/src/auth/guards/team-role.guard.ts"
      provides: "RBAC enforcement for team-scoped resources"
---

<objective>
Implement the shared membership logic and authorization guards. This provides a central service for checking user permissions relative to a team and a guard to protect endpoints from unauthorized access by non-members or incorrect roles.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-team-join-codes-assistant-onboarding/20-01-SUMMARY.md
@apps/api/src/entities/team-member.entity.ts
@apps/api/src/auth/guards/roles.guard.ts (if exists, as reference)
</context>

<tasks>

<task type="auto">
  <name>Implement Membership Service</name>
  <files>apps/api/src/memberships/membership.service.ts, apps/api/src/memberships/memberships.module.ts, apps/api/src/app.module.ts</files>
  <action>
    - Create `apps/api/src/memberships/membership.service.ts`:
      - `isMember(userId, teamId): Promise<boolean>`
      - `getRole(userId, teamId): Promise<TeamRole | null>`
      - `hasRole(userId, teamId, roles: TeamRole[]): Promise<boolean>`
    - Create `apps/api/src/memberships/memberships.module.ts` exporting `MembershipService`.
    - Import `MembershipsModule` in `apps/api/src/app.module.ts`.
  </action>
  <verify>
    - Unit test `MembershipService` with mocked repository.
  </verify>
  <done>Membership verification logic is centralized and available globally in the API.</done>
</task>

<task type="auto">
  <name>Implement Team Role Guard</name>
  <files>apps/api/src/auth/guards/team-role.guard.ts, apps/api/src/auth/decorators/team-role.decorator.ts</files>
  <action>
    - Create `apps/api/src/auth/decorators/team-role.decorator.ts`:
      - `@TeamRole(...roles: TeamRole[])`
    - Create `apps/api/src/auth/guards/team-role.guard.ts`:
      - Extract `teamId` from request params.
      - Extract `userId` from request user.
      - Use `MembershipService` to verify the user has one of the required roles for that team.
      - Throw `ForbiddenException` if check fails.
  </action>
  <verify>
    - Mock a request and verify the guard returns true/false correctly based on MembershipService output.
  </verify>
  <done>Authorization guard for team-specific roles is implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller -> Service | Authorization logic is now extracted into a reusable guard and service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Elevation of Privilege | Endpoints | mitigate | Enforce `TeamRoleGuard` on sensitive actions. |
</threat_model>

<verification>
`npx vitest apps/api/src/memberships` passes.
</verification>

<success_criteria>
- `MembershipService` correctly identifies user roles in teams.
- `TeamRoleGuard` blocks access when user role doesn't match decorator.
</success_criteria>

<output>
After completion, create `.planning/phases/20-team-join-codes-assistant-onboarding/20-02-SUMMARY.md`
</output>
