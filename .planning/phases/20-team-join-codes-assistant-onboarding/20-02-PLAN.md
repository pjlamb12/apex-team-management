---
phase: 20-team-join-codes-assistant-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - package.json
  - apps/api/src/teams/teams.join-code.service.ts
  - apps/api/src/teams/teams.service.ts
  - apps/api/src/teams/teams.controller.ts
  - apps/api/src/teams/teams.module.ts
  - apps/api/src/auth/guards/roles.guard.ts
  - apps/api/src/teams/teams.service.spec.ts
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "TeamsService can generate unique 6-character alphanumeric join codes"
    - "Users can join a team using a valid join code"
    - "Only Head Coaches can regenerate join codes or delete teams"
  artifacts:
    - path: "apps/api/src/teams/teams.join-code.service.ts"
      provides: "Secure code generation logic"
    - path: "apps/api/src/auth/guards/roles.guard.ts"
      provides: "RBAC enforcement"
  key_links:
    - from: "apps/api/src/teams/teams.controller.ts"
      to: "apps/api/src/auth/guards/roles.guard.ts"
      via: "@Roles() decorator"
---

<objective>
Implement the core backend logic for generating join codes and handling team joining. Enforce Role-Based Access Control (RBAC) to ensure only Head Coaches can perform administrative actions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-team-join-codes-assistant-onboarding/20-01-SUMMARY.md
@apps/api/src/teams/teams.service.ts
@apps/api/src/teams/teams.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Join Code Generation Service</name>
  <files>package.json, apps/api/src/teams/teams.join-code.service.ts, apps/api/src/teams/teams.module.ts</files>
  <action>
    - Install `nanoid`: `npm install nanoid`.
    - Create `apps/api/src/teams/teams.join-code.service.ts`:
      - Use `customAlphabet` from `nanoid` with alphabet `23456789ABCDEFGHJKLMNPQRSTUVWXYZ` (avoiding ambiguous characters).
      - Method `generate()` returns a 6-character string.
    - Register the service in `apps/api/src/teams/teams.module.ts`.
  </action>
  <verify>
    - Run a script or test to generate 100 codes and verify they are all 6 chars and alphanumeric.
  </verify>
  <done>Secure 6-character alphanumeric code generation is available.</done>
</task>

<task type="auto">
  <name>Update Teams Service and Controller for Memberships</name>
  <files>apps/api/src/teams/teams.service.ts, apps/api/src/teams/teams.controller.ts</files>
  <action>
    - Update `TeamsService`:
      - `findAllByCoach` -> `findAllByMember(userId)`: Query `TeamMemberEntity` to find teams the user belongs to.
      - `create`: Update to create a `TeamMemberEntity` with `HEAD_COACH` role for the creator. Also generate an initial `joinCode`.
      - Add `join(userId, code)`: Find team by code, create `ASSISTANT` membership.
      - Add `regenerateCode(teamId)`: Generate new code, update team, ensure uniqueness.
    - Update `TeamsController`:
      - Add `POST /teams/join` with `{ code }`.
      - Add `POST /teams/:id/code/regenerate`.
  </action>
  <verify>
    - Unit test `TeamsService.join` with valid and invalid codes.
    - Verify `findAllByMember` returns teams where user is an assistant.
  </verify>
  <done>Backend supports the full join lifecycle and membership-based queries.</done>
</task>

<task type="auto">
  <name>Implement RBAC Guards and Secure Endpoints</name>
  <files>apps/api/src/auth/guards/roles.guard.ts, apps/api/src/teams/teams.controller.ts, apps/api/src/teams/teams.service.spec.ts</files>
  <action>
    - Create `apps/api/src/auth/guards/roles.guard.ts`:
      - Check if the authenticated user has the required `TeamRole` for the specified team (from params).
    - Create `@Roles()` decorator to metadata.
    - Apply `@Roles(TeamRole.HEAD_COACH)` to:
      - `TeamsController.remove`
      - `TeamsController.regenerateCode`
    - Update `TeamsController.update` to also require `HEAD_COACH` if needed (per D-04 assistants can manage team details, so maybe leave update open).
    - Write unit tests in `apps/api/src/teams/teams.service.spec.ts` for RBAC scenarios.
  </action>
  <verify>
    - Test that an assistant coach receives 403 when trying to delete the team or regenerate the code.
  </verify>
  <done>Administrative actions are restricted to Head Coaches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Join code input must be validated and role checked before execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Elevation of Privilege | TeamsController | mitigate | Enforce `HEAD_COACH` role check via `RolesGuard` for destructive/admin actions. |
| T-20-03 | Spoofing | Join Endpoint | mitigate | Rate limit join attempts (out of scope for this task but noted) and ensure codes are unpredictable (nanoid). |
</threat_model>

<verification>
`npx vitest apps/api/src/teams` passes all tests.
Manual curl tests for join and regen endpoints.
</verification>

<success_criteria>
- Join code generation logic is robust.
- Assistants can join teams but cannot delete them.
- `findAll` returns teams for both roles.
</success_criteria>

<output>
After completion, create `.planning/phases/20-team-join-codes-assistant-onboarding/20-02-SUMMARY.md`
</output>
