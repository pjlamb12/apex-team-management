---
phase: 15-practice-drills-integration
plan: 04
type: execute
wave: 4
depends_on: [15-03]
files_modified:
  - libs/client/data-access/drill/src/lib/practice-drills.service.ts
  - libs/client/data-access/drill/src/index.ts
  - libs/client/feature/practice-console/project.json
  - libs/client/feature/practice-console/src/index.ts
autonomous: true
requirements: [PRAC-03, DRIL-03]
must_haves:
  truths:
    - "A dedicated client-side service exists to interact with the practice drills API."
    - "A new feature library 'practice-console' is created to house practice-specific UI."
  artifacts:
    - path: "libs/client/data-access/drill/src/lib/practice-drills.service.ts"
      provides: "PracticeDrillsService for API interaction"
    - path: "libs/client/feature/practice-console/project.json"
      provides: "Nx library for the practice console feature"
  key_links:
    - from: "libs/client/data-access/drill/src/lib/practice-drills.service.ts"
      to: "/api/teams/:teamId/events/:eventId/drills"
      via: "HttpClient"
---

<objective>
Set up the client-side data access and feature structure for the Practice Console.
Purpose: Provide the foundational service and library structure required for the Practice Console UI.
Output: PracticeDrillsService and practice-console library.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-practice-drills-integration/15-RESEARCH.md
@.planning/phases/15-practice-drills-integration/15-CONTEXT.md
@libs/client/data-access/drill/src/lib/drill.service.ts

<interfaces>
From .planning/phases/15-practice-drills-integration/15-RESEARCH.md:
```typescript
export interface PracticeDrill {
  id: string;
  eventId: string;
  drillId: string;
  sequence: number;
  durationMinutes: number;
  teamRating: number | null;
  notes: string | null;
  drill?: Drill; // Assumed available via relation from API
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PracticeDrillsService</name>
  <files>libs/client/data-access/drill/src/lib/practice-drills.service.ts, libs/client/data-access/drill/src/index.ts</files>
  <action>
    - Create `PracticeDrillsService` in `libs/client/data-access/drill/src/lib/practice-drills.service.ts`.
    - Inject `HttpClient` and `RuntimeConfigLoaderService` (follow `drill.service.ts` pattern).
    - Implement methods (per D-15-CONTEXT):
      - `getPlan(teamId: string, eventId: string)`: `GET /teams/${teamId}/events/${eventId}/drills`
      - `addDrill(teamId: string, eventId: string, drillId: string, durationMinutes: number, notes?: string)`: `POST /teams/${teamId}/events/${eventId}/drills`
      - `updateDrill(teamId: string, eventId: string, practiceDrillId: string, dto: any)`: `PATCH /teams/${teamId}/events/${eventId}/drills/${practiceDrillId}`
      - `removeDrill(teamId: string, eventId: string, practiceDrillId: string)`: `DELETE /teams/${teamId}/events/${eventId}/drills/${practiceDrillId}`
      - `reorderDrills(teamId: string, eventId: string, ids: string[])`: `PUT /teams/${teamId}/events/${eventId}/drills/reorder`
    - Export the service from `libs/client/data-access/drill/src/index.ts`.
  </action>
  <verify>
    <automated>npx nx build client-data-access-drill</automated>
  </verify>
  <done>PracticeDrillsService created and exported. Library builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Generate practice-console library</name>
  <files>libs/client/feature/practice-console/project.json, libs/client/feature/practice-console/src/index.ts</files>
  <action>
    - Generate a new Angular library: `libs/client/feature/practice-console` using Nx.
    - Command: `npx nx g @nx/angular:library client/feature/practice-console --directory=libs/client/feature/practice-console --standalone --changeDetection=OnPush --style=scss --unitTestRunner=vitest --importPath=@apex-team/client/feature/practice-console`.
    - Ensure `project.json` and `src/index.ts` are correctly scaffolded.
  </action>
  <verify>
    <automated>npx nx build client-feature-practice-console</automated>
  </verify>
  <done>Library generated and builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → API | Client expects API to enforce ownership checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Information Disclosure | PracticeDrillsService | mitigate | Ensure no sensitive data is logged in the service. Follow established patterns for API communication. |
</threat_model>

<verification>
Verify that the service is correctly implemented and the library is scaffolded.
</verification>

<success_criteria>
- PracticeDrillsService exists with all required methods.
- `practice-console` library is created in the correct location.
- Both libraries build successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/15-practice-drills-integration/15-04-SUMMARY.md`
</output>
