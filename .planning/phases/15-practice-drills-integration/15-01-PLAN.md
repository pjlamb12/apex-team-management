---
phase: 15-practice-drills-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/entities/practice-drill.entity.ts
  - apps/api/src/entities/event.entity.ts
  - apps/api/src/entities/drill.entity.ts
  - apps/api/src/drills/dto/add-drill-to-plan.dto.ts
  - apps/api/src/drills/dto/update-practice-drill.dto.ts
  - apps/api/src/drills/dto/reorder-practice-drills.dto.ts
autonomous: true
requirements: [PRAC-03, DRIL-03]
must_haves:
  truths:
    - "A junction entity exists to connect drills and practices with metadata (sequence, duration, rating, notes)."
    - "Relations are established in Event and Drill entities to access practice drills."
    - "DTOs are defined with validation rules for adding, updating, and reordering drills."
  artifacts:
    - path: "apps/api/src/entities/practice-drill.entity.ts"
      provides: "PracticeDrillEntity with all required fields and relations"
    - path: "apps/api/src/drills/dto/add-drill-to-plan.dto.ts"
      provides: "Validation for adding a drill"
    - path: "apps/api/src/drills/dto/update-practice-drill.dto.ts"
      provides: "Validation for updating drill metadata"
    - path: "apps/api/src/drills/dto/reorder-practice-drills.dto.ts"
      provides: "Validation for bulk reordering"
  key_links:
    - from: "apps/api/src/entities/practice-drill.entity.ts"
      to: "apps/api/src/entities/event.entity.ts"
      via: "ManyToOne"
    - from: "apps/api/src/entities/practice-drill.entity.ts"
      to: "apps/api/src/entities/drill.entity.ts"
      via: "ManyToOne"
---

<objective>
Define the data model and input validation contracts for practice drill integration.
Purpose: Provide the foundational structure for associating library drills with practice sessions.
Output: PracticeDrill entity and DTOs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-practice-drills-integration/15-RESEARCH.md
@apps/api/src/entities/event.entity.ts
@apps/api/src/entities/drill.entity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PracticeDrillEntity and relations</name>
  <files>apps/api/src/entities/practice-drill.entity.ts, apps/api/src/entities/event.entity.ts, apps/api/src/entities/drill.entity.ts</files>
  <action>
    - Create `PracticeDrillEntity` in `apps/api/src/entities/practice-drill.entity.ts`.
    - Implement fields: `id` (uuid), `eventId` (uuid), `drillId` (uuid), `sequence` (int), `durationMinutes` (int, default 0), `teamRating` (int, nullable), `notes` (text, nullable).
    - Configure `ManyToOne` relations to `EventEntity` and `DrillEntity` with `onDelete: 'CASCADE'`.
    - Update `EventEntity` in `apps/api/src/entities/event.entity.ts` with `OneToMany` to `PracticeDrillEntity` (property: `practiceDrills`).
    - Update `DrillEntity` in `apps/api/src/entities/drill.entity.ts` with `OneToMany` to `PracticeDrillEntity` (property: `practiceDrills`).
    - Generate a database migration to create the `practice_drills` table.
  </action>
  <verify>
    <automated>npx nx build api</automated>
  </verify>
  <done>PracticeDrillEntity created and linked to Event and Drill entities. Migration generated. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create Practice Drill DTOs</name>
  <files>apps/api/src/drills/dto/add-drill-to-plan.dto.ts, apps/api/src/drills/dto/update-practice-drill.dto.ts, apps/api/src/drills/dto/reorder-practice-drills.dto.ts</files>
  <action>
    - Create `apps/api/src/drills/dto/add-drill-to-plan.dto.ts`: `drillId` (UUID), `durationMinutes` (number), `notes` (optional string).
    - Create `apps/api/src/drills/dto/update-practice-drill.dto.ts`: `sequence` (optional number), `durationMinutes` (optional number), `teamRating` (optional number, 1-5), `notes` (optional string).
    - Create `apps/api/src/drills/dto/reorder-practice-drills.dto.ts`: `ids` (string array of UUIDs).
    - Use `class-validator` decorators for all fields.
  </action>
  <verify>
    <automated>npx nx build api</automated>
  </verify>
  <done>DTOs created with correct validation decorators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Input validation occurs here via DTOs and class-validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Input Validation | DTOs | mitigate | Use class-validator to enforce data types, UUID format, and range constraints (e.g., teamRating 1-5). |
</threat_model>

<verification>
Verify that the `PracticeDrillEntity` is correctly mapped by TypeORM and that the DTOs validate inputs as expected.
</verification>

<success_criteria>
- Entities and DTOs exist and are correctly structured.
- The project builds successfully with the new files.
</success_criteria>

<output>
After completion, create `.planning/phases/15-practice-drills-integration/15-01-SUMMARY.md`
</output>
