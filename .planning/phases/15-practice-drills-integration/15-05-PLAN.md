---
phase: 15-practice-drills-integration
plan: 05
type: execute
wave: 5
depends_on: [15-04]
files_modified:
  - libs/client/feature/practice-console/src/lib/practice-console-wrapper/practice-console-wrapper.ts
  - libs/client/feature/practice-console/src/lib/practice-plan-tab/practice-plan-tab.ts
  - libs/client/feature/practice-console/src/lib/drill-selector-modal/drill-selector-modal.ts
  - apps/frontend/src/app/app.routes.ts
  - apps/frontend/src/app/teams/events/schedule/schedule.ts
autonomous: false
requirements: [PRAC-03, DRIL-03]
must_haves:
  truths:
    - "Coaches can navigate to a Practice Console for any practice event."
    - "The console features Summary and Plan tabs using ion-segment."
    - "Coaches can add drills to the practice plan via a modal selector."
    - "Drills in the plan can be reordered using ion-reorder-group."
    - "Duration, notes, and ratings can be updated for drills in the plan."
    - "Ratings are status-gated (editable only for in_progress or completed practices)."
  artifacts:
    - path: "libs/client/feature/practice-console/src/lib/practice-console-wrapper/practice-console-wrapper.ts"
      provides: "Main tabbed interface for the practice session"
    - path: "libs/client/feature/practice-console/src/lib/practice-plan-tab/practice-plan-tab.ts"
      provides: "Drill sequencing and metadata management"
    - path: "libs/client/feature/practice-console/src/lib/drill-selector-modal/drill-selector-modal.ts"
      provides: "Modal to browse and select drills from the library"
  key_links:
    - from: "apps/frontend/src/app/teams/events/schedule/schedule.ts"
      to: "PracticeConsoleWrapper"
      via: "Router link"
    - from: "libs/client/feature/practice-console/src/lib/practice-plan-tab/practice-plan-tab.ts"
      to: "DrillSelectorModal"
      via: "IonModalController"
---

<objective>
Implement the UI and logic for managing drills within the Practice Console.
Purpose: Allow coaches to transform a simple practice event into a structured training session with sequenced drills and time allocation.
Output: PracticeConsoleWrapper, PracticePlanTab, and DrillSelectorModal components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-practice-drills-integration/15-RESEARCH.md
@.planning/phases/15-practice-drills-integration/15-CONTEXT.md
@libs/client/data-access/drill/src/lib/practice-drills.service.ts
@libs/client/feature/drill-library/src/lib/drill-list/drill-list.ts
@apps/frontend/src/app/app.routes.ts
@apps/frontend/src/app/teams/events/schedule/schedule.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PracticeConsoleWrapper component</name>
  <files>libs/client/feature/practice-console/src/lib/practice-console-wrapper/practice-console-wrapper.ts</files>
  <action>
    - Create `PracticeConsoleWrapper` in `libs/client/feature/practice-console/src/lib/practice-console-wrapper/`.
    - Use `ion-segment` for tabs: 'Summary', 'Plan'.
    - Implement navigation between tabs using a signal (`selectedSegment`).
    - Use `EventsService` and `PracticeDrillsService` to fetch data based on `teamId` and `eventId` route params.
    - Provide `event` and `plan` signals to child components.
    - Export `PracticeConsoleWrapper` from the library's `index.ts`.
  </action>
  <verify>
    <automated>npx nx build client-feature-practice-console</automated>
  </verify>
  <done>PracticeConsoleWrapper created with tabbed navigation and data fetching.</done>
</task>

<task type="auto">
  <name>Task 2: Create PracticePlanTab and DrillSelectorModal</name>
  <files>libs/client/feature/practice-console/src/lib/practice-plan-tab/practice-plan-tab.ts, libs/client/feature/practice-console/src/lib/drill-selector-modal/drill-selector-modal.ts</files>
  <action>
    - **DrillSelectorModal**:
      - Create a modal component that embeds `DrillList` from `@apex-team/client/feature/drill-library`.
      - Allow searching and filtering by tags.
      - Close modal and return selected drill(s) to the caller.
    - **PracticePlanTab**:
      - Display list of drills in the plan (ordered by sequence).
      - Use `ion-reorder-group` to handle reordering; call `practiceDrillsService.reorderDrills` on change.
      - Show duration (editable), notes (editable), and team rating (editable only if status is `in_progress` or `completed`).
      - Implement "Add Drill" button opening `DrillSelectorModal` via `ModalController`.
      - Implement individual edit/delete for drills in the plan.
  </action>
  <verify>
    <automated>npx nx build client-feature-practice-console</automated>
  </verify>
  <done>Plan management UI implemented with reordering and drill selection.</done>
</task>

<task type="auto">
  <name>Task 3: Wire routes and Schedule link</name>
  <files>apps/frontend/src/app/app.routes.ts, apps/frontend/src/app/teams/events/schedule/schedule.ts</files>
  <action>
    - **app.routes.ts**:
      - Add route `teams/:id/events/:eventId/practice` pointing to `PracticeConsoleWrapper`.
    - **schedule.ts**:
      - Update the schedule list item for 'practice' events to link to the new route.
      - Match existing pattern for 'game' events linking to the game console.
  </action>
  <verify>
    <automated>npx nx build frontend</automated>
  </verify>
  <done>Routing updated and practice sessions linked from the schedule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Practice Console UI and Drill Integration</what-built>
  <how-to-verify>
    1. Open the app and navigate to a Team's Schedule.
    2. Tap on a Practice event.
    3. Verify you are taken to the Practice Console with Summary/Plan tabs.
    4. Go to the Plan tab.
    5. Tap "Add Drill", search for a drill, and add it.
    6. Verify the drill appears in the plan.
    7. Change the duration and notes, and verify they save.
    8. Add another drill and test reordering.
    9. Change practice status to 'in_progress' (via Summary or DB) and verify ratings are editable.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Service | Input sanitization before API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | PracticePlanTab | mitigate | Ensure UI correctly enforces status-gating for ratings to prevent premature feedback. |
</threat_model>

<verification>
Verify the end-to-end flow from the schedule to the practice console.
</verification>

<success_criteria>
- Navigation to Practice Console works.
- Drills can be added, reordered, and edited.
- Metadata (duration, notes, rating) persists via the service.
</success_criteria>

<output>
After completion, create `.planning/phases/15-practice-drills-integration/15-05-SUMMARY.md`
</output>
