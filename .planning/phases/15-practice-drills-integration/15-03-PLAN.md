---
phase: 15-practice-drills-integration
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - apps/api/src/drills/practice-drills.controller.ts
  - apps/api/src/drills/drills.module.ts
autonomous: true
requirements: [PRAC-03, DRIL-03]
must_haves:
  truths:
    - "REST API endpoints are exposed for managing practice drills."
    - "Endpoints are protected by JWT authentication."
    - "The DrillsModule correctly registers the new entity, service, and controller."
  artifacts:
    - path: "apps/api/src/drills/practice-drills.controller.ts"
      provides: "PracticeDrillsController with REST endpoints"
    - path: "apps/api/src/drills/drills.module.ts"
      provides: "Module integration for practice drills"
  key_links:
    - from: "apps/api/src/drills/practice-drills.controller.ts"
      to: "apps/api/src/drills/practice-drills.service.ts"
      via: "Dependency injection"
---

<objective>
Expose the practice drills functionality via a REST API and integrate it into the application module.
Purpose: Allow the frontend to interact with the practice plan data.
Output: PracticeDrillsController and updated DrillsModule.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-practice-drills-integration/15-RESEARCH.md
@apps/api/src/drills/practice-drills.service.ts
@apps/api/src/drills/drills.module.ts

<interfaces>
From apps/api/src/drills/practice-drills.service.ts:
```typescript
export class PracticeDrillsService {
  async findAllForEvent(userId: string, eventId: string): Promise<PracticeDrillEntity[]>;
  async addDrillToPlan(userId: string, eventId: string, dto: AddDrillToPlanDto): Promise<PracticeDrillEntity>;
  async update(userId: string, eventId: string, practiceDrillId: string, dto: UpdatePracticeDrillDto): Promise<PracticeDrillEntity>;
  async remove(userId: string, eventId: string, practiceDrillId: string): Promise<void>;
  async reorder(userId: string, eventId: string, dto: ReorderPracticeDrillsDto): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PracticeDrillsController</name>
  <files>apps/api/src/drills/practice-drills.controller.ts</files>
  <action>
    - Create `PracticeDrillsController` in `apps/api/src/drills/practice-drills.controller.ts`.
    - Apply `@UseGuards(JwtAuthGuard)` to the controller or methods.
    - Implement routes (use `@req.user.id` for userId):
      - `GET /teams/:teamId/events/:eventId/drills` -> `findAllForEvent`
      - `POST /teams/:teamId/events/:eventId/drills` -> `addDrillToPlan`
      - `PATCH /teams/:teamId/events/:eventId/drills/:id` -> `update`
      - `DELETE /teams/:teamId/events/:eventId/drills/:id` -> `remove`
      - `PUT /teams/:teamId/events/:eventId/drills/reorder` -> `reorder`
  </action>
  <verify>
    <automated>npx nx build api</automated>
  </verify>
  <done>Controller implemented with all required routes. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update DrillsModule</name>
  <files>apps/api/src/drills/drills.module.ts</files>
  <action>
    - Import `PracticeDrillEntity` and add it to `TypeOrmModule.forFeature`.
    - Register `PracticeDrillsService` and `PracticeDrillsController` in the module.
  </action>
  <verify>
    <automated>npx nx build api</automated>
  </verify>
  <done>DrillsModule updated and project builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Authentication enforced via JwtAuthGuard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Spoofing | PracticeDrillsController | mitigate | Use JwtAuthGuard to ensure only authenticated users can access the endpoints. |
</threat_model>

<verification>
Perform a build check to ensure all dependencies are correctly wired.
</verification>

<success_criteria>
- All endpoints are correctly mapped and protected.
- The project builds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/15-practice-drills-integration/15-03-SUMMARY.md`
</output>
