---
phase: 19-tactical-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [libs/client/feature/game-console/src/lib/live-game-state.service.ts, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.html, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.scss]
autonomous: true
requirements: [ROTN-04, ROTN-05]
must_haves:
  truths:
    - "Rotation alert banner appears exactly at the interval boundary (e.g., 8:00, 16:00)"
    - "Alert banner is skipped if manual substitutions are already staged"
    - "Last triggered interval persists across page refreshes"
    - "Clicking 'Apply' stages engine suggestions into the Sub-Queue"
  key_links:
    - "LiveGameStateService.lastIntervalTriggered -> ConsoleWrapper alert effect"
    - "ConsoleWrapper.rotationAlertVisible -> ion-toolbar condition"
    - "ConsoleWrapper.handleApplySuggestions -> RotationService.generateSuggestions"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/live-game-state.service.ts"
      provides: "Persisted lastIntervalTriggered state"
    - path: "libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts"
      provides: "Alert lifecycle and skip logic"
    - path: "libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.html"
      provides: "Rotation alert banner UI"
---

<objective>
Implement the Rotation Alert Banner system with state persistence and conflict resolution logic.

Purpose: Bridge the gap between automated rotation logic and manual coach control, ensuring alerts are high-visibility but non-obstructive.
Output: Persisted rotation state, skip-if-staged logic, and a conditional banner UI in the game console.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tactical-polish/19-RESEARCH.md
@.planning/phases/19-tactical-polish/19-CONTEXT.md
@libs/client/feature/game-console/src/lib/live-game-state.service.ts
@libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts
@libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist lastIntervalTriggered in LiveGameStateService</name>
  <files>libs/client/feature/game-console/src/lib/live-game-state.service.ts</files>
  <action>
    - Add `_lastIntervalTriggered` signal (default: 0) and a public readonly `lastIntervalTriggered`.
    - Create `setLastIntervalTriggered(interval: number)` method.
    - Update `initialize()` to load `lastIntervalTriggered` from `localStorage` using key `rotation-state-${eventId}`.
    - Implement `saveRotationState()` private method that persists the interval and call it when the interval is updated.
    - (per D-XX in Phase 19) Ensure this state survives page refresh.
  </action>
  <verify>
    <automated>nx test client-feature-game-console --testFile=live-game-state.service.spec.ts</automated>
  </verify>
  <done>lastIntervalTriggered is persisted and recoverable across refreshes</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ConsoleWrapper Alert Logic</name>
  <files>libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts</files>
  <action>
    - Add `rotationAlertVisible = signal(false)` to `ConsoleWrapper`.
    - Update the `effect` monitoring `clockService.elapsedMs()`:
      - Use `stateService.lastIntervalTriggered()` instead of a local variable.
      - If a new interval is reached:
        - Immediately call `stateService.setLastIntervalTriggered(currentInterval)`.
        - If `stateService.stagedSubs().length > 0`, return (Skip alert logic).
        - Otherwise, set `rotationAlertVisible(true)` and call `triggerRotationAlert()`.
    - Update `triggerRotationAlert()` to ONLY handle haptics (remove the auto-staging of suggestions).
    - Implement `handleApplySuggestions()`:
      - Call `rotationService.generateSuggestions(config)`.
      - Stage the suggestions using `stateService.stageSub()`.
      - Set `rotationAlertVisible(false)`.
    - Implement `handleDismissAlert()`:
      - Set `rotationAlertVisible(false)`.
  </action>
  <verify>
    <automated>nx test client-feature-game-console --testFile=console-wrapper.spec.ts</automated>
  </verify>
  <done>Alert logic correctly handles skipping and triggers the banner signal</done>
</task>

<task type="auto">
  <name>Task 3: Implement Rotation Alert Banner UI</name>
  <files>libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.html, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.scss</files>
  <action>
    - Update `console-wrapper.html`:
      - Add a second `ion-toolbar` inside `ion-header`, right below the primary toolbar.
      - Conditionally render it using `*ngIf="rotationAlertVisible()"`.
      - Set `color="warning"` or a high-contrast style.
      - Content: "Rotation Interval Reached" text + "Apply" and "Dismiss" buttons.
    - Update `console-wrapper.scss`:
      - Style the banner to be high-contrast and non-obstructive.
      - Ensure buttons are easily tappable on mobile (Athletic Professional design).
  </action>
  <verify>
    <automated>nx test client-feature-game-console --testFile=console-wrapper.spec.ts</automated>
  </verify>
  <done>Rotation alert banner is visually present and interactive in the header</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Game Clock -> Alert Logic | Clock data must be monotonically increasing to prevent alert loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Denial of Service | Rotation Alert Effect | mitigate | Ensure lastIntervalTriggered is updated before any async calls to prevent re-triggering while alert is visible |
</threat_model>

<verification>
1. Run `nx test client-feature-game-console`.
2. Manual verification: Start game, wait for interval, confirm banner appears. Stage sub before interval, confirm banner is skipped.
</verification>

<success_criteria>
- Banner appears at interval boundary if no subs staged.
- Banner skips if subs staged.
- Banner state (last interval) persists across refresh.
- Apply button populates Sub-Queue.
- Dismiss button clears banner.
</success_criteria>

<output>
After completion, create `.planning/phases/19-tactical-polish/19-01-SUMMARY.md`
</output>
