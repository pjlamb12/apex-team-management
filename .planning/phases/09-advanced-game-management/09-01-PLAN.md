---
phase: 09-advanced-game-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/entities/season.entity.ts
  - apps/api/src/entities/game.entity.ts
  - apps/api/src/entities/lineup-entry.entity.ts
  - apps/api/src/teams/dto/create-season.dto.ts
  - apps/api/src/teams/dto/update-season.dto.ts
  - apps/api/src/games/dto/create-game.dto.ts
  - apps/api/src/games/dto/update-game.dto.ts
  - apps/api/src/games/dto/save-lineup.dto.ts
  - apps/api/src/games/games.service.ts
autonomous: true
requirements: [SEAS-01, SEAS-02, GAME-05, LIVE-05, LIVE-09]
must_haves:
  truths:
    - "Seasons can store default venue and jersey colors"
    - "Games track if they are home games"
    - "Lineup entries store a persistent slot index (0-10)"
  artifacts:
    - path: "apps/api/src/migrations/*-SeasonDefaultsAndSlots.ts"
      provides: "Schema update for defaults and slots"
    - path: "apps/api/src/entities/season.entity.ts"
      provides: "Default venue/color fields"
  key_links:
    - from: "apps/api/src/games/games.service.ts"
      to: "apps/api/src/entities/game.entity.ts"
      via: "Mapping isHomeGame in create()"
---

<objective>
Extend the backend data model to support season-level defaults and slot-based lineup tracking. 

Purpose: Provide the persistent storage layer for reactive UI defaults and reliable position-based substitution logic.
Output: Updated entities, DTOs, and a database migration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-advanced-game-management/09-CONTEXT.md
@.planning/phases/09-advanced-game-management/09-RESEARCH.md
@apps/api/src/entities/season.entity.ts
@apps/api/src/entities/game.entity.ts
@apps/api/src/entities/lineup-entry.entity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Entities</name>
  <files>apps/api/src/entities/season.entity.ts, apps/api/src/entities/game.entity.ts, apps/api/src/entities/lineup-entry.entity.ts</files>
  <action>
    - Update `SeasonEntity`: Add `defaultHomeVenue` (string, nullable), `defaultHomeColor` (string, nullable), `defaultAwayColor` (string, nullable).
    - Update `GameEntity`: Add `isHomeGame` (boolean, default: true).
    - Update `LineupEntryEntity`: Add `slotIndex` (int, nullable - though starting players MUST have one).
  </action>
  <verify>
    <automated>npm run test apps/api -- --watch=false</automated>
  </verify>
  <done>Entities updated with new columns</done>
</task>

<task type="auto">
  <name>Task 2: Update DTOs and Services</name>
  <files>apps/api/src/teams/dto/create-season.dto.ts, apps/api/src/teams/dto/update-season.dto.ts, apps/api/src/games/dto/create-game.dto.ts, apps/api/src/games/dto/update-game.dto.ts, apps/api/src/games/dto/save-lineup.dto.ts, apps/api/src/games/games.service.ts</files>
  <action>
    - Add new fields to all relevant DTOs with appropriate Class Validator decorators (IsString, IsBoolean, IsOptional, IsInt).
    - Update `GamesService.create()` to map `isHomeGame` from DTO to entity.
    - Update `GamesService.saveLineup()` to map `slotIndex` from the lineup entry DTO to the entity.
  </action>
  <verify>
    <automated>npm run test apps/api -- --watch=false</automated>
  </verify>
  <done>DTOs and mapping logic updated</done>
</task>

<task type="auto">
  <name>Task 3: Create and Run Migration</name>
  <files>apps/api/src/migrations/*-SeasonDefaultsAndSlots.ts</files>
  <action>
    - Generate a new migration: `npx nx run api:typeorm migration:generate apps/api/src/migrations/SeasonDefaultsAndSlots`
    - **CRITICAL**: Manually update the migration file's `up()` method to prevent state corruption.
    - Add logic to assign `slotIndex` (0-10) to existing `lineup_entries` where `status = 'starting'`.
    - Group by `game_id` and assign indices sequentially from 0 up to 10 for each game's starting players.
    - Verify migration contains `ALTER TABLE` commands for `seasons`, `games`, and `lineup_entries`.
    - Run migration: `npx nx run api:typeorm migration:run`
  </action>
  <verify>
    <automated>npx nx run api:typeorm migration:show</automated>
  </verify>
  <done>Database schema updated and existing starting entries assigned slot indices</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API → Database | Migration adds new nullable columns; existing data remains safe. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Update DTOs | mitigate | Use Class Validator decorators (IsString, IsInt) to ensure clean input. |
</threat_model>

<verification>
Verify entity changes reflect in DB schema and DTOs accept new fields without error.
</verification>

<success_criteria>
- SeasonEntity has defaultHomeVenue, defaultHomeColor, defaultAwayColor.
- GameEntity has isHomeGame.
- LineupEntryEntity has slotIndex.
- Existing starting players have valid slotIndex (0-10) assigned per game.
- Migration is successfully applied to PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-game-management/09-01-SUMMARY.md`
</output>
