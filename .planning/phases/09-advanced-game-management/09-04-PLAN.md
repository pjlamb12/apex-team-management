---
phase: 09-advanced-game-management
plan: 04
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts
  - libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.html
  - libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts
autonomous: false
requirements: [LIVE-09, LIVE-10]
must_haves:
  truths:
    - "Players on the pitch are rendered at fixed coordinates based on their slot index"
    - "Tapping two on-field players triggers a position swap"
    - "Bench players take the exact pitch location of the player they replace"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts"
      provides: "Slot-to-coordinate mapping"
  key_links:
    - from: "ConsoleWrapper"
      to: "POSITION_SWAP event"
      via: "interaction logic"
---

<objective>
Implement high-fidelity pitch interactions including fixed-slot rendering and on-field position swaps.

Purpose: Provide a tactile, reliable interface for making quick tactical adjustments during a game.
Output: Slot-aware pitch visualization and advanced 2-tap interaction logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-advanced-game-management/09-CONTEXT.md
@.planning/phases/09-advanced-game-management/09-RESEARCH.md
@libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts
@libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixed Slot Pitch Visualization</name>
  <files>libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts, libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.html</files>
  <action>
    - Update `soccer-pitch-view.ts`: Define a coordinate map for slots 0–10 (e.g., standard 4-4-2 formation layout).
    - Map each player in the `activePlayers()` signal to their assigned coordinate based on `slotIndex`.
    - Use `slotIndex` as the key for rendering, ensuring players transition smoothly between slots.
  </action>
  <verify>
    <automated>npx nx run client-feature-game-console:lint</automated>
  </verify>
  <done>Pitch renders based on fixed slot coordinates</done>
</task>

<task type="auto">
  <name>Task 2: Advanced Selection Logic</name>
  <files>libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts</files>
  <action>
    - Update selection logic in `console-wrapper.ts`:
      - Current Player (Selected) is Active AND New Player (Tapped) is Active:
        - Emit `POSITION_SWAP` event to `LiveGameStateService`.
        - Deselect both.
      - Current Player (Selected) is Bench AND New Player (Tapped) is Active:
        - Emit `SUB` event using the Active player's `slotIndex`.
        - Deselect both.
      - Current Player (Selected) is Active AND New Player (Tapped) is Bench:
        - Emit `SUB` event using the Active player's `slotIndex`.
        - Deselect both.
  </action>
  <verify>
    <automated>npx nx test client-feature-game-console --testFile=console-wrapper.spec.ts</automated>
  </verify>
  <done>2-tap position swaps and slot-based subs implemented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Advanced pitch interactions</what-built>
  <how-to-verify>
    1. Open Game Console.
    2. Tap an on-field player (e.g., Defender).
    3. Tap another on-field player (e.g., Forward).
    4. Verify they swap positions on the pitch.
    5. Tap a bench player, then an on-field player.
    6. Verify the sub occurs and the new player takes the exact spot of the old one.
    7. Perform multiple swaps and subs, then use UNDO to verify state rolls back correctly to previous positions.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Service | Interactions trigger events that update the local state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Interaction State | mitigate | Verify players are in appropriate pools (Active/Bench) before emitting events. |
</threat_model>

<verification>
Manual verification of 2-tap interaction flow and slot persistence across sub/swap.
</verification>

<success_criteria>
- 2-tap on-field swap works correctly.
- Subbed players take the exact location (slot) of the outgoing player.
- All interactions are stable and undoable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-game-management/09-04-SUMMARY.md`
</output>
