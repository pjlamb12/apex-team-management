---
phase: 17-tactical-sub-queue
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified: [libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts, libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.html, libs/client/feature/game-console/src/lib/bench-view/bench-view.ts, libs/client/feature/game-console/src/lib/bench-view/bench-view.html, libs/client/feature/game-console/src/lib/sub-queue/sub-queue.ts, libs/client/feature/game-console/src/lib/sub-queue/sub-queue.html, libs/client/feature/game-console/src/lib/sub-queue/sub-queue.scss]
autonomous: true
requirements: [SUBS-04]
must_haves:
  truths:
    - "SoccerPitchView dynamically looks up slot indices for staged players from the current activePlayers state"
    - "Incoming players are displayed in their target slots with a 'staged' visual style"
    - "BenchView identifies players who are already staged to enter the game"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/sub-queue/sub-queue.ts"
      provides: "Queue display with player metadata lookup"
  key_links:
    - from: "SoccerPitchView"
      to: "LiveGameStateService.activePlayers"
      via: "dynamic lookup for StagedSub.outPlayerId"
---

<objective>
Implement the UI components for the tactical sub-queue, utilizing dynamic slot lookup to ensure visual accuracy.

Purpose: Provide the coach with clear, real-time visual feedback of pending changes.
Output: New SubQueueComponent and enhanced Pitch/Bench views.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts
@libs/client/feature/game-console/src/lib/bench-view/bench-view.ts
@libs/client/feature/game-console/src/lib/live-game-state.service.ts

<interfaces>
From libs/client/feature/game-console/src/lib/live-game-state.service.ts:
```typescript
export interface StagedSub {
  inPlayerId: string;
  outPlayerId: string;
}

// activePlayers() returns:
// { id: string, name: string, number: string, slotIndex: number, preferredPosition: string, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SoccerPitchView with dynamic staging indicators</name>
  <files>libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts, libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.html</files>
  <action>
    - Add `stagedSubs` input signal to `SoccerPitchView`.
    - Create a computed signal `stagedInPlayers` that:
        - Iterates over `stagedSubs()`.
        - For each sub, finds the `outPlayer` in `activePlayers()`.
        - If found, retrieves the `slotIndex`.
        - Creates a view-model object for the `inPlayer` (from `initialLineup()`) and assigns the `slotIndex` of the `outPlayer`.
        - Adds `isStaged: true` property.
    - Create a computed signal `stagedOutIds` (Set of `outPlayerId` from `stagedSubs()`).
    - Update `soccer-pitch-view.html`:
        - Apply a "staged-out" CSS class (e.g., opacity-50, grayscale) to players whose ID is in `stagedOutIds()`.
        - Render `stagedInPlayers()` using the same slot-based positioning logic as active players, but with "staged-in" styling (e.g., dashed border, semi-transparent, "IN" badge).
  </action>
  <verify>
    Template compiles and correctly references the dynamic slot indices from activePlayers.
  </verify>
  <done>Pitch view correctly overlays staged substitutions based on current game state.</done>
</task>

<task type="auto">
  <name>Task 2: Update BenchView for staging</name>
  <files>libs/client/feature/game-console/src/lib/bench-view/bench-view.ts, libs/client/feature/game-console/src/lib/bench-view/bench-view.html</files>
  <action>
    - Add `stagedInIds` input signal (Set<string>).
    - In `bench-view.html`, apply a visual indicator (e.g., "STAGED" label or yellow border) to any player whose ID is in the `stagedInIds` set.
  </action>
  <verify>
    Bench players show staging status when relevant.
  </verify>
  <done>Bench view identifies pending players.</done>
</task>

<task type="auto">
  <name>Task 3: Create SubQueueComponent</name>
  <files>libs/client/feature/game-console/src/lib/sub-queue/sub-queue.ts, libs/client/feature/game-console/src/lib/sub-queue/sub-queue.html, libs/client/feature/game-console/src/lib/sub-queue/sub-queue.scss</files>
  <action>
    Create a standalone component for the sub-queue.
    - Inputs: `stagedSubs` (signal).
    - Outputs: `apply`, `clear`, `remove` (emits playerId).
    - UI: Displays list of pairs. Since `StagedSub` only has IDs, the component must look up player metadata (names/numbers) from the `initialLineup` (pass this in as an input or inject service).
    - Display format: "OUT: #10 Smith -> IN: #22 Jones".
    - Buttons: "Apply [N] Subs" and "Clear All".
  </action>
  <verify>
    Component renders and emits events correctly.
  </verify>
  <done>SubQueueComponent built and ready for integration.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Information Disclosure | SoccerPitchView | accept | Staged subs are visual indicators for the coach only. |
</threat_model>

<success_criteria>
- Pitch view renders incoming players at the correct slots by looking up outgoing player positions.
- Bench view highlights staged-in players.
- SubQueue component displays human-readable pairs.
</success_criteria>

<output>
After completion, create `.planning/phases/17-tactical-sub-queue/17-02-SUMMARY.md`
</output>
