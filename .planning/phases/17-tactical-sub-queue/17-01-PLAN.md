---
phase: 17-tactical-sub-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [libs/client/feature/game-console/src/lib/live-game-state.service.ts, libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts]
autonomous: true
requirements: [SUBS-01, SUBS-02]
must_haves:
  truths:
    - "LiveGameStateService supports bulk event pushing in a single signal update"
    - "StagedSub interface only contains inPlayerId and outPlayerId"
    - "Staging logic prevents a player from being involved in more than one pending substitution"
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/live-game-state.service.ts"
      provides: "Sub-queue state and bulk push logic"
  key_links:
    - from: "LiveGameStateService.pushEvents"
      to: "localStorage"
      via: "single save() call"
---

<objective>
Update LiveGameStateService to support bulk event updates and a streamlined staging mechanism.

Purpose: Provide the foundational state logic for tactical substitutions while ensuring data consistency and efficiency.
Output: Extended LiveGameStateService with pushEvents and optimized staging logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@libs/client/feature/game-console/src/lib/live-game-state.service.ts
@libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts

<interfaces>
From libs/client/feature/game-console/src/lib/live-game-state.service.ts:
```typescript
export interface GameEvent {
  type: string;
  playerIdIn?: string;
  playerIdOut?: string;
  slotIndex?: number;
  minuteOccurred: number;
  timestamp: number;
  status?: 'active' | 'deleted';
  // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update LiveGameStateService with bulk push and staging logic</name>
  <files>libs/client/feature/game-console/src/lib/live-game-state.service.ts</files>
  <behavior>
    - Define `StagedSub` interface: `{ inPlayerId: string; outPlayerId: string; }`. (Note: slotIndex and positionName removed per feedback).
    - Add private `_stagedSubs` signal and public `stagedSubs` asReadonly.
    - Implement `pushEvents(events: GameEvent[])`:
        - Update `_events` signal once using a single `.update()` call.
        - Ensure each event is decorated with `{ status: 'active', period: this._currentPeriod() }`.
        - Call `this.save()` exactly once.
    - Implement `stageSub(inPlayerId: string, outPlayerId: string)`:
        - Logic: Remove any existing staged sub where `inPlayerId` OR `outPlayerId` matches either of the new IDs. (Prevents a player from being in multiple pairs).
        - Add the new `{ inPlayerId, outPlayerId }` pair.
    - Implement `unstageSub(playerId: string)`: Removes any sub involving this player ID.
    - Implement `clearStagedSubs()`: Resets `_stagedSubs` to `[]`.
  </behavior>
  <action>
    Implement bulk event processing and the volatile staging state. 
    Ensure `pushEvents` minimizes side effects (one signal write, one disk write).
    Validation in `stageSub` ensures that if Bench-A is staged to replace Pitch-X, then staging Bench-A to replace Pitch-Y will remove the (A, X) pair first.
  </action>
  <verify>
    <automated>npx vitest libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts</automated>
  </verify>
  <done>Service updated with bulk push and validated staging logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests for bulk push and staging constraints</name>
  <files>libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts</files>
  <behavior>
    - Test `pushEvents`: Verifies multiple events are added and `save` is called.
    - Test `stageSub` exclusivity: 
        - Stage (A, B). Then stage (C, B) -> Only (C, B) should remain.
        - Stage (A, B). Then stage (A, D) -> Only (A, D) should remain.
        - Stage (A, B). Then stage (B, C) -> Only (B, C) should remain (prevents crazy swaps).
    - Test `unstageSub`: Verifies player is removed from any pair they were in.
    - Test `clearStagedSubs`: Verifies queue is emptied.
  </behavior>
  <action>
    Add comprehensive tests for the new methods, specifically focusing on the uniqueness constraints for staged substitutions.
  </action>
  <verify>
    <automated>npx vitest libs/client/feature/game-console/src/lib/live-game-state.service.spec.ts</automated>
  </verify>
  <done>All service logic tests pass.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | LiveGameStateService | mitigate | Staging is local-only and volatile, reducing persistence-based tampering risk. |
| T-17-01-02 | Integrity | pushEvents | mitigate | Bulk push ensures atomic local state update, reducing chance of partial application in case of crash. |
</threat_model>

<success_criteria>
- `pushEvents` implemented and tested.
- `StagedSub` uses minimal interface.
- `stageSub` enforces player uniqueness.
</success_criteria>

<output>
After completion, create `.planning/phases/17-tactical-sub-queue/17-01-SUMMARY.md`
</output>
