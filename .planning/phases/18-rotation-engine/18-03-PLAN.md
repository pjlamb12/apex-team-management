---
phase: 18-rotation-engine
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified: [libs/client/feature/game-console/src/lib/live-game-state.service.ts, libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts, libs/client/feature/game-console/src/lib/bench-view/bench-view.html, libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.html, libs/client/feature/game-console/src/lib/live-clock.service.ts]
autonomous: true
requirements: [ROTN-01, ROTN-04]
must_haves:
  truths:
    - "Coach can configure rotation interval and mode."
    - "System triggers a haptic alert when the rotation interval is reached."
    - "Suggestions are automatically staged into the sub-queue at the interval."
    - "Cumulative playtime is visible on player cards in the UI."
  artifacts:
    - path: "libs/client/feature/game-console/src/lib/live-game-state.service.ts"
      provides: "Rotation configuration state"
  key_links:
    - from: "ConsoleWrapper"
      to: "RotationService"
      via: "DI and method call"
    - from: "ConsoleWrapper"
      to: "Capacitor Haptics"
      via: "direct call on interval"
---

<objective>
Integrate the rotation engine into the live game console, providing configuration, interval-based alerts, and real-time playtime visualization.

Purpose: Surface the engine's benefits to the coach and automate the sub-staging workflow.
Output: Integrated rotation alerts and playtime display.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-rotation-engine/18-CONTEXT.md
@.planning/phases/18-rotation-engine/18-RESEARCH.md
@libs/client/feature/game-console/src/lib/live-clock.service.ts
@libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts
@libs/client/feature/game-console/src/lib/rotation-engine/playtime.service.ts
</context>

<tasks>

<task type="auto">
  <name>Add Rotation Configuration to State</name>
  <files>libs/client/feature/game-console/src/lib/live-game-state.service.ts</files>
  <action>
    - Add `RotationConfig` interface and signal to `LiveGameStateService`:
      ```typescript
      export interface RotationConfig {
        enabled: boolean;
        intervalMinutes: number;
        mode: 'PURE' | 'POSITION' | 'CONSTRAINT';
        minBenchMinutes?: number;
        maxFieldMinutes?: number;
      }
      ```
    - Initialize with defaults (e.g., enabled: false, interval: 8, mode: 'PURE').
    - Persist to `localStorage` alongside events.
    - Provide a `updateRotationConfig(config: Partial<RotationConfig>)` method.
  </action>
  <verify>
    <automated>grep "rotationConfig" libs/client/feature/game-console/src/lib/live-game-state.service.ts</automated>
  </verify>
  <done>Rotation configuration is managed and persisted in LiveGameStateService.</done>
</task>

<task type="auto">
  <name>Implement Interval Monitoring and Auto-Staging</name>
  <files>libs/client/feature/game-console/src/lib/console-wrapper/console-wrapper.ts, libs/client/feature/game-console/src/lib/live-clock.service.ts</files>
  <action>
    - In `ConsoleWrapper`, create an effect or computed signal that monitors `clockService.elapsedMs()`.
    - When `elapsedMs` crosses a multiple of `rotationConfig().intervalMinutes * 60000`:
      1. Trigger `Haptics.notification({ type: NotificationType.Success })`.
      2. Call `rotationService.generateSuggestions(rotationConfig().mode)`.
      3. For each suggestion, call `stateService.stageSub(inPlayerId, outPlayerId)`.
    - Ensure it only triggers once per interval boundary (e.g., track `lastIntervalTriggered`).
    - Add a simple "Rotation Settings" button/popover in `console-wrapper.html` to toggle and set the interval.
  </action>
  <verify>
    <automated>npx nx test client-feature-game-console</automated>
  </verify>
  <done>Console automatically stages subs and alerts the coach at the specified interval.</done>
</task>

<task type="auto">
  <name>Display Playtime in UI</name>
  <files>libs/client/feature/game-console/src/lib/bench-view/bench-view.html, libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.html, libs/client/feature/game-console/src/lib/bench-view/bench-view.ts, libs/client/feature/game-console/src/lib/soccer-pitch-view/soccer-pitch-view.ts</files>
  <action>
    - Inject `PlaytimeService` into `BenchViewComponent` and `SoccerPitchViewComponent`.
    - Map player lists to include their current playtime from `playtimeService.playtimeMap()`.
    - Update templates to show playtime badge (e.g., `8:45`) on each player card/avatar.
    - Style the badge to be small and high-contrast.
  </action>
  <verify>
    <automated>grep "playtimeService" libs/client/feature/game-console/src/lib/bench-view/bench-view.ts</automated>
  </verify>
  <done>Cumulative playtime is visible for every player in the Live Console.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System -> UI | Real-time updates must not block the main thread (UI lag). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information Disclosure | Playtime Display | accept | playtime is public info for coaches. |
</threat_model>

<success_criteria>
- Interval alert triggers correctly at 8, 16, 24 mins (etc.).
- Auto-staging populates the Sub-Queue without manual input.
- Playtime display is readable and updates live.
</success_criteria>

<output>
After completion, create `.planning/phases/18-rotation-engine/18-03-SUMMARY.md`
</output>
