---
phase: 34-export-polish
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - package.json
  - libs/client/data-access/team/src/lib/analytics.service.ts
  - apps/frontend/src/app/teams/team-dashboard/analytics/analytics.ts
  - apps/frontend/src/app/teams/team-dashboard/analytics/analytics.html
  - apps/frontend/src/app/teams/team-dashboard/analytics/export-modal/export-modal.ts
  - apps/frontend/src/app/teams/team-dashboard/analytics/export-modal/export-modal.html
autonomous: false # Has human-verify for export results
requirements: [ANLY-DASH-03]

must_haves:
  truths:
    - "Coach can configure export options via a modal"
    - "Exports work on both Web (direct download) and Mobile (native sharing)"
  artifacts:
    - path: "apps/frontend/src/app/teams/team-dashboard/analytics/export-modal/export-modal.ts"
      provides: "Export configuration UI"
    - path: "libs/client/data-access/team/src/lib/analytics.service.ts"
      provides: "Export trigger methods"
---

<objective>
Create the Export Modal and integrate it with the backend endpoints, supporting native mobile sharing.

Purpose: Provide an intuitive UI for coaches to generate and share their analytics data.
Output: A new configuration modal and updated Analytics Dashboard with export capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/frontend/src/app/teams/team-dashboard/analytics/analytics.ts
@libs/client/data-access/team/src/lib/analytics.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Export Modal and Dashboard Integration</name>
  <files>apps/frontend/src/app/teams/team-dashboard/analytics/export-modal/export-modal.ts, apps/frontend/src/app/teams/team-dashboard/analytics/export-modal/export-modal.html, apps/frontend/src/app/teams/team-dashboard/analytics/analytics.ts, apps/frontend/src/app/teams/team-dashboard/analytics/analytics.html</files>
  <action>
    - Create `ExportModalComponent` (Standalone) in `apps/frontend/src/app/teams/team-dashboard/analytics/export-modal/`.
    - Modal should allow selecting:
        - Format (PDF/CSV)
        - Layout (Overview/Player Pack/Tabular)
        - Granularity (Aggregated/Per-game)
        - Visuals Toggle
    - Use Angular signals for modal state.
    - Add an "Export" button (icon: `download-outline`) to the `TeamAnalytics` toolbar.
    - Implement `openExportModal()` to launch the modal via `ModalController`.
  </action>
  <verify>
    <automated>npx nx test frontend --testFile=export-modal</automated>
  </verify>
  <done>Export modal is accessible and allows user configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement File Export Logic with Mobile Support</name>
  <files>package.json, libs/client/data-access/team/src/lib/analytics.service.ts</files>
  <action>
    - Install Capacitor plugins: `@capacitor/filesystem`, `@capacitor/share`, `@capacitor/file-transfer`.
    - Run `npx cap sync`.
    - Update `AnalyticsService`:
        - Add `exportData(teamId: string, options: ExportOptions): Observable<Blob>` using `HttpClient`.
    - Implement the download/share logic in `ExportModalComponent`:
        - **Web**: Create a temporary link and trigger `click()` to download the Blob.
        - **Mobile**: Use `FileTransfer.download()` to save the file to `Directory.Cache`, then `Share.share()` to open the native share sheet (Pitfall 3).
  </action>
  <verify>
    <automated>npx nx lint frontend</automated>
  </verify>
  <done>Exported files are correctly handled on both web and mobile platforms.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Export functionality (PDF/CSV) across Web and Mobile.</what-built>
  <how-to-verify>
    1. Run the app in a browser.
    2. Go to Team Analytics -> Export.
    3. Select PDF (Overview) and download. Verify the PDF looks correct.
    4. Select CSV (Aggregated) and download. Verify the CSV data is accurate.
    5. (If possible) Run on a mobile emulator/device and verify the Share sheet opens.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Client | Exported file content (Blob) |
| Client → Mobile OS | Local file sharing via Share sheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Information Disclosure | `Capacitor Share` | accept | Files are stored in temporary cache and shared via user action; no sensitive PII is exposed beyond what's in the dashboard. |
</threat_model>

<verification>
Verify export flow from start to finish: Modal configuration -> Service call -> File delivery -> Native sharing.
</verification>

<success_criteria>
- Export modal opens and captures user preferences.
- Web downloads trigger correctly with proper filenames.
- Mobile sharing uses native OS components.
</success_criteria>

<output>
After completion, create `.planning/phases/34-export-polish/34-03-SUMMARY.md`
</output>
