---
phase: 34-export-polish
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - apps/api/src/analytics/export/pdf-export.service.ts
  - apps/api/src/analytics/export/templates/report.hbs
  - apps/api/src/analytics/analytics.controller.ts
  - apps/api/src/analytics/analytics.module.ts
autonomous: true
requirements: [ANLY-DASH-03]

must_haves:
  truths:
    - "Backend generates high-fidelity PDF reports with Tailwind styling"
    - "Reports support multiple layouts: Overview, Player Pack, and Tabular"
  artifacts:
    - path: "apps/api/src/analytics/export/pdf-export.service.ts"
      provides: "Puppeteer-based PDF generation"
    - path: "apps/api/src/analytics/export/templates/report.hbs"
      provides: "Handlebars report template"
---

<objective>
Implement high-fidelity PDF generation using Puppeteer and Handlebars.

Purpose: Provide coaches with professional, shareable reports that mirror the dashboard aesthetic.
Output: A new PDF export endpoint and a sophisticated PDF generation service with multi-layout support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/src/analytics/analytics.controller.ts
@apps/api/src/analytics/dto/export-options.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PDF Export Service with Puppeteer</name>
  <files>apps/api/src/analytics/export/pdf-export.service.ts, apps/api/src/analytics/analytics.module.ts</files>
  <action>
    - Create `PdfExportService` in `apps/api/src/analytics/export/pdf-export.service.ts`.
    - Implement `generate(teamId: string, options: ExportOptionsDto)`:
        - Launch Puppeteer with safe defaults (`--no-sandbox`, `--disable-setuid-sandbox`).
        - Use Handlebars to compile `report.hbs` with data from `PerformanceMetricsService` and `PlayingTimeService`.
        - Inject Tailwind CSS into the page using `page.addStyleTag`.
        - Render the PDF with `page.pdf()` using `A4` format and proper margins.
        - Ensure the browser is closed in a `finally` block to prevent zombie processes (Pitfall 1).
    - Register `PdfExportService` in `AnalyticsModule`.
  </action>
  <verify>
    <automated>npx nx test api --testFile=pdf-export</automated>
  </verify>
  <done>PdfExportService successfully generates a PDF buffer from HTML templates.</done>
</task>

<task type="auto">
  <name>Task 2: Create Handlebars Templates for Report Layouts</name>
  <files>apps/api/src/analytics/export/templates/report.hbs, apps/api/src/analytics/analytics.controller.ts</files>
  <action>
    - Create `apps/api/src/analytics/export/templates/report.hbs`.
    - Implement three layout sections using Handlebars partials or conditional logic based on `options.layout`:
        - **Overview**: Summary cards and team charts.
        - **Player Pack**: Page break per player with individual stats and heatmaps.
        - **Tabular**: Dense table of all metrics.
    - Use Tailwind classes for styling to match the "Athletic Professional" theme (D-03).
    - Update `AnalyticsController`:
        - Add `@Get('analytics/export/pdf')` endpoint.
        - Stream the PDF buffer with `Content-Type: application/pdf`.
  </action>
  <verify>
    <automated>curl -v http://localhost:3000/teams/:teamId/analytics/export/pdf?layout=overview</automated>
  </verify>
  <done>Report templates support all specified layouts and the PDF endpoint is functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handlebars → Puppeteer | Local HTML template rendered by Chromium |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Elevation of Privilege | Puppeteer | mitigate | Run Puppeteer with `--no-sandbox` only if absolutely necessary; avoid rendering user-supplied HTML/URLs (SSRF mitigation). |
| T-34-04 | Denial of Service | Puppeteer | mitigate | Set a timeout for PDF generation (e.g., 30s) to prevent hung processes from blocking resources. |
</threat_model>

<verification>
Verify PDF export by generating reports for each layout type and visually inspecting the resulting files for styling consistency and data accuracy.
</verification>

<success_criteria>
- PDF export returns a 200 OK with `application/pdf` content type.
- PDFs for 'overview', 'player-pack', and 'tabular' layouts show appropriate data structures.
- Tailwind styles are correctly applied in the rendered PDF.
</success_criteria>

<output>
After completion, create `.planning/phases/34-export-polish/34-02-SUMMARY.md`
</output>
